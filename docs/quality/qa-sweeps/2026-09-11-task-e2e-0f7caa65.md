# Playwright E2E Suite — full smoke, 2026-09-11 (task-e2e-0f7caa65)

Auto-run card `[AUTO-RUN] Playwright E2E Suite — full smoke`, tree `f4b16d7e0`
(origin/main at dispatch). Playwright 1.58.2, Node 24.11.0, Python 3.14.0,
win32 10.0.26200.

**Result: 88 routes clean; 849 E2E tests collected, 833 passed, 0 failed,
0 flaky, 16 skipped; 68/68 spec files measured, 20.5 min at one worker.**
No failures, so no fix cards. The suite ran on the throwaway `icdev_e2e`
database and neither board gained an E2E row (§3).

## 1. Route smoke — clean

`python tools/testing/route_smoke.py --all --json --base http://127.0.0.1:5050`
against the live dashboard: **88 checks, 0 failures**, and `skipped_by_cap`,
`excluded`, `tolerated`, `stale_expect_fail` all 0, so the 88 is the whole
declared surface. The card's stop-gate did not fire. Route smoke is GET-only,
so it ran against the live dashboard; the E2E suite, which writes, did not.

## 2. Full run — one invocation, 849 tests

```
node node_modules/@playwright/test/cli.js test tests/e2e/ --project=chromium --reporter=line,json,html
```

launched detached from this worktree (an `npx` spawn detached on Windows
drops its stdout; the direct node invocation streams) with
`ICDEV_NO_SERVER=1`, `ICDEV_E2E_BASE_URL=http://127.0.0.1:5095`,
`ICDEV_DATABASE_URL=` (empty), `ICDEV_PG_DATABASE=icdev_e2e`. Started
20:43:28Z, exited 21:05:02Z.

**The zero was derived three ways.** The line reporter's summary reads
`16 skipped / 833 passed (20.5m)`. Playwright's JSON `stats` block reads
833 expected / **0 unexpected** / 0 flaky / 16 skipped with 0 top-level
`errors`. A recursive walk of every suite tree, reading each test's last
`results[].status` at any depth, gives 833 `passed` / 16 `skipped` and no
`failed` or `timedOut`, across 68 distinct spec files. All three agree.

Against the 2026-09-10 sweep (843 collected, 827 passed, 16 skipped, over 12
batches) the collected count grew by six and every one of them passed.

The run is recorded in the structured build log via
`tools.logging.build_logger.capture_playwright` (returncode 0). Artifacts:
`.tmp/test_runs/playwright-results.json`, `.tmp/test_runs/playwright-report/`
(`npx playwright show-report`), 338 screenshots under
`.tmp/test_runs/screenshots/`.

## 3. Isolation — measured, not assumed

* **Its own server.** `from tools.dashboard.app import app; app.run(port=5095)`
  from this worktree (`app.root_path` asserted under the worktree — a `.tmp/`
  launcher with `sys.path[0]` elsewhere serves the main checkout), with the
  flags `playwright.config.ts` puts in `webServer.env`, `ICDEV_DATABASE_URL`
  cleared and `ICDEV_PG_DATABASE=icdev_e2e`. Not `python tools/dashboard/app.py`:
  that `__main__` spawns a kanban scheduler against the fleet. Ports 5090 and
  5094 were held by other sessions; `netstat` showed exactly one listener on
  5095 and its command line was this server's. Terminated by pid tree after
  the run; the port is free.
* **Measured.** `/api/health` on 5095: `"database":"icdev_e2e",
  "database_measured":true`. `tests/e2e/fixtures/database_probe.py` under the
  suite's env: `icdev_e2e`. globalSetup printed both confirmations
  (`server` and `subprocess`) before `Running 849 tests`.
* **The boards, before and after** (`kanban_tasks`). Canonical `icdev`:
  4,027 rows before, 4,049 after, 0 `[E2E`-titled either time. The 22 new rows
  are one `xrv-*` project batch (22 ids, one `created_at` instant, 20:53:44Z)
  seeded by another session's planner during the window — not fixtures.
  `icdev_e2e`: 3 rows before and after (the July leak), **0 created during the
  run**. Scope: measured on `kanban_tasks`, not on every table the suite writes.

## 4. The 16 skips, enumerated

A skip is an unmeasured test, not a passing one. The set is identical to the
2026-09-10 sweep.

| n | spec | reason |
|---|---|---|
| 5 | `e2e_me_conflict_lifecycle.spec.ts` | seed session `sess-9cc6891cb548` fixture not present |
| 4 | `clawhub.spec.ts` | ClawHub service not running on port 5077 |
| 4 | `skillhub.spec.ts` | SkillHub service not running on port 5077 |
| 1 | `dwo_restart_durability.spec.ts` | opt-in — `ICDEV_E2E_DWO_RESTART=1` |
| 1 | `dwo_trigger_linkage.spec.ts` | opt-in — `ICDEV_E2E_DWO_TRIGGER=1` |
| 1 | `nav_regression_probes.spec.ts` | no seeded pulse post on this DB; covered at source by pytest |

## 5. Not measured

`icdev_e2e` is a persistent throwaway with prior state, not a fresh container,
and it shared the PostgreSQL server and host with the scheduler, pr_watcher and
several genesis daemons. Only the route smoke and the E2E suite ran; this card
changed no code, so no pytest, coherence tier or CI gate is part of its
measurement beyond this PR's own CI.

## Re-derive

```
python tools/testing/route_smoke.py --all --json --base http://127.0.0.1:5050
# isolated server: app.run() from the worktree with playwright.config.ts's webServer.env,
#   ICDEV_DATABASE_URL= ICDEV_PG_DATABASE=icdev_e2e, on a free port
ICDEV_NO_SERVER=1 ICDEV_E2E_BASE_URL=http://127.0.0.1:<port> ICDEV_PG_DATABASE=icdev_e2e \
  node node_modules/@playwright/test/cli.js test tests/e2e/ --project=chromium --reporter=line,json,html
```
