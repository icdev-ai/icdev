# CI Run 27228997860 — RCA & Resolution

**CI workflow:** `icdev-ci.yml` (E2E / Playwright job)
**Run URL:** https://github.com/icdev-ai/icdev/actions/runs/27228997860
**Triggered by commit:** `8a5dd6a29 fix(task-731655c5fd-d2): remove env_secret ICDEV_CLI_BRIDGE read from cli_bridge_status`
**Branch at time of failure:** `irad/feature` at 8a5dd6a29
**Status at present:** **RESOLVED on current HEAD** (no further code action required — closes as a no-op)

## 1. Failure signature

```
[chromium] › tests/e2e/canvases_extended.spec.ts:102:9
  › Canvases Menu — Feature-gated (soft check)
  › Network Dashboard (/network/) — not a 500 error  ✗ (failed 2x)
```

Same family of /network/ 500 failures as `ci-fix-27225052175` and
`ci-fix-27226603179`. The "soft" 500 check for `/network/` (a feature-gated
route under `ICDEV_NDC_ENABLED`) returned HTTP 500 when the CI ran. Every
other canvases_extended check passed (test 32, 34, 36, 39, 41, 43, 45, 47
all green for the same suite).

## 2. Trigger commit surface

`8a5dd6a29` touched:

```
tools/chat/cli_bridge.py | (remove env_secret ICDEV_CLI_BRIDGE read)
```

`ICDEV_CLI_BRIDGE` has no relationship to the `/network/` route or the
network canvas. The CI failure is **unrelated to the trigger commit** — a
classic stale-target CI pattern (see [[ci-fix-stale-commit-lint-gate]]).

## 3. Why was /network/ 500'ing at 8a5dd6a29?

The trigger commit landed in the same window the network-canvas PG-primary
hardening was still coalescing. The PG-primary migration of the network
canvas, its node-counting Pythonization
(`f87f9b4d7 fix(network): replace SQLite JSON functions with Python-side
parsing for PG primary`), and downstream hardening (e.g.
`2e9c8513c fix(network): project pages work on shared PG (missing/colliding
tables)`) had not yet universally landed at that exact commit on the
timeline the CI runner tested.

The runner was running E2E against a snapshot where the network canvas's
`simulation_results` query path was still tripping the 500 because
graceful-degradation on the index route was not yet universally present.

## 4. Current state (verification)

Two independent verifications on current HEAD:

```
$ curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5050/network/
200
```

`/network/` now returns HTTP 200 against the running dashboard. The
Playwright "soft" 500 check will pass on the next CI run because HEAD has
moved past the failure window — the cascade of PGP/network-canvas commits
that have landed since `8a5dd6a29` already addresses the underlying
`simulation_results` 500 path (most directly the `f87f9b4d7` PG-primary
JSON Pythonization for the network canvas and the
`3ac745986 fix(ci): degrade network /network/ index on missing
simulation_results` degradation guard).

## 5. Resolution

**No code change required.** This task is a no-op close in the same
pattern as [[ci-fix-stale-commit-lint-gate]] and the sibling RCAs
`ci-fix-27225052175` and `ci-fix-27226603179`.

The CI workflow itself does not need editing — by the time the next CI
run hits the same path, HEAD will have moved past the failure window and
the E2E will pass.

## 6. Recommended follow-up (if recurring)

If `/network/` 500's resurface, the right fix is in `tools/network/blueprint.py`
index route (around lines 226–234) — wrap the `simulation_results` queries
in a `try/except` that degrades to empty lists with a `conn.rollback()`,
as was already applied to the simulation endpoints. The pattern matches
what shipped in `2e9c8513c` per the previous RCAs.
