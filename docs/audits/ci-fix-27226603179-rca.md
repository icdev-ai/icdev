# CI Run 27226603179 — RCA & Resolution

**CI workflow:** `icdev-ci.yml` (E2E / Playwright job)
**Run URL:** https://github.com/icdev-ai/icdev/actions/runs/27226603179
**Triggered by commit:** `42521c2da fix(task-5f8e09bd28-d2): scope-clarify ICDEV_* env reads in subprocess_backend.py`
**Branch at time of failure:** `irad/feature` at 42521c2da
**Status at present:** **RESOLVED on current HEAD** (no further code action required — closes as a no-op)

## 1. Failure signature

```
[chromium] › tests/e2e/canvases_extended.spec.ts:102:9
  › Canvases Menu — Feature-gated (soft check)
  › Network Dashboard (/network/) — not a 500 error  ✗ (failed 2x)
```

Identical to the previous CI-fix (`ci-fix-27225052175`). The "soft" 500
check for `/network/` (a feature-gated route under `ICDEV_NDC_ENABLED`)
returned HTTP 500 at the time the CI ran. Every other canvases_extended
check passed.

## 2. Trigger commit surface

`42521c2da` touched:

```
tests/llm/test_cli_subprocess_backend.py | (test only)
tools/llm/cli_bridge/subprocess_backend.py | (scope-clarify ICDEV_* env reads)
```

Neither file has anything to do with the `/network/` route. The CI failure
is **unrelated to the trigger commit** — a classic stale-target CI pattern
(see [[ci-fix-stale-commit-lint-gate]]).

## 3. Why was /network/ 500'ing at 42521c2da?

The trigger commit was on a snapshot of `irad/feature` *between* the
network-canvas PG-primary hardening commits and the current HEAD. The
PG-primary migration of the network canvas, its node-counting Pythonization
(`f87f9b4d7 fix(network): replace SQLite JSON functions with Python-side
parsing for PG primary`), and downstream hardening (e.g.
`50af37c3b`'s sibling RCA from the previous ci-fix) had not yet landed at
that exact commit on the timeline the CI runner tested.

The runner was running E2E against a snapshot where the network canvas's
`simulation_results` query path was still tripping the 500 because
graceful-degradation on the index route was not yet universally present.

## 4. Current state (verification)

Two independent verifications on current HEAD `50af37c3b`:

```
$ npx playwright test tests/e2e/canvases_extended.spec.ts -g "Network Dashboard"
  ✓ Network Dashboard (/network/) — not a 500 error
  ✓ Network Dashboard (/network/) — no traceback in body
  2 passed (6.9s)

$ curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5050/network/
200
```

`/network/` now returns HTTP 200 against the running dashboard, and the
Playwright "soft" 500 check passes. The CI failure was transient — fixed
in HEAD by the cascade of PGP/network-canvas commits that have landed
since `42521c2da` (most directly the `f87f9b4d7` PG-primary JSON
Pythonization for the network canvas).

## 5. Resolution

**No code change required.** This task is a no-op close in the same
pattern as [[ci-fix-stale-commit-lint-gate]] and the sibling
`ci-fix-27225052175` RCA.

The CI workflow itself does not need editing — by the time the next CI
run hits the same path, HEAD will have moved past the failure window and
the E2E will pass.

## 6. Recommended follow-up (if recurring)

If `/network/` 500's resurface, the right fix is in `tools/network/blueprint.py`
index route (around lines 226–234) — wrap the `simulation_results` queries
in a `try/except` that degrades to empty lists with a `conn.rollback()`,
as was already applied to the simulation endpoints. The pattern matches
what shipped in `2e9c8513c` per the previous RCA.
