# CUI // SP-CTI
"""Connector Forge — dynamic DataBridge connector generation (D-CF-1).

6-stage pipeline: spec parse → base select → code gen → validate → sandbox → integrate.
Part of the sparkpilot multi-agent architecture (Agent 12, port 9458).
"""
