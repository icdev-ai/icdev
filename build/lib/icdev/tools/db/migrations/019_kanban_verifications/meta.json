{
  "id": "019",
  "name": "kanban_verifications",
  "description": "Add append-only kanban_verifications audit table + 'decomposed' status + migrate 'validating' status to kanban_tasks. Logs every completion verification attempt so phantom completion patterns are visible on the dashboard.",
  "created_at": "2026-04-12",
  "author": "icdev-builder",
  "database": "icdev",
  "reversible": true,
  "requires_downtime": false,
  "notes": "Safe to re-run (CREATE TABLE IF NOT EXISTS). Also expands kanban_tasks.status CHECK constraint to include 'decomposed' (guard-3) and 'validating' (guard-7)."
}
