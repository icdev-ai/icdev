{
  "id": "124",
  "name": "dashboard_users_email_tenant",
  "description": "Add composite unique index on dashboard_users(email, tenant_id) so the same email can exist under different tenants without collision. Uses COALESCE(tenant_id, '') so NULL tenant rows remain unique per email.",
  "created_at": "2026-05-09",
  "author": "icdev-builder",
  "database": "icdev",
  "reversible": false,
  "requires_downtime": false,
  "notes": "The original single-column email unique constraint remains; this adds a composite index alongside it for multi-tenant email reuse."
}
