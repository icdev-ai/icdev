{
  "id": "040",
  "name": "kanban_bypass_flag",
  "description": "Add completed_via_bypass boolean flag to kanban_tasks so bypass completions are queryable without a JOIN to kanban_verifications.",
  "created_at": "2026-04-26",
  "author": "icdev-kanban-vv",
  "database": "icdev",
  "reversible": true,
  "requires_downtime": false,
  "notes": "Safe to re-run (ALTER TABLE IF column not exists guard). Default 0 — existing rows are unaffected."
}
