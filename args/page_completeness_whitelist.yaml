# CUI // SP-CTI
# Page Completeness Whitelist (OPT-62)
#
# Grandfathered canvas pages that were created without all 8 required components
# (CLAUDE.md §8-component gate). Listed canvases are DOWNGRADED from failing
# the coherence_checker `new_page_completeness` gate so active development is
# not blocked. Each entry requires a follow-up task to complete the canvas.
#
# Consumed by: tools/workflow/coherence_checker.py :: check_new_page_completeness
#
# Rules:
#   - Each entry is a canvas directory name under tools/dashboard/templates/
#   - Only add entries for pre-existing incomplete canvases, not new work
#   - Include a comment: canvas name — what is missing (task-id)
#   - Remove the entry once all 8 components are complete
#
# ---------------------------------------------------------------------------
# STATUS: FULLY RETIRED (cvx-tch-01/02/03, 2026-07).
#
# The legacy grandfather list is now EMPTY. The final 10 canvases were resolved:
#
#   FIXED (brought to full 8-component compliance):
#     ai_observatory  — registry nav.section added; the real /ai-observatory link
#                       (base.html) is now recognized by the registry-aware gate.
#     cache_savings   — registry nav.section added + 3 .iqe seed queries authored.
#     aisg            — 3 .iqe seed queries authored (prior seeds were .md, which
#                       the gate's .yaml/.yml/.iqe glob does not count).
#     strategos       — iqe_query_widget include wired into page.html (+ mirror),
#                       posting to the existing /api/strategos/iqe-query route.
#
#   ALREADY PASSING (removed; no work needed — resolved by earlier merged work):
#     demo_runner     — passes all 8 (blueprint/nav/seeds already present).
#     ontology        — passes; .iqe seeds now counted by the glob fix.
#
#   RECLASSIFIED (removed here; still correctly excused as utility pages via
#   args/completion_exemptions.yaml :: iqe_exempt — no query-worthy data model):
#     forge_academy   — static curriculum content, no data model.
#     il5             — IL5 enclave posture page, no query-worthy records.
#     safety_monitor  — runtime guardrail state, not stored records.
#     system_graph    — derived topology view, not a stored record set.
#
# The nav-02 removals (aiify, workflow_canvas, canvas_health) were already gone.
# ---------------------------------------------------------------------------

whitelisted_canvases: []
