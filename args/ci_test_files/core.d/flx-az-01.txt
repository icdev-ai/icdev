# flx-az-01 — floci-az Azure emulator seam, connector and twin.
# One fragment named for the task rather than an append to core.txt: two PRs
# writing two differently-named files cannot collide (tsg-policy-03).
tests/cloud/test_floci_az_seam.py
