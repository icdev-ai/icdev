# CUI // SP-CTI
# AADC Workflow — AI/ATO Design Canvas
# Full 12-step pipeline: governance scan → compliance check → IaC → AO gate → validate → apply → verify

description: "AADC: AI governance scan, compliance check, IaC generation, AO gate, Terraform/Ansible validate, migration report, apply approval, Terraform apply, AWS verification, post-deploy validation"
category: deploy
canvas: aadc

steps:
  # ── Canvas-specific steps (AADC only) ─────────────────────────────────────
  - id: step_1
    name: Governance Scan
    tool: tools/aadc/governance_scanner.py
    node_type: tool
    required: true
    timeout: 300
    depends_on: []
    # penta-aadc-02: graph-driven. governance_scanner loads the AADC design
    # (--project-id = design id, injected by workflow_runner) and derives the
    # AI Risk Score and findings from agentic_engine.assess_design — no default
    # posture. Reports assessment-unavailable (exit 2) if the design is absent.
    description: >
      Audit AI governance posture from the design graph (assess_design):
      autonomy, HITL coverage, model/agent inventory, AI Risk Score.

  - id: step_2
    name: Compliance Check
    tool: tools/aadc/compliance_checker.py
    node_type: tool
    required: true
    timeout: 300
    depends_on: [step_1]
    # penta-aadc-02: graph-driven. compliance_checker derives the PASS/FAIL gate
    # from agentic_engine.assess_design findings (blocking = CRITICAL/CAT1/HIGH)
    # — no default checklist. Reports assessment-unavailable (exit 2) if absent.
    description: >
      Evaluate NIST AI RMF / OWASP LLM / OMB M-25-21 compliance from the design
      assessment (assess_design); gate fails on any blocking-severity finding.

  - id: step_3
    name: Generate IaC
    tool: tools/aadc/iac_generator.py
    node_type: tool
    required: true
    timeout: 600
    depends_on: [step_2]
    description: >
      Generate Terraform (.tf), Ansible playbook (.yml), inventory (.ini),
      validation script (.py), and checklist (.md) for AI/ATO infrastructure.
      Output: data/studio_artifacts/aadc/ — consumed by all downstream steps.

  - id: step_4
    name: AO Approval
    node_type: human
    required: true
    role: approver
    timeout: 86400
    tool: ""
    depends_on: [step_3]
    description: >
      Authorizing Official reviews governance findings and IaC before provisioning.
      Telegram: /approve <step_run_id> or /reject <step_run_id> [reason].

  # ── Shared executor steps (canvas-agnostic) ───────────────────────────────
  - id: step_5
    name: Terraform Plan
    tool: tools/studio/executors/terraform_plan.py
    node_type: tool
    required: true
    timeout: 300
    depends_on: [step_4]

  - id: step_6
    name: Ansible Execute
    tool: tools/studio/executors/ansible_executor.py
    node_type: tool
    required: true
    timeout: 300
    depends_on: [step_4]

  - id: step_7
    name: Validation Run
    tool: tools/studio/executors/validation_runner.py
    node_type: tool
    required: true
    timeout: 120
    depends_on: [step_4]

  - id: step_8
    name: Migration Report
    tool: tools/studio/executors/migration_reporter.py
    node_type: tool
    required: true
    timeout: 60
    depends_on: [step_5, step_6, step_7]

  - id: step_9
    name: Apply Approval
    node_type: human
    required: true
    role: infrastructure_approver
    timeout: 86400
    tool: ""
    depends_on: [step_5, step_6, step_7]
    description: >
      Human gate before applying AI/ATO infrastructure to AWS.
      Telegram: /approve <step_run_id> or /reject <step_run_id> [reason].

  - id: step_10
    name: Terraform Apply
    tool: tools/studio/executors/terraform_apply.py
    node_type: tool
    required: true
    timeout: 900
    depends_on: [step_9]

  - id: step_11
    name: AWS Config Execute
    tool: tools/studio/executors/aws_config_executor.py
    node_type: tool
    required: false
    timeout: 600
    depends_on: [step_10]

  - id: step_12
    name: Post-deploy Validation
    tool: tools/studio/executors/validation_runner.py
    node_type: tool
    required: false
    timeout: 300
    depends_on: [step_10]
