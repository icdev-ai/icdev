# CUI // SP-CTI — TEMPLATE ONLY: replace values with DISA-issued certs before deployment
apiVersion: v1
kind: Secret
metadata:
  name: icdev-sipr-pki
  namespace: icdev-ste
  labels:
    classification: SECRET
    environment: ste
type: kubernetes.io/tls
stringData:
  tls.crt: |
    # INSERT DISA-ISSUED CERTIFICATE HERE
    # Source: DISA PKI (https://public.cyber.mil/pki-pke/)
    # Cert type: DoD ID SW CA-73 through CA-80 or current SIPR server cert
  tls.key: |
    # INSERT PRIVATE KEY HERE (never commit to source control)
  ca.crt: |
    # INSERT DISA ROOT CA BUNDLE HERE
