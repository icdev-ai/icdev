#!/usr/bin/env python3
"""SageMaker serving entrypoint — starts llama.cpp server + Flask wrapper."""
import os
import subprocess
import sys
import time

# Start llama.cpp server in background
server_proc = subprocess.Popen(["/bin/bash", "/opt/program/server.sh"])
time.sleep(5)  # Wait for model load

# Start Flask wrapper on port 8080 (SageMaker requirement)
from main import app
app.run(host="0.0.0.0", port=8080)
