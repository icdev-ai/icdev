# [TEMPLATE: CUI // SP-CTI]
# ICDEV Environment Variables — Template
# Copy to .env and fill in your values. NEVER commit .env to version control.
#
# Usage:
#   cp .env.template .env
#   # Edit .env with your actual values
#   docker compose up -d

# ============================================================================
# Python Path
# ============================================================================

# Ensure 'tools' and other top-level modules are importable from worktree root
PYTHONPATH=.

# ============================================================================
# LLM Providers
# ============================================================================

# OpenAI (for embeddings in semantic search)
# OPENAI_API_KEY=sk-proj-...

# AWS Bedrock (primary LLM provider for GovCloud)
# AWS_DEFAULT_REGION=us-gov-west-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Ollama (local/air-gapped LLM)
# OLLAMA_BASE_URL=http://host.docker.internal:11434/v1

# LLM Proxy / Virtual Keys (LPX) — OPT-IN, OFF BY DEFAULT
# Route cloud providers through the opt-in LiteLLM proxy
# (docker compose --profile llm-proxy up). Disabled by default. DISTINCT from
# ICDEV_LLM_PROXY (the corporate HTTPS egress proxy in proxy_resolver.py).
# ICDEV_LLM_PROXY_ENABLED=false
# ICDEV_LLM_PROXY_BASE_URL=http://localhost:4000
# Virtual key presented to the proxy (issue via tools/llm/proxy_keys.py) — never
# a real provider key.
# ICDEV_LLM_PROXY_VIRTUAL_KEY=
# LITELLM_MASTER_KEY=

# Cloud provider override (aws, azure, gcp, oci, ibm, local)
# ICDEV_CLOUD_PROVIDER=aws

# ============================================================================
# Dashboard
# ============================================================================

# Flask session secret key (auto-generated if unset, but set for persistence)
# ICDEV_DASHBOARD_SECRET=change-me-to-a-random-string

# CUI banner toggle (default: true)
# ICDEV_CUI_BANNER_ENABLED=true

# Custom CUI banner text
# CUI_BANNER_TOP=CUI // SP-CTI
# CUI_BANNER_BOTTOM=CUI // SP-CTI

# ============================================================================
# BYOK (Bring Your Own Key)
# ============================================================================

# Enable BYOK for per-user LLM keys (default: false)
# ICDEV_BYOK_ENABLED=false

# Encryption key for BYOK stored keys (Fernet AES-256)
# ICDEV_BYOK_ENCRYPTION_KEY=

# ============================================================================
# Database
# ============================================================================

# Database path (default: /app/data/icdev.db inside container)
# ICDEV_DB_PATH=/app/data/icdev.db

# PostgreSQL (production — overrides SQLite)
# DATABASE_URL=postgresql://icdev:password@db:5432/icdev

# ============================================================================
# Security
# ============================================================================

# HMAC signing key for webhook/hook event verification
# ICDEV_HMAC_SECRET=

# Classification level (CUI, SECRET)
# ICDEV_CLASSIFICATION=CUI

# Impact level (IL2, IL4, IL5, IL6)
# ICDEV_IMPACT_LEVEL=IL4
