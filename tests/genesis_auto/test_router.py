#!/usr/bin/env python3
# CUI // SP-CTI
"""Auto-generated tests for tools.llm.router.

Generated by Genesis Test Reflex using api_surface_extractor.
Tests validate importability, function signatures, return types,
and basic invocation patterns.
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import pytest


# --- Module Import ---

def test_router_imports():
    """Verify module can be imported without errors."""
    try:
        import tools.llm.router  # noqa: F401
    except ImportError as e:
        pytest.skip(f"Import dependency missing: {e}")


# --- Class Tests ---

def test_router_LLMUnavailableError_exists():
    """Verify class LLMUnavailableError exists."""
    try:
        from tools.llm.router import LLMUnavailableError
        assert isinstance(LLMUnavailableError, type), 'LLMUnavailableError is not a class'
    except ImportError:
        pytest.skip("Module not importable")

def test_router_LLMRouter_exists():
    """Verify class LLMRouter exists."""
    try:
        from tools.llm.router import LLMRouter
        assert isinstance(LLMRouter, type), 'LLMRouter is not a class'
    except ImportError:
        pytest.skip("Module not importable")

def test_router_LLMRouter_methods():
    """Verify LLMRouter has expected public methods."""
    try:
        from tools.llm.router import LLMRouter
        assert hasattr(LLMRouter, "is_no_llm_mode"), "Missing method is_no_llm_mode"
        assert hasattr(LLMRouter, "has_any_llm"), "Missing method has_any_llm"
        assert hasattr(LLMRouter, "get_provider_for_function"), "Missing method get_provider_for_function"
        assert hasattr(LLMRouter, "get_effort"), "Missing method get_effort"
        assert hasattr(LLMRouter, "invoke"), "Missing method invoke"
        assert hasattr(LLMRouter, "invoke_streaming"), "Missing method invoke_streaming"
        assert hasattr(LLMRouter, "get_embedding_provider"), "Missing method get_embedding_provider"
        assert hasattr(LLMRouter, "get_model_pricing"), "Missing method get_model_pricing"
    except ImportError:
        pytest.skip("Module not importable")
