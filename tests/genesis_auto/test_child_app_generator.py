#!/usr/bin/env python3
# CUI // SP-CTI
"""Auto-generated tests for tools.builder.child_app_generator.

Generated by Genesis Test Reflex using api_surface_extractor.
Tests validate importability, function signatures, return types,
and basic invocation patterns.
"""

import ast
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import pytest


# --- Module Import ---


def test_child_app_generator_imports():
    """Verify module can be imported without errors."""
    try:
        import tools.builder.child_app_generator  # noqa: F401
    except ImportError as e:
        pytest.skip(f"Import dependency missing: {e}")


# --- Function Signature Tests ---


def test_child_app_generator_audit_log_event_exists():
    """Verify audit_log_event exists and is callable."""
    try:
        from tools.builder.child_app_generator import audit_log_event

        assert callable(audit_log_event), "audit_log_event is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_01_create_directory_tree_exists():
    """Verify step_01_create_directory_tree exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_01_create_directory_tree

        assert callable(step_01_create_directory_tree), "step_01_create_directory_tree is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_01_create_directory_tree_signature():
    """Verify step_01_create_directory_tree accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_01_create_directory_tree

        sig = inspect.signature(step_01_create_directory_tree)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_02_copy_and_adapt_tools_exists():
    """Verify step_02_copy_and_adapt_tools exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_02_copy_and_adapt_tools

        assert callable(step_02_copy_and_adapt_tools), "step_02_copy_and_adapt_tools is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_02_copy_and_adapt_tools_signature():
    """Verify step_02_copy_and_adapt_tools accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_02_copy_and_adapt_tools

        sig = inspect.signature(step_02_copy_and_adapt_tools)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
        assert "icdev_root" in params, f'Missing parameter "icdev_root" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_03_agent_infrastructure_exists():
    """Verify step_03_agent_infrastructure exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_03_agent_infrastructure

        assert callable(step_03_agent_infrastructure), "step_03_agent_infrastructure is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_03_agent_infrastructure_signature():
    """Verify step_03_agent_infrastructure accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_03_agent_infrastructure

        sig = inspect.signature(step_03_agent_infrastructure)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_04_memory_bootstrap_exists():
    """Verify step_04_memory_bootstrap exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_04_memory_bootstrap

        assert callable(step_04_memory_bootstrap), "step_04_memory_bootstrap is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_04_memory_bootstrap_signature():
    """Verify step_04_memory_bootstrap accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_04_memory_bootstrap

        sig = inspect.signature(step_04_memory_bootstrap)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_05_db_init_script_exists():
    """Verify step_05_db_init_script exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_05_db_init_script

        assert callable(step_05_db_init_script), "step_05_db_init_script is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_05_db_init_script_signature():
    """Verify step_05_db_init_script accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_05_db_init_script

        sig = inspect.signature(step_05_db_init_script)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_06_goals_and_hardprompts_exists():
    """Verify step_06_goals_and_hardprompts exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_06_goals_and_hardprompts

        assert callable(step_06_goals_and_hardprompts), "step_06_goals_and_hardprompts is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_06_goals_and_hardprompts_signature():
    """Verify step_06_goals_and_hardprompts accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_06_goals_and_hardprompts

        sig = inspect.signature(step_06_goals_and_hardprompts)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
        assert "icdev_root" in params, f'Missing parameter "icdev_root" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_07_args_and_context_exists():
    """Verify step_07_args_and_context exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_07_args_and_context

        assert callable(step_07_args_and_context), "step_07_args_and_context is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_07_args_and_context_signature():
    """Verify step_07_args_and_context accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_07_args_and_context

        sig = inspect.signature(step_07_args_and_context)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
        assert "icdev_root" in params, f'Missing parameter "icdev_root" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_08_a2a_callback_client_exists():
    """Verify step_08_a2a_callback_client exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_08_a2a_callback_client

        assert callable(step_08_a2a_callback_client), "step_08_a2a_callback_client is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_08_a2a_callback_client_signature():
    """Verify step_08_a2a_callback_client accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_08_a2a_callback_client

        sig = inspect.signature(step_08_a2a_callback_client)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_09_cicd_setup_exists():
    """Verify step_09_cicd_setup exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_09_cicd_setup

        assert callable(step_09_cicd_setup), "step_09_cicd_setup is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_09_cicd_setup_signature():
    """Verify step_09_cicd_setup accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_09_cicd_setup

        sig = inspect.signature(step_09_cicd_setup)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
        assert "icdev_root" in params, f'Missing parameter "icdev_root" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_09c_claude_code_config_exists():
    """Verify step_09c_claude_code_config exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_09c_claude_code_config

        assert callable(step_09c_claude_code_config), "step_09c_claude_code_config is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_09c_claude_code_config_signature():
    """Verify step_09c_claude_code_config accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_09c_claude_code_config

        sig = inspect.signature(step_09c_claude_code_config)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
        assert "icdev_root" in params, f'Missing parameter "icdev_root" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_10_csp_mcp_config_exists():
    """Verify step_10_csp_mcp_config exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_10_csp_mcp_config

        assert callable(step_10_csp_mcp_config), "step_10_csp_mcp_config is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_10_csp_mcp_config_signature():
    """Verify step_10_csp_mcp_config accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_10_csp_mcp_config

        sig = inspect.signature(step_10_csp_mcp_config)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_11_dynamic_claude_md_exists():
    """Verify step_11_dynamic_claude_md exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_11_dynamic_claude_md

        assert callable(step_11_dynamic_claude_md), "step_11_dynamic_claude_md is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_11_dynamic_claude_md_signature():
    """Verify step_11_dynamic_claude_md accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_11_dynamic_claude_md

        sig = inspect.signature(step_11_dynamic_claude_md)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_12_audit_and_registration_exists():
    """Verify step_12_audit_and_registration exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_12_audit_and_registration

        assert callable(step_12_audit_and_registration), "step_12_audit_and_registration is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_12_audit_and_registration_signature():
    """Verify step_12_audit_and_registration accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_12_audit_and_registration

        sig = inspect.signature(step_12_audit_and_registration)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
        assert "db_path" in params, f'Missing parameter "db_path" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_13_production_audit_exists():
    """Verify step_13_production_audit exists and is callable."""
    try:
        from tools.builder.child_app_generator import step_13_production_audit

        assert callable(step_13_production_audit), "step_13_production_audit is not callable"
    except ImportError:
        pytest.skip("Module not importable")


def test_child_app_generator_step_13_production_audit_signature():
    """Verify step_13_production_audit accepts expected parameters."""
    import inspect

    try:
        from tools.builder.child_app_generator import step_13_production_audit

        sig = inspect.signature(step_13_production_audit)
        params = list(sig.parameters.keys())
        assert "child_root" in params, f'Missing parameter "child_root" in {params}'
        assert "blueprint" in params, f'Missing parameter "blueprint" in {params}'
    except ImportError:
        pytest.skip("Module not importable")


# --- Dashboard stub: banned raw-sqlite3 pattern (cvx-gen-03 / FORGE-12) ---


def _generate_stub_app(tmp_path):
    """Generate a dashboard stub into tmp_path and return the emitted source."""
    from tools.builder.child_app_generator import _generate_dashboard_stub

    blueprint = {
        "app_name": "acme-child",
        "classification": "CUI",
        "agents": [],
        "capabilities": {"compliance": True, "document_intelligence": True},
        "demo_mode": False,
    }
    assert _generate_dashboard_stub(tmp_path, blueprint) is True
    app_py = tmp_path / "tools" / "dashboard" / "app.py"
    assert app_py.is_file(), "dashboard stub app.py was not written"
    return app_py.read_text(encoding="utf-8")


def test_dashboard_stub_has_no_raw_sqlite3(tmp_path):
    """cvx-gen-03: the emitted dashboard stub must not use raw sqlite3.

    PostgreSQL is the primary backend; a raw ``sqlite3.connect()`` bypasses
    ``get_connection()``/RLS. FORGE-12 (added by cvx-gen-01) FAILs on this
    pattern, so the generator must never emit it.
    """
    try:
        source = _generate_stub_app(tmp_path)
    except ImportError:
        pytest.skip("Module not importable")

    assert "import sqlite3" not in source, "stub still emits 'import sqlite3'"
    assert "sqlite3.connect" not in source, "stub still emits a raw sqlite3.connect()"
    # The stub is a placeholder with no DB access, but it must point developers
    # at the correct abstraction when they add data-backed routes.
    assert "get_connection" in source, "stub should reference get_connection() guidance"


def test_dashboard_stub_passes_forge12(tmp_path):
    """The generated stub must PASS FORGE-12 in forge_validator."""
    try:
        _generate_stub_app(tmp_path)
        from tools.builder.forge_validator import _check_db_patterns
    except ImportError:
        pytest.skip("Module not importable")

    checks = _check_db_patterns(tmp_path)
    forge12 = [c for c in checks if c.check_id == "FORGE-12"]
    assert forge12, "FORGE-12 check did not run"
    assert forge12[0].status == "pass", (
        f"FORGE-12 failed on generated stub: {forge12[0].message}"
    )


# --- cvx-gen-05: emitted stubs must compile clean (latent NameError guard) ---


def test_dashboard_stub_compiles_and_imports_os(tmp_path):
    """cvx-gen-05: the emitted dashboard stub must compile as a whole and, since
    its ``__main__`` block reads ``os.environ``, it must ``import os`` (else a
    latent NameError when a generated child dashboard is run directly)."""
    try:
        source = _generate_stub_app(tmp_path)
    except ImportError:
        pytest.skip("Module not importable")

    # Compiles clean as a whole module (no SyntaxError).
    compile(source, "child_dashboard_app.py", "exec")

    # The __main__ block uses os.environ -> os must be imported.
    assert "os.environ" in source, "stub __main__ should read os.environ"
    tree = ast.parse(source)
    imported = {
        alias.name
        for node in ast.walk(tree)
        if isinstance(node, ast.Import)
        for alias in node.names
    }
    assert "os" in imported, "dashboard stub uses os but never imports it"


def test_mcp_stub_compiles_and_has_no_undefined_logger(tmp_path):
    """cvx-gen-05: emitted MCP server stub must compile and not reference an
    undefined ``get_logger`` (it imports ``logging`` and must call
    ``logging.getLogger``)."""
    try:
        from tools.builder.child_app_generator import _generate_mcp_stubs
    except ImportError:
        pytest.skip("Module not importable")

    mcp_dir = tmp_path / "mcp"
    mcp_dir.mkdir(parents=True, exist_ok=True)
    blueprint = {"app_name": "acme-child", "classification": "CUI"}
    agents = [{"name": "builder", "port": 8445, "role": "builder"}]
    assert _generate_mcp_stubs(mcp_dir, agents, "acme-child", blueprint) >= 1

    stubs = list(mcp_dir.glob("*.py"))
    assert stubs, "no MCP stub emitted"
    for stub in stubs:
        source = stub.read_text(encoding="utf-8")
        compile(source, stub.name, "exec")
        assert "get_logger(" not in source, f"{stub.name} references undefined get_logger"
        assert "logging.getLogger(" in source, f"{stub.name} should use logging.getLogger"


def test_a2a_callback_client_compiles_and_has_no_undefined_logger(tmp_path):
    """cvx-gen-05: emitted A2A callback client must compile and use
    ``logging.getLogger`` rather than an undefined ``get_logger``."""
    try:
        from tools.builder.child_app_generator import step_08_a2a_callback_client
    except ImportError:
        pytest.skip("Module not importable")

    blueprint = {
        "app_name": "acme-child",
        "classification": "CUI",
        "parent_callback": {
            "enabled": True,
            "url": "https://parent.example/a2a",
            "auth": "mtls",
        },
    }
    result = step_08_a2a_callback_client(tmp_path, blueprint)
    client = Path(result["client_path"])
    source = client.read_text(encoding="utf-8")
    compile(source, client.name, "exec")
    assert "get_logger(" not in source, "a2a client references undefined get_logger"
    assert "logging.getLogger(" in source, "a2a client should use logging.getLogger"


# ---------------------------------------------------------------------------
# cvx-gen-04: TRUST re-inheritance refresh
# ---------------------------------------------------------------------------


def _make_fake_parent(root: Path) -> Path:
    """Build a minimal fake parent repo with a CURRENT content_grounding.py
    (has ground_content) plus a PARENT_ONLY dir (tools/saas) that must never
    be re-inherited."""
    quality = root / "tools" / "quality"
    quality.mkdir(parents=True, exist_ok=True)
    (quality / "content_grounding.py").write_text(
        "def ground_content(text, evidence):\n"
        "    '''semantic grounding upgrade (trust-cite-05)'''\n"
        "    return {'grounded': True}\n",
        encoding="utf-8",
    )
    (quality / "citation_grounding.py").write_text(
        "def parse_citations(text):\n    return []\n", encoding="utf-8"
    )
    workflow = root / "tools" / "workflow"
    workflow.mkdir(parents=True, exist_ok=True)
    (workflow / "coherence_checker.py").write_text(
        "def check_all():\n    return True\n", encoding="utf-8"
    )
    # PARENT_ONLY_DIRS invariant: tools/saas must NOT be copied.
    saas = root / "tools" / "saas"
    saas.mkdir(parents=True, exist_ok=True)
    (saas / "billing.py").write_text("SECRET = 'parent-only'\n", encoding="utf-8")
    return root


def _make_stale_child(root: Path) -> Path:
    """Child app whose content_grounding.py is STALE (no ground_content)."""
    quality = root / "tools" / "quality"
    quality.mkdir(parents=True, exist_ok=True)
    (quality / "content_grounding.py").write_text(
        "def old_grounding(text):\n    '''pre-upgrade snapshot'''\n    return None\n",
        encoding="utf-8",
    )
    return root


def test_refresh_trust_function_exists():
    """refresh_trust_modules must exist and be callable."""
    try:
        from tools.builder.child_app_generator import refresh_trust_modules
    except ImportError:
        pytest.skip("Module not importable")
    assert callable(refresh_trust_modules)


def test_refresh_trust_dry_run_reports_stale_update(tmp_path):
    """Dry-run reports the stale content_grounding.py as 'would update' and
    writes nothing to the child."""
    try:
        from tools.builder.child_app_generator import refresh_trust_modules
    except ImportError:
        pytest.skip("Module not importable")

    parent = _make_fake_parent(tmp_path / "parent")
    child = _make_stale_child(tmp_path / "child")

    result = refresh_trust_modules(
        child, icdev_root=parent, dirs=["tools/quality", "tools/workflow"], dry_run=True
    )
    assert result["status"] == "success"
    assert result["dry_run"] is True
    assert result["applied"] is False
    assert "tools/quality/content_grounding.py" in result["updated"]
    # workflow + citation_grounding are absent in child -> reported as added.
    assert "tools/quality/citation_grounding.py" in result["added"]
    assert "tools/workflow/coherence_checker.py" in result["added"]
    assert result["would_change"] >= 3

    # Nothing written: child still lacks ground_content.
    stale = (child / "tools" / "quality" / "content_grounding.py").read_text(encoding="utf-8")
    assert "ground_content" not in stale
    assert not (child / "tools" / "workflow" / "coherence_checker.py").exists()


def test_refresh_trust_apply_updates_stale_and_reinherits(tmp_path):
    """--apply (dry_run=False) writes the current API into the child."""
    try:
        from tools.builder.child_app_generator import refresh_trust_modules
    except ImportError:
        pytest.skip("Module not importable")

    parent = _make_fake_parent(tmp_path / "parent")
    child = _make_stale_child(tmp_path / "child")

    result = refresh_trust_modules(
        child, icdev_root=parent, dirs=["tools/quality", "tools/workflow"], dry_run=False
    )
    assert result["status"] == "success"
    assert result["applied"] is True

    # Re-check: current API is now present in the child.
    refreshed = (child / "tools" / "quality" / "content_grounding.py").read_text(encoding="utf-8")
    assert "ground_content" in refreshed
    assert (child / "tools" / "workflow" / "coherence_checker.py").exists()

    # Idempotency: a second dry-run reports no changes.
    again = refresh_trust_modules(
        child, icdev_root=parent, dirs=["tools/quality", "tools/workflow"], dry_run=True
    )
    assert again["would_change"] == 0


def test_refresh_trust_never_copies_parent_only_dirs(tmp_path):
    """PARENT_ONLY_DIRS (e.g. tools/saas) must never be re-inherited, even if
    explicitly requested."""
    try:
        from tools.builder.child_app_generator import refresh_trust_modules
    except ImportError:
        pytest.skip("Module not importable")

    parent = _make_fake_parent(tmp_path / "parent")
    child = tmp_path / "child"
    child.mkdir()

    # Default dirs -> saas not in scope at all.
    default = refresh_trust_modules(child, icdev_root=parent, dry_run=False)
    assert not any("tools/saas" in f for f in default["added"] + default["updated"])
    assert not (child / "tools" / "saas").exists()

    # Even when explicitly asked, the parent-only guard skips it.
    forced = refresh_trust_modules(
        child, icdev_root=parent, dirs=["tools/saas"], dry_run=False
    )
    assert not (child / "tools" / "saas" / "billing.py").exists()
    assert any(s["dir"] == "tools/saas" and s["reason"] == "parent_only" for s in forced["skipped"])


def test_refresh_trust_dirs_are_always_on_and_not_parent_only():
    """Every TRUST_REFRESH_DIRS entry must be always-on (in DIRECTORY_TREE) and
    never a PARENT_ONLY_DIR."""
    try:
        from tools.builder.child_app_generator import (
            TRUST_REFRESH_DIRS,
            DIRECTORY_TREE,
            PARENT_ONLY_DIRS,
        )
    except ImportError:
        pytest.skip("Module not importable")

    for d in TRUST_REFRESH_DIRS:
        assert d in DIRECTORY_TREE, f"{d} not always-on"
        assert d not in PARENT_ONLY_DIRS, f"{d} is parent-only"


def test_refresh_trust_missing_app_dir_errors(tmp_path):
    """A non-existent child dir returns an error status, not a crash."""
    try:
        from tools.builder.child_app_generator import refresh_trust_modules
    except ImportError:
        pytest.skip("Module not importable")
    result = refresh_trust_modules(tmp_path / "does-not-exist", icdev_root=tmp_path, dry_run=True)
    assert result["status"] == "error"
