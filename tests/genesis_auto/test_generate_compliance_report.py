#!/usr/bin/env python3
# CUI // SP-CTI
"""Auto-generated tests for tools.generate_compliance_report.

Generated by Genesis Test Reflex using api_surface_extractor.
Tests validate importability, function signatures, return types,
and basic invocation patterns.
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import pytest


# --- Module Import ---

def test_generate_compliance_report_imports():
    """Verify module can be imported without errors."""
    try:
        import tools.generate_compliance_report  # noqa: F401
    except ImportError as e:
        pytest.skip(f"Import dependency missing: {e}")


# --- Function Signature Tests ---

def test_generate_compliance_report_run_exists():
    """Verify run exists and is callable."""
    try:
        from tools.generate_compliance_report import run
        assert callable(run), 'run is not callable'
    except ImportError:
        pytest.skip("Module not importable")

def test_generate_compliance_report_run_signature():
    """Verify run accepts expected parameters."""
    import inspect
    try:
        from tools.generate_compliance_report import run
        sig = inspect.signature(run)
        params = list(sig.parameters.keys())
        assert "baseline_path" in params, f"Missing parameter \"baseline_path\" in {params}"
        assert "env_state_path" in params, f"Missing parameter \"env_state_path\" in {params}"
        assert "output_path" in params, f"Missing parameter \"output_path\" in {params}"
        assert "fmt" in params, f"Missing parameter \"fmt\" in {params}"
    except ImportError:
        pytest.skip("Module not importable")

def test_generate_compliance_report_main_exists():
    """Verify main exists and is callable."""
    try:
        from tools.generate_compliance_report import main
        assert callable(main), 'main is not callable'
    except ImportError:
        pytest.skip("Module not importable")


# --- Constants ---

def test_generate_compliance_report_constants():
    """Verify module exports expected constants."""
    try:
        import tools.generate_compliance_report as mod
        assert hasattr(mod, "STATUS_COMPLIANT"), "Missing constant STATUS_COMPLIANT"
        assert hasattr(mod, "STATUS_PARTIAL"), "Missing constant STATUS_PARTIAL"
        assert hasattr(mod, "STATUS_NON_COMPLIANT"), "Missing constant STATUS_NON_COMPLIANT"
        assert hasattr(mod, "STATUS_NOT_APPLICABLE"), "Missing constant STATUS_NOT_APPLICABLE"
        assert hasattr(mod, "STATUS_MISSING"), "Missing constant STATUS_MISSING"
        assert hasattr(mod, "CUI_BANNER"), "Missing constant CUI_BANNER"
    except ImportError:
        pytest.skip("Module not importable")
