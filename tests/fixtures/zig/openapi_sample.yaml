openapi: "3.0.3"
info:
  title: Sample API
  version: "1.0"
servers:
  - url: http://api.example.com/v1
    description: Production (HTTP only — missing TLS)
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
  /data:
    get:
      summary: Get data
      responses:
        "200":
          description: Data returned
