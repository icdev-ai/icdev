# Kanban System

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## Kanban Tools

| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Kanban Executor Reflex | tools/genesis/reflexes/kanban.py | Polls kanban_tasks for due tasks, dispatches to Claude CLI in isolated worktrees, then merges or (with `ICDEV_KANBAN_PR_FLOW=1`) pushes the branch and opens a PR via `gh pr create --base <default>`. PR-flow base guard (incident 2026-07-08, PR #114): after create, `_ensure_pr_base()` verifies `baseRefName` == `_default_branch()` via `gh pr view --json baseRefName,url` and retargets with `gh pr edit --base <default>` if not — also resolves/reuses a PR the task agent opened itself with a wrong base. **One task, one PR (kpr-dup-02):** before `gh pr create`, `_open_prs_for_task()` looks for an open PR on ANY branch belonging to the task (workers push suffixed branches like `kanban/<id>-land`, and `gh pr create` only fails for the exact head) and reuses it when it already carries these commits; after creating, `_supersede_stale_prs()` closes the task's other open PRs — but only those whose commits are all present on the surviving branch by `git cherry` patch-id. A PR with unique work is left open with a warning. | Invoked by Genesis daemon (`run(config, conn)`) | Task lifecycle transitions, PR URL in executor_url |
| PR Watcher (OPT-70) | tools/ci/pr_watcher.py | Polls kanban PRs, classifies CI state, injects resume context, auto-merges on green (opt-in). Base-branch guard (incident 2026-07-08, PR #114): refuses auto-merge when the PR `baseRefName` is not the repo default branch (resolved via `gh repo view --json defaultBranchRef`, git fallback) — logs a warning and reports `wait` instead; unknown base is treated as unsafe. Full entry in `tools/manifest/ai-security.md`. | --once, --daemon, --interval, --task, --dry-run, --json, --config | WatcherReport |
| Superseded-PR Classifier (mfx-mrg-02) | tools/ci/pr_superseded.py | Answers the one question none of pr_watcher's guards asked: has this open PR's work ALREADY landed under a DIFFERENT PR NUMBER? Four cases in two days (#2015/#2014 42s apart, #1985/#1983 82s, #2056/#2053, #2049/#2053) each sat CONFLICTING or 'ahead' and held `no_sibling_conflict` against every open PR touching the same files until a human diagnosed it with `gh pr view <sibling> --json commits`. `landed_check` greps commit SUBJECTS for the TASK ID and so cannot see a sibling that absorbed another task's branch; `_behind_by` measures distance from main, and a duplicate at the SAME head sha is 0 behind. The signal is the COMMIT LIST, which GitHub keeps whatever merge method was used - so a SQUASH is answerable. `decide_superseded(pr, merged_siblings, revert=...)` fires when our head sha is in a MERGED family sibling's commit list AND every commit we can see is in that list; requiring the HEAD closes the truncation hole (`gh` caps the commits connection, and a short list of OURS would make the subset test trivially true). Family is `same_branch` / `named_branch` / `named_pr` - three FACTS, never a title resemblance. Second leg `pure_revert` (`git cherry` all `-` plus a non-empty two-dot diff) ships OFF: measured 2026-09-05 it fires on 3 of 15 open PRs, a strict subset of leg A's 8, and finds nothing leg A misses. FAIL-OPEN throughout - `checked: False` (no gh, a refused listing, no head sha, no commit list) is NEVER a finding, because the consumer CLOSES pull requests. `fetch_merged_prs` halves its page and retries once on a GraphQL node-budget refusal (`--limit 60` is refused on this repo, `--limit 40` is not). `comment_body` carries the sibling, the shared sha, the two-dot stat, the command that re-derives all three and how to reopen. Survey: docs/audits/mfx-mrg-02-superseded-pr-survey.md | --survey, --state open|closed, --limit, --repo, --json | JSON survey; exit 2 = could not measure |
| PR Linker | tools/kanban/pr_linker.py | Reconciles `kanban_tasks.executor_url` against open GitHub PRs — the link `pr_watcher` needs to see a task's PR at all. Only `kanban.py::_push_branch_and_open_pr` ever writes that column, and only for an un-suffixed `kanban/<id>` branch with a local ref, so PRs opened by retry dispatches (`kanban/<id>-r2`, `-land`) stayed invisible permanently (10 green PRs stranded, 2026-08-02). Maps head branch → task by longest task-id prefix at a `-` boundary; links the newest PR when several are open and reports the rest as ambiguous. **Never overwrites an existing PR link** — a wrong link would make the watcher merge the wrong branch. Called best-effort from `pr_watcher.poll_once` each cycle (config `link_prs_on_poll`, default on). | `--dry-run`, `--json`, `--limit N` | `{linked, ambiguous, already_linked, unmatched}` |
| Re-Verifier | tools/kanban/reverify.py | Recomputes a task's verification from **durable remote git refs** and APPENDS a `kanban_verifications` row. Exists because `pr_watcher._enforced_done_ok` reads only the latest row and nothing but a dispatch writes one, so one bad verdict blocks auto-merge until re-dispatch — which opens a second PR. The bad verdicts come from `kanban.py::_git_worktree_has_real_changes` reading the process-local `_worktrees` / `_dispatch_main_heads` dicts: after a daemon restart all three of its checks skip and it reports "No git commits found on task branch" for a branch full of work. This module reads `git log <base>..origin/<branch>` only, so the answer is process-independent. Leaves `review_passed` NULL (= "not judged, allowed"); it verifies work EXISTS, it does not judge conformance. Also invoked by `pr_watcher._maybe_reverify` when the enforced done-gate holds (config `reverify_on_hold`, cap `reverify_max_attempts_per_task`); it REFUSES to refresh a `review_passed=0` conformance failure or to invent a first verdict for a never-judged task (`reverify_when_missing`, off by default) — either would launder the gate rather than refresh it. | `<task_id>`, `--base`, `--dry-run`, `--no-fetch`, `--json` | `{result, reason, branch, files_changed, commits, written}`; exit 0=passed / 1=failed / 2=no such task |
| Startup Recovery | tools/kanban/startup_recovery.py | The one policy for "the scheduler restarted; what happens to the `in_progress` rows" (kax-recover-04). Both restart sweeps — the `kanban_scheduler.py` entrypoint block and `reflexes/kanban.py::_startup_recover_stale_in_progress` on cycle 1 — call `recover_interrupted_tasks`, because a liveness guard in only one of them buys nothing: the other resets the live task a minute later. The reset is NOT removed (an orphaned `in_progress` row is invisible to every promotion path); it is HELD for any task with provable liveness, from four independent sources, any one sufficient: the scheduler's in-process `_running` handle, a fresh `agent_sessions` heartbeat whose cwd IS the task worktree (component match, so `kax-recover-041` never shields `kax-recover-04`), a `kanban:task:<id>` lease whose holder pid is alive, and an OS process whose `ICDEV_DISPATCH_TASK_ID`/cwd/argv names the task (ONE psutil pass for all candidates). Absence of evidence is death — otherwise nothing is ever recovered. Per reset task the notification says what survived, derived from **git** and not from `kanban_tasks.branch_name` (migration 114 writes that column at COMPLETION, so it is NULL for every interrupted task and reading it as "no branch" is wrong for exactly these rows): `commits preserved on branch kanban/<id> (N commit(s))` / `worktree retained at <path>` / `no branch — work discarded` (the last is severity `warning`). `failure_count` and `last_failure_reason` are left ALONE — an interruption is not a failure, and a reason written here is what put clean tasks into `failure_triage`'s autofix queue. Appends a `startup-recovery` row to `kanban_status_transitions`. `respect_foreign_owner` is what stops a `--once` or Task Scheduler run (which bypasses the entrypoint lockfile check) from sweeping the live daemon's board. Mirrored to icdev/tools/kanban/. | `--dry-run`, `--json`, `--no-notify`, `--no-process-scan`, `--force` | `{swept, reset[{id, provenance{branch, commits, worktree, dirty, recoverable, summary}}], held[{id, reason, detail}], sweep_skipped, reason}` |
| Rebase Recovery | tools/kanban/rebase_recovery.py | Rebases a DIRTY kanban PR branch onto its base so a stale branch is not treated as a human problem (kax-conflict-01). A branch that has merely drifted behind main reports `mergeable: CONFLICTING`, which `pr_watcher` classifies as a resume class — so the PR burns all 5 LLM resumes on a conflict `git rebase origin/main` would have cleared, then escalates to a permanent human queue (measured 2026-08-07: 2 of 5 open kanban PRs DIRTY; `xbm-wake-01` parked 95.7h). Four safety properties: `branch_is_task_owned()` permits ONLY `kanban/<task-id>` or its `-rN` retry sibling (whole-string, not prefix — `kanban/foo-2` is a different task); the rebase runs in a throwaway DETACHED worktree under the system temp dir, never the task worktree or the shared checkout; a rebase with real conflicts is `--abort`ed and pushes nothing; the push is `--force-with-lease` pinned to the sha observed before rebasing. A rebase that replays to zero commits is reported, not pushed (it would empty the PR). Called from `pr_watcher._maybe_rebase` on MERGE_CONFLICT before any resume is spent (config `auto_rebase_on_conflict`, cap `max_rebase_attempts_per_task`); attempts are audited as `pr_watcher.rebase` / `.rebase_failed` so they are a SEPARATE ledger from `pr_watcher.resume` and never consume the resume budget. | `--task <id>`, `--branch`, `--base`, `--repo-root`, `--dry-run`, `--json` | `{attempted, pushed, conflict, reason, branch, base, commits_ahead, old_sha, new_sha}` |
| Re-Queue | tools/kanban/requeue.py | `requeue_task(task_id, *, status, reason, actor, force, get_conn)` — the ONLY supported way to send a task back for a clean rebuild (kax-recover-02). Clears `last_failure_reason` and `branch_name`, sets status (stamping `scheduled_at` via COALESCE when the target is `scheduled`, since `_get_due_tasks` ignores a NULL stamp), and writes a `kanban_status_transitions` row. **`failure_count` / `last_failure_at` are deliberately PRESERVED** — the count is the recovery guard's budget and the real history; zeroing it would launder a task that has genuinely failed five times into a fresh one. Exists because a hand-written re-queue UPDATE bumps `updated_at` while leaving `last_failure_reason` set, and that pair is exactly what `failure_triage.find_recent_failures` selects on — so a *clean* re-queue is indistinguishable from a *fresh failure*. Measured 2026-08-08 (PR #1379): re-queueing the nine `sbx` tasks put five healthy ones into the autofix queue. Reads the current status from the row instead of validating it against `cli.VALID_STATUSES`, so a task parked in a pipeline-owned state (`pr_opened`, which the CLI and the dashboard move API both reject) can be re-queued without widening what `--set-status` may write. Refuses a manual-mode gate sentinel unless `force=True`. Never raises on an unknown task. | `task_id`, `status` (backlog\|scheduled), `reason`, `actor`, `force`, `get_conn` | `{requeued, from_status, to_status, failure_count, cleared[], transition_recorded, error}` |
| Idle Advisor | tools/kanban/idle_advisor.py | Explains WHY dispatch produced nothing, and recommends what a human should do (kpr-idle-01). `diagnose()` classifies the idle state as one of `paused` / `wedged` / `review_bound` / `decision_bound` / `chain_bound` / `drained` — six situations the old `idle (no due tasks)` heartbeat rendered identical. For `decision_bound` it ranks the held manual gates by work that would actually become dispatchable, priority mix, and a penalty for tasks whose titles self-declare BLOCKED/DECLINED (dispatching those burns a session). **Read-only by design — it never releases a gate or moves a task**; a gate is a human's "not yet", and an advisor that opened gates to stay busy would trade intent for throughput. **A gate is only justified by a stated risk** (kpr-idle-02): `gates.declared_risk()` reads a `RISK:` line from the gate's description, and a gate that states none outranks every justified gate for release no matter how much work it holds — procedure ("do not move without a decision") is not a risk and cannot be reviewed. Publishes its own `blind_spots`. Wired into the scheduler heartbeat, which now logs the reason instead of a bare `idle`. | `--json` / `diagnose(conn)` | reason + detail + ranked candidates + recommendation |
| Lease Liveness | tools/kanban/lease_liveness.py | The ONE answer to "is the `kanban:task:<id>` lease guarding live work?" (autonomy-adm-03). A lease records the DISPATCHING pid, which exits after handing off while the worker heartbeats on under another pid, so a pid-only reader gets both errors at once: a dead pid read as dead work (a live worker's lease reaped, the task dispatched twice) and a reused pid read as live work (a stale lease never reaped, the queue starved). `task_lease_verdict(task_id)` reduces BOTH signals to one of four states — `free` / `live` (pid alive, OR `holder_is_alive` returned None: cannot-tell is ALIVE, never litter) / `working` (pid gone, task heartbeating within `HEARTBEAT_LIVE_MINUTES`; NOT reapable) / `litter` (pid gone AND no heartbeat — the only reapable state). The heartbeat is consulted only when the pid is dead. `reap_if_litter` is the only reaper and releases nothing else. `task_is_heartbeating` moved here from the reflex verbatim (rem-hyg-15); the reflex's `_task_is_heartbeating` / `_HEARTBEAT_LIVE_MINUTES` are aliases, not copies. Consumers: `reflexes/kanban.py::_lease_blocks_dispatch`, `cli.py::cmd_release` (refuses a heartbeating task instead of reclaiming it), `idle_advisor._withhold_cause_clause` (a heartbeating task is reported as live work, never as litter), `startup_recovery._lease_holder_pid` (pid half only — a heartbeat stamped by the scheduler that just died is not proof the worker is alive now). `tools.coordination.leases` stays a pid-level primitive: the `kanban:runner:pause` lease has no task to heartbeat. Library, no CLI. Mirrored to icdev/tools/kanban/. | `task_lease_verdict(task_id)` / `reap_if_litter(task_id)` / `describe(verdict)` | `LeaseVerdict{task_id, resource, state, holder, pid_alive, heartbeating}` / `(verdict, reaped)` |
| HITL Notifier | tools/kanban/hitl_notify.py | Delivers the human-in-the-loop queue to a person who is NOT watching the dashboard. `pending()` reads the same firing `pr_watcher:hitl:*` alert rows `/api/hitl/pending` serves, so the terminal, the OS notification centre and the browser popup cannot disagree about what is stuck. `render_terminal()` emits an ASCII-only block prefixed with a bell (a silent print is invisible behind another window; a box-drawing character raises UnicodeEncodeError on a cp1252 console). `desktop_toast()` shells the platform's own notifier — PowerShell/WinRT on Windows, osascript on macOS, notify-send elsewhere — with a list argv, `shell=False`, and the text passed through the ENVIRONMENT rather than interpolated, so a task id containing a quote cannot break the command; a box with no notification daemon returns False rather than raising. `notify()` returns the count for use as an exit code. No third-party dependency: a toast is not worth a package in an air-gapped install. | `pending()`, `render_terminal(items, bell=)`, `desktop_toast(title, body)`, `notify(toast=, stream=)`; CLI: `python tools/kanban/hitl_notify.py` | Terminal block + OS toast; exit 1 when a human is needed |
| Kanban CLI | tools/kanban/cli.py | One-stop wrapper for kanban_tasks operations, always via get_connection(). `--set-status <id...> <status>` is **merge-verified** for `done` (refuses while a branch carrying that task id has commits not on origin/<default>; override with `--force-done --reason`, or SATISFY the gate with `--merge` — see PR Lander). `--reverify <id>` recomputes the done-gate verdict from git and appends a fresh row, clearing a stale block without re-dispatching (exit 0=passed, 1=failed, 2=unknown task). `--requeue <id...> [--requeue-status backlog\|scheduled] [--reason TEXT] [--force]` sends tasks back for a clean rebuild via `tools/kanban/requeue.py` — use it instead of `--set-status <id> backlog`, whose fresh-`updated_at` + stale-`last_failure_reason` pair `failure_triage` reads as a brand-new failure; exit 1 if any task was refused. | --set-status, --merge, --show, --list, --pipeline, --reverify, --requeue, --requeue-status, --force, --dry-run, --claim, --release, --build-mode, --build-model, --json | Status transitions, task views, verification verdicts |
| PR Lander | tools/kanban/land.py | kax-merge-01: library (no CLI of its own) — backs `cli.py --set-status <id> done --merge`. The done-gate only ever REFUSED, so a worker session that genuinely finished could be refused or bypass verification with `--force-done`; neither lands the work. `preflight()` proves the PR is landable, `land()` merges it and then CONFIRMS `state == MERGED` before the caller may write `done` (`gh pr merge --auto` exits 0 while GitHub queues the merge, so the request is not evidence of a landing). Strictly HARDER to satisfy than the refusal it replaces: fail-CLOSED on every unknown (unreadable PR state, unreadable verification, unconfirmable merge) where `_refuses_done` fails open, and it never reads `KANBAN_REQUIRE_MERGE_FOR_DONE` — that switch disables the local git heuristic and cannot disable a single landing check. Gates: PR recorded + OPEN, base == default branch, not CONFLICTING, no CHANGES_REQUESTED, CI green (empty rollup = unknown, not green), approval when `auto_merge_require_approval`, `pr_watcher._enforced_done_ok` (reused, never re-derived), and the sibling-file-conflict guard when `hold_on_sibling_conflict` is set (fail-closed if the open-PR listing is unreadable). Reuses `PRWatcher._auto_merge` / `._open_pr_files` / `._sibling_conflicts` / `list_pr_tasks` / `fetch_pr_state` and `tools.ci.error_classifier`. One task per invocation (merging is an irreversible per-task side effect — no partial batches); not combinable with `--force-done`. | `preflight(task_id, *, get_conn, watcher)`, `land(task_id, *, get_conn, watcher, dry_run, sleeper, confirm_attempts)` | `{ok, merged, pr_url, reason, checks[]}` |
| DES Audit Logger | tools/kanban/des_audit_logger.py | Append-only DES execution audit trail (NIST AU). Inserts dispatch, completion, verification, and gate_override events into `des_execution_events` table — never UPDATE/DELETE. Class `DESAuditLogger` exposes: `log_dispatch(task_id, skill, inputs, executor, source)`, `log_completion(task_id, status, outputs, duration_ms)`, `log_verification(task_id, signals)`, `log_gate_override(task_id, reason, operator)`. Table: `des_execution_events` (migration 065). | `--query --task-id <id> [--json]` | Event ID string / JSON list of events |
| State Machine | tools/kanban/state_machine.py | Kanban task state transition engine — enforces valid status flows (scheduled→in_progress→done/failed) with guard-22 verification gate | Task ID, target status | Transition result |
| Transition Reason Boundary | tools/kanban/transition_reason.py | obs-cov-05: library (no CLI) — `resolve_transition_reason(reason, *, from_status, to_status, actor, skip_frames)` guarantees `kanban_status_transitions.reason` is never blank. A real reason passes through verbatim; a missing one becomes `unattributed: … at <file>:<line> <func> < …`, naming the call site that omitted it. ALL SIX writers of the table must route through it (enforced by `tests/test_kanban_transition_reason.py`): reflexes/kanban.py, dashboard/api/kanban.py, kanban/cli.py, kanban/state_machine.py, ci/pr_watcher.py, mcp/kanban_server.py. #1183 fixed the reason-less call sites but not this boundary, so blank rows kept arriving. Never raises. | import only | Non-empty reason string |
| Source Stats | tools/kanban/source_stats.py | Kanban task source statistics aggregator — counts tasks by source/type for dashboard metrics; also embeds the crx-kan-01 SLA + cycle-time board metrics | --json | JSON stats dict |
| SLA / Cycle-time Metrics | tools/kanban/metrics.py | crx-kan-01: `sla_snapshot` classifies open tasks as overdue / at-risk from the nullable `due_date` / `sla_hours` columns; `cycle_time_metrics` computes p50/p90 cycle time + per-ISO-week throughput from the append-only `kanban_status_transitions` timeline; `board_metrics` combines both for the board summary. kax-stall-01: `throughput_stall_check` answers "is the board completing work at all" — stalled ⇔ zero `to_status='done'` transitions inside `window_hours` WHILE ≥ `min_active_tasks` sit in `STALL_ACTIVE_STATUSES` (scheduled/in_progress), so an empty board reads as idle, not stalled. Cutoff comparison is done in Python (PG/SQLite portable). kax-obs-02: `watcher_heartbeat` answers "is the PR watcher actually polling" WITHOUT the log file — it reads the newest `heartbeat_checks` row with `check_type='pr_watcher_poll'`, written by `tools/ci/pr_watcher.py::_record_heartbeat` at the end of every completed poll, and returns `state` ∈ `never_polled`/`stale`/`polling` plus `last_poll_at`, `tasks_checked`, `actions_taken`. Folded into `throughput_stall_check` as `watcher` + `stall_attribution` (`watcher_not_polling` vs `watcher_polling_nothing_mergeable`) and into `board_metrics`, so a flatline names WHICH failure it is instead of leaving the two indistinguishable. Never raises — a DB without `heartbeat_checks` reports `present: False`. | --weeks N, --stall [--window-hours H], --watcher, --json | JSON `{sla, cycle_time, watcher}` or `{stalled, reason, stall_attribution, watcher, completed_in_window, active_tasks, last_done_at, …}` |
| Board Stall Rule Verifier | tools/testing/verify_board_stall_rule.py | Runtime proof of the board-throughput stall rule on the AMBIENT backend (the unit tests run on SQLite, where `translate_sql` masks dialect bugs). Drives all six scenarios — flatline fires, consecutive cycles don't duplicate, cooldown suppresses re-open after a manual resolve, recovery resolves, idle board stays silent, the self_monitor probe rule leaves the stall alert firing. Refuses to run against a database that already holds kanban/alert rows; point it at a throwaway PG built by `bootstrap_pg.py`. | --json, --force | Per-check pass/fail; exit 0 iff all pass |
| Kanban Verify | tools/kanban_verify.py | Runs coherence gate and records detailed failure reason in `kanban_verifications`. Parses coherence_checker.py JSON output, extracts the first failing rule/file/line, and stores it as the `reason` field instead of the opaque "unknown". | `--task-id <id> [--json] [--dry-run] [--changed-files f1,f2]` | JSON verification result |
| PEV Step Verifier | tools/kanban/pev.py | Plan-Execute-Verify (agx-verify-03): per-step post-execution verification with a three-valued ENUM verdict {verified, unverified, contradicted} and a pure-Python continue/replan/halt policy (`compose_step_policy`). Step checks (file exists on the worktree via explicit `base_dir`, pytest actually ran+passed — 0-collected is NOT a pass, route responds, migration present) reuse existing tooling; fail-closed (no evidence never reads as verified). Appends step verdicts to the existing append-only `kanban_verifications` (no new table). Runner hook `record_completion_pev` is opt-in (`ICDEV_KANBAN_PEV`) and ADDITIVE — never weakens the terminal merge-verify done-gate. | `--task-id <id> --steps '<json>' [--base-dir d] [--max-replans N] [--json]` | JSON {final_action, passed, replans_used, steps[]} |
| AI Skills Gap Seed | tools/kanban/seed_aisg.py | Seed script for the AI Skills Gap Enablement Platform (aisg- prefix). Queues four-phase Kanban initiative: Phase A (Zero-to-AI wizard + role views), Phase B (Guided AI Curriculum + pattern library), Phase C (No-Code AI Customization + visual builder), Phase D (AI Workforce Multiplier Metrics + ROI dashboard). Sets depends_on_task_id and scheduled_at on all seeded tasks. | python tools/kanban/seed_aisg.py [--dry-run] [--json] | {seeded, tasks[], dry_run} |
| ARC (Autonomous Recovery v2) Seed | tools/kanban/seed_arc.py | Seed script for the Autonomous Recovery v2 project (arc-* prefix). Queues 19 tasks across 6 epics (ev/obs/dbg/dec/cal/am) plus a final V&V: traceback_analyzer + evidence_collector foundation, observability + dashboard surfacing of triage, deep code-logic debugger (static-on-diff + iterative hypothesis→repro), decision hardening (self-consistency + expanded verification), calibration feedback loop (append-only triage_runs / triage_outcomes + rolling precision), and calibration-gated auto-merge (ff-only, permanent ceilings retained). Linear `depends_on_task_id` chain; all tasks seeded to BACKLOG via `task_factory.create_tasks`. Design doc: `docs/features/autonomous-recovery-v2-design.md`. | python tools/kanban/seed_arc.py [--json] | SeedReport{seeded, skipped, warnings} |
| UCB (UI→CLI Bridge) Seed | tools/kanban/seed_ucb_kanban.py | Seed script for the UI→CLI Bridge initiative (ucb-* prefix, 12 tasks across 6 epics: be/widget/roll/dic/audit/vv). Idempotent, validator-gated seeder for wiring claude-cli as a routing tier behind the dashboard's per-request bridge UI, with cross-canvas coverage and a final V&V. Routes through the canonical `tools.kanban.task_factory.create_tasks` path (no raw INSERT); dependency-ordered TaskSpecs; plan ref: `~/.claude/plans/please-verify-whether-dic-fancy-jellyfish.md`. | python tools/kanban/seed_ucb_kanban.py [--dry-run] [--json] | SeedReport{seeded, skipped, healed, warnings} |
| DSYN (DIC Canvas Synergy) Seed | tools/kanban/seed_dsyn_kanban.py | Seed script for the DIC Canvas Synergy project (dsyn-* prefix). Queues 7-epic initiative (adapt/reflex/emit/patch/suggest/consist/vv) to wire every ICDEV canvas into the Document Intelligence Canvas so that canvas state changes (network config pushes, compliance findings, posture shifts, AI-ify gaps) automatically surface as AI-drafted document update suggestions queued for HITL review. Architecture: canvas_events bus → canvas_adapter.py → DIC integration Genesis reflex → dic_suggestions table → HITL review queue. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT). | python tools/kanban/seed_dsyn_kanban.py [--dry-run] [--json] | SeedReport{seeded, skipped, warnings} |
| RTED (DIC Real-Time Collaborative Editing) Seed | tools/kanban/seed_rted_kanban.py | Seed script for the DIC Real-Time Collaborative Editing project (rted-* prefix). Queues 11 tasks across 5 epics (lock/history/presence/conflict/vv): pessimistic section locking via `dic_section_locks` table + lock_manager.py, per-section edit history via `dic_edit_history` (append-only, NIST AU) + history_recorder.py, SSE user presence via `dic_presence_sessions` + presence_registry.py, conflict detection via content_hash on save (409 response with both versions), and merge UI modal with Keep mine/Keep theirs/Merge manually. Deliberately avoids OT/CRDT/WebSocket — stdlib-only, air-gap-safe. Direct INSERT (not task_factory) with dependency chain enforced via depends_on_task_id. | python tools/kanban/seed_rted_kanban.py [--dry-run] | Prints INSERT/SKIP per task and summary count |
| Compass Dispatch Probe Seed | tools/kanban/seed_compass_dispatch_probe.py | Seeds the single deliberately-trivial compass task (`prem-vfy-01`, add a CI badge to compass's README) that proves repo-aware dispatch end to end (ked-vfy-01). Task definition is declarative in `args/kanban_seed_compass_dispatch.yaml`; routing to compass comes from the `prem-vfy` prefix in `args/kanban_external_repos.yaml`. FAILS CLOSED: cross-checks the YAML's `target_repo` against `repo_registry.resolve_task_repo` and refuses to seed if they disagree — an unregistered prefix resolves to ICDev by default, which would build a "compass" task inside ICDev's own tree. An external repo with an unset `root_env` is a WARNING (dispatcher SKIPs it), not an error. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT). | python -m tools.kanban.seed_compass_dispatch_probe [--dry-run] [--json] | {ok, dry_run, routing{repo, base_branch, root, dispatchable}, created[], warnings[]} |
| Requirement Gate | tools/kanban/requirement_gate.py | ONE decision table for "was this task ever judged against a requirement" (wire-req-01), consumed by task_factory, pr_watcher and bypass_survey so a survey cannot measure a gate that does not exist. Separates the three states the done gate merges into `allowed`: a criterion that PASSED, one never WRITTEN (review_passed NULL because the column is empty -- 92.5% of tasks), and a verification SKIPPED (bypassed). Two switches, two defaults, both `report`: the done rung measured 96.7% refusal and seed admission would break five live reflexes that seed `fix` cards on a 6-hour cadence. | library (no CLI); KANBAN_REQUIRE_ACCEPTANCE_CRITERIA, KANBAN_REQUIREMENT_GATE = off|report|enforce | (verdict, reason) tuples |
| Requirement Fire-Rate Survey | tools/kanban/bypass_survey.py | What the requirement gate WOULD have refused over recorded history, the survey CLAUDE.md demands before arming (wire-req-01). Calls requirement_gate's own predicates, never a second copy. Reports the COST for seed admission (no right/wrong exists -- refusing a seed withholds no delivered work) and a right/wrong split for the done rung whose `wrong` bucket is an UPPER BOUND, erring against arming. Rates are None, never 0.0, over an empty denominator. | --window-days, --json | per-rung refusal rates + verdict breakdown; exit 2 = could not run |
| OSS Adaptation Seed | tools/kanban/seed_oss_adaptation.py | Seed script for the OSS Adaptation card (oss- prefix): 23 tasks across 12 epics adapting RAGFlow / Crawl4AI / browser-use / STRIX as patterns, not dependencies (analysis: `docs/spikes/oss-00-ragflow-crawl4ai-browseruse-strix-adaptation.md`). Seeds the manual-mode sentinel `oss-gate-00` (held in_progress per tools/kanban/gates.py; title carries GATE_TITLE_MARKER so both the id-suffix and title signals match) plus 22 gated tasks. TWO dependency layers: scalar `depends_on_task_id=oss-gate-00` on every build task, AND 21 junction `kanban_task_deps` edges (`EDGES`) for intra-epic ordering — the scalar layer alone leaves all 22 simultaneously eligible with an arbitrary `created_at` tiebreak, which lets the runner build oss-browse-02 (scope controls) before oss-browse-01 (the primitive) or oss-filter-02 (call-site retrofit) before oss-filter-01 creates the module. `validate()` refuses to seed on unknown edge endpoints, self-deps, or cycles. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT); idempotent for both tasks and edges. Release: `python -m tools.kanban.cli --set-status oss-gate-00 done`. | python tools/kanban/seed_oss_adaptation.py [--dry-run] [--json] | {ok, dry_run, requested, created[], skipped_existing[], edges{declared, added[], already_present[]}, gate, release_with} |
| NAV Menu Readiness Seed | tools/kanban/seed_nav_readiness.py | Seed script for the NAV Dashboard Menu Production Readiness sweep (nav-* prefix). Seeds the manual-mode sentinel `nav-gate-00` (held in_progress forever per tools/kanban/gates.py) plus 34 gated tasks across 9 epics (sec/llm/bld/intel/comp/strat/plat/misc/qa) covering the 2026-07-18 audit of the 8 top-level dashboard menus: auth P0s (env-key auto-login bypass, admin fail-open, usage API scoping), RBAC on HITL/Strategos/pulse/AISG/kanban mutations, XSS fixes + sweep, dead LLMRouter.complete() repair, fabricated-metrics removal (AISG suite), TRUST citation grounding for INTSUM/OPORD, FathomDesk/GeoSIGINT data-honesty labels, and end-of-wave Playwright + gates V&V. MANUAL-ONLY: Opus 4.8 implements in worktrees, push+PR, no auto-merge. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT); idempotent. | python tools/kanban/seed_nav_readiness.py | created/skipped task-id list |
| Stranded-Branch Auditor | tools/kanban/stranded_audit.py | Retroactively reconciles kanban_tasks marked done/validating against origin/<default>: a task whose kanban/<id> branch (local OR origin) still has commits not on origin is STRANDED (built-but-never-merged — the "42/42 done LIED" class). Closes the gap that the transition-time merge-verify gate (_move_task, fail-open, fires only at the done-write) leaves open; also flags validating rows with no branch (validating is otherwise dead). FAIL-OPEN on git errors. Surfaces findings to a JSON report + files HITL 'suggested' remediation cards via task_factory.create_tasks (stable ids dedupe daily re-runs; never fed to the enforcing runner). Genesis reflex kanban_stranded_reflex (daily 24h, green). Headless: `python -m tools.kanban.stranded_audit --json`. Mirrored to icdev/tools/kanban/. | --json / --no-cards / --no-fetch | {default_branch, total, stranded[], orphan_validating[], clean_count, cards_filed[]} |
| Source Stats | tools/kanban/source_stats.py | Kanban task source statistics aggregator — counts tasks by source/type for dashboard metrics | --json | JSON stats dict |
| AHX/ARR/CLX Seed | tools/kanban/seed_ahx_arr_clx.py | Seed script for the three cards carrying the adopted third of the five agent-harness audit docs in `C:\AI\searches` (disposition: `docs/spikes/ahx-00-agent-audit-docs-disposition.md`). Seeds 3 manual-gate sentinels (`ahx-gate-00`, `arr-gate-00`, `clx-gate-00`, all held in_progress) plus 16 gated tasks. ARR and CLX are gated behind AHX because neither can be evaluated until the harness can measure outcomes. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT); idempotent. | python -m tools.kanban.seed_ahx_arr_clx [--dry-run] [--json] | {created_gates[], created_tasks[], skipped_existing} |
| Flow Control | tools/kanban/backpressure.py | Bounds finished-but-UNREVIEWED output in the autonomous build loop (clx-flow-01). `MAX_IN_PROGRESS` caps tasks that are *executing*; a task moving to `pr_opened` stops being counted, frees a slot, and the loop dispatches more — so open PRs stack without limit, conflict with each other, and defer the human review that is the loop's feedback signal. Counts `pr_opened`/`ci_failed`/`changes_requested`/`merge_conflict`/`validating` and narrows or holds dispatch. OFF unless `KANBAN_BACKPRESSURE_ENABLED=1` (throttling throughput is an operator's call); ceiling via `KANBAN_MAX_UNREVIEWED` (default 3). Fails OPEN — a counting error never wedges dispatch. Wired into `promote_backlog_to_scheduled`'s slot calculation. | `apply_backpressure(slots)`, `count_unreviewed()`, `status()` | int / dict |
| HGX Harness Parity Seed | tools/kanban/seed_hgx_kanban.py | Seed script for the HGX card (`hgx-` prefix) — Harness Agent Parity and Graph Runtime. Seeds the manual sentinel `hgx-gate-00` held in_progress plus the gated `hgx-*` task set across the exec/ctxw/guard/port/sess/goal/eval/gov/cfg epics, each carrying `depends_on_task_id` (the scalar dep is what actually blocks `_deps_satisfied()`; the id prefix is only a naming convention). Every task description restates the card's binding LLM-agnostic / OS-agnostic acceptance criteria and the "extend the existing surface" ground rule (Studio `workflow_runner.py` is already the durable DAG runtime; `tools/gateway/adapters` is already the ingress) so a cold session cannot build a second engine. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT); idempotent. Release: `python tools/kanban/cli.py --set-status hgx-gate-00 done`. | python tools/kanban/seed_hgx_kanban.py [--dry-run] [--json] | {created[], created_count, submitted_count, skipped_existing[], gated, gate, release} |
| SBX SBOM-2026 Seed | tools/kanban/seed_sbx_sbom2026.py | Seed script for the SBX card (sbx- prefix): SBOM conformance against the 2026 Minimum Elements for a Software Bill of Materials (CISA with NSA, FBI and 16 international partners, published 2026-07-29, document version 2.1), which REPLACES the 2021 NTIA minimum elements. Seeds the manual sentinel `sbx-gate-00` held in_progress plus 19 gated tasks across 7 epics (fnd/fld/cov/prc/sig/fmt/gov): deprecation-marker resolution on the load-bearing `tools/compliance/sbom_generator.py`, schema migration for the new elements, the 17 required data fields, transitive Coverage + CycloneDX dependency graph, unknown-vs-withheld semantics, SBOM author signature, a minimum-elements conformance validator, an SPDX writer, and conformance-based gating. Every task carries `depends_on_task_id=sbx-gate-00` — the scalar dep is the mechanism that actually blocks `_deps_satisfied()`, not the id naming convention. Gap analysis: `docs/compliance/sbom-2026-minimum-elements-gap-analysis.md`. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT); idempotent. Release: `python -m tools.kanban.cli --set-status sbx-gate-00 done`. | python tools/kanban/seed_sbx_sbom2026.py [--dry-run] [--json] | {created[], count, requested, gate} |
| OBS Observability→CLI Seed | tools/kanban/seed_obs_observability_cli.py | Seed script for the OBS card (obs-* prefix) — Observability → CLI wiring. Scope was derived by verifying `docs/observability_cli_kanban_integration_analysis.md` against the LIVE PostgreSQL board (`docs/observability_cli_kanban_suitability_review.md`) rather than taking the analysis at its word: most of its twelve proposals rested on tables no code writes, so only the measured-real surfaces (`audit_trail`, `kanban_status_transitions`) were seeded. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT). | python tools/kanban/seed_obs_observability_cli.py [--dry-run] [--json] | SeedReport{seeded, skipped, warnings} |
| OBS Observability CLI Seed | tools/kanban/seed_obs_observability_cli.py | Seed script for the OBS card (`obs-` prefix) — Observability → CLI wiring. Seeds the manual sentinel `obs-gate-00` held in_progress plus gated `obs-*` tasks, each carrying `depends_on_task_id` (the scalar dep is what actually blocks `_deps_satisfied()`). Scope was cut to the one proposal with data behind it: an audit-tail CLI over `audit_trail` (15,173 rows, 165 INSERT sites) and `hook_events`. The rest are seeded as explicitly-blocked tasks carrying the measurement that blocks them — `otel_spans` and `metric_snapshots` are empty with no live INSERT path, so a task built on them would be unactionable. Verification: `docs/observability_cli_kanban_suitability_review.md` (measured the live PostgreSQL board rather than trusting `docs/observability_cli_kanban_integration_analysis.md`). Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT); idempotent. | python tools/kanban/seed_obs_observability_cli.py [--dry-run] [--json] | {created[], skipped_existing[], gate} |
| Policy Drift | tools/kanban/policy_drift.py | kax-merge-02: keeps the operating policy COPIED into task descriptions in sync with the card. Policy is pasted in at seed time, so correcting the card corrects nothing that already exists — on 2026-08-08 the HGX card and its seeder were both fixed to say "open PRs normally, not `--draft`" and 35 of 38 hgx rows kept the original sentence; a dispatched session reads its DESCRIPTION, so they went on opening drafts, `pr_watcher` may not merge a draft, and finished green work needed a human three separate times (8 PRs, then 7, then 5). Single source is `policy:` on the project entry in `args/projects.yaml`; a description carries it as a delimited `<!-- icdev:policy <key> -->` block whose body is re-synced on every scan (exact, idempotent, no pattern matching). Rows that predate the block are migrated ONCE by a `legacy_pattern` rule in `args/kanban_policy_drift.yaml` — that file describes only what is STALE and never the replacement text. Scoping is FAIL-CLOSED: `applies_to.task_id_prefix` is required, must start with the rule's own card `task_prefix`, and `load_rules` raises on an unscoped/uncompilable/unexplained rule or one targeting `exempt_projects`. `exempt_projects` is a veto outranking a `policy:` field — `agov` is listed there because AGOV is MANUAL-ONLY and its 19 rows legitimately say `--draft` (verified byte-identical across a migration run). `load_exemptions()` reads only that veto and never validates rules, so one bad rule cannot take down every seeder; an unreadable list exempts EVERYTHING. Writes only `backlog`/`scheduled` (a dispatcher can still hand those to a session, and no live session holds them); `in_progress` and `done` are reported, never rewritten. Each rewrite appends a `kanban_task_comments` audit row. `apply_policy_block()` is called from `task_factory.create_tasks` so new rows are born card-linked. Dry run is the default. Decision record: `docs/features/kax-merge-02-policy-drift.md`. | `--fix`, `--statuses`, `--fix-statuses`, `--project`, `--json` | `{drifted, rules, exempt_projects, findings[], fixed[], skipped[], dry_run}` |
| Policy Drift Reflex | tools/genesis/reflexes/kanban_policy_drift.py | The scheduled half of `tools/kanban/policy_drift.py` — a drift checker nobody invokes gets noticed the same way the drift did, by watching the pipeline misbehave. 6h cadence, `risk_tier: amber`, registered at all four points (`daemon.REFLEX_NAMES`, `reflex_registry.py`, `args/genesis_config.yaml`, this shard). Scans EVERY status so a `done` row disagreeing with its card is still visible, but rewrites only `backlog`/`scheduled`. `metric_value` is `unresolved_dispatchable` (not total drift) so a reported-only archive row cannot hold the success metric permanently red and trip the circuit breaker. `dry_run: true` in config leaves the report and turns the rewrite off. Never raises. | `run(ctx, conn)`; `ctx` accepts `dry_run`, `project` | `{drifted_total, drifted_dispatchable, rewritten[], reported_only[], unresolved_dispatchable, success, metric_value, details}` |
| Dispatch Reaper | tools/kanban/dispatch_reaper.py | Kills the process tree behind a reaped kanban task. The stale-reaper previously only flipped a status, so an orphaned tree kept its worktree and port and the scheduler re-dispatched into it. Kills ONLY when the recorded pid provably still belongs to the dispatched process — pids are reused, and every unknown declines. Windows identity via ctypes `GetProcessTimes` (wmic is removed on build 26200). | `record_dispatch(conn, task_id, pid)`; `kill_recorded_dispatch(conn, task_id)` | `{killed: bool, reason: str}` |
| HITL Alert View | tools/kanban/hitl_alert_view.py | Parses what a firing `pr_watcher:hitl:*` alert already says — cause, resume budget, PR link — out of its `description`, and states which remediations that cause allows. The monitoring page never rendered `description`, so on 2026-08-10 those facts were read out of the database while 12 alerts were cleared by hand; and `rebase` was offered for the 10 whose cause was `merge_conflict`, which `rebase_and_push` refuses by design. Parsed here rather than in the template so the dashboard route and `/api/kanban/alerts/<id>/action` reach the SAME verdict — a disabled button and a refused request cannot diverge. Never raises on an unrecognised description: the text is prose from another module, and a page that 500s because a sentence drifted is worse than a row saying "unknown". | `parse_alert(alert)`, `task_id_from_source(source)`, `rebase_refusal(cause)`, `action_is_available(action, cause)` | dict of cause / cycle / pr_url, or None for a non-HITL source |
| Conflict Resolvers | tools/kanban/conflict_resolvers.py | Resolves only the merge conflicts that are provably not disagreements: additive doc blocks (two branches each append an independent block to a shared reference file) and allocation-number collisions (two branches each take "the next free number"). Never touches code, never resolves a conflict where either side DELETED a line, and runs only on an allowlist of files whose format it understands. Returns None when it cannot prove a case is safe, so the caller falls through to the existing abort-and-escalate path. | (library) | Resolved text, or None |
| CTX Cortex Activation Seed | tools/kanban/seed_ctx_kanban.py | Seed script for the CTX card (`ctx-` prefix) — Cortex Activation & Hardening, successor to `cxo`. Seeds the manual sentinel `ctx-gate-00` held in_progress plus 20 gated `ctx-*` tasks across the trust/enf/obs/perf/reach epics, each carrying `depends_on_task_id=ctx-gate-00` (the scalar dep is what actually blocks `_deps_satisfied()`; the id prefix is only a naming convention). Refuses to seed if any task is ungated or an id is duplicated. Covers the five governance-correctness defects (undeclared `summarization` routing function -> cloud-first fallback under air-gap; REST running the TRUST chain twice; IQE adapters closing a caller-owned connection; the 500-row pre-filter cap that returns confidence 1.0 on a truncated scan), the two gates that cannot fire (vendor parity in CI, 56 ungated Cortex test modules), observability before optimization, and the three inert defaults. `ctx-reach-01` is MANUAL even after release — it touches the private compass/idea_lab repos the runner cannot build in. Routes through canonical `tools.kanban.task_factory.create_tasks` (no raw INSERT); idempotent. Release: `python tools/kanban/cli.py --set-status ctx-gate-00 done`. | python -m tools.kanban.seed_ctx_kanban [--dry-run] [--json] | {created[], created_count, skipped_existing[], gate, context} |
| Landed Check (task -> main) | tools/kanban/landed_check.py | trust-disc-05: the board tracks task -> PR and NOTHING checked task -> main. On 2026-08-15 two of five cards in `pr_opened` had their work already merged under a DIFFERENT PR number — ctx-perf-02 landed as #1641, ctx-trust-02 as #1638 — while #1646 and #1651 stayed open against them; both conflicted because both re-apply changes already present against files that have moved on (#1651's diff was -38/+26 on rest_v1.py, i.e. merging it would DELETE 38 lines main has — a revert wearing a feature's clothes). One `git log -E --grep` against origin/<default>, with TIERED evidence: `merge_ref` (a merge commit naming a branch carrying the id) and `subject` (id in the commit subject) block; `body` never does, because a body mention is a citation at least as often as a landing (commit a758250c0 cites ctx-trust-02 while implementing ctx-reach-03). Matching is on a NAME BOUNDARY so ctx-perf-02 does not match ctx-perf-021 and a parent id does not match its decomposed children. FAIL-OPEN everywhere: no git, an unresolvable origin ref, or a non-id-shaped id all report `checked: false` — an unavailable check must never read as a clean one. `check_landed_bulk` answers a whole batch in one git call (3,176 done tasks in 56.8s, chunked at 150 ids for the Windows argv cap). Second half `rival_prs`: only the `kanban/<task_id>` branch settles a card, and ctx-enf-01 had #1640 and #1647 open at once. Wired at three seams — `task_factory.create_tasks` (seed), `reflexes/kanban.py::_write_prompt_file` + the pre-dispatch loop, and `_push_branch_and_open_pr` (PR body banner). ADVISORY by default (`KANBAN_LANDED_CHECK=warn`); `enforce` refuses, `off` disables. Survey before enforcing: 1,003/3,176 done tasks (31.6%) detected, 207 body-only (non-blocking), 0 fires on the non-terminal population. Mirrored to icdev/tools/kanban/. | `--task <id>` (repeatable) / `--all` / `--status` / `--branch` / `--no-prs` / `--json` / `--gate` | `{checked, unchecked[], landed[], referenced[], mode}` |
| Task Identity (seed-time card coverage) | tools/kanban/task_identity.py | rem-hyg-02: answers, for ONE task id, the question nothing asked at seed time — does a registered card own its prefix, and does an epic key claim it? Every number on a project card comes from `<task_prefix><epic_key>-%` patterns, never from `task_prefix` alone, so a row no epic matches is counted by NOTHING and a card all of whose rows are unclaimed vanishes entirely — indistinguishable from a project with no work. `check_project_card_coverage` finds that AFTER the fact and its own docstring names the gap: 'invisible at seed time, because task_factory does not consult the project registry'. Measured 2026-08-16 seeding HCX by hand: `hcx-` was absent from all 150+ `task_prefix` values while the BOARD already held 25 rows under it — reading the YAML proved nothing, and by the time a manual board query found it the scheduler had dispatched three of the new tasks. Pure functions, no DB/env/LLM; the only I/O is reading `args/projects.yaml` in `load_cards`, which fails OPEN. Nesting is NOT re-derived — `prefix_scope.child_prefixes` decides that `aadc-enh-01` belongs to `aadc-enh-` and not `aadc-`. Three zeroes that are never findings and are kept out of `ACTIONABLE_REASONS`: an unreadable registry (`no_registry` — else a missing config file becomes a finding against every id on the board), a `<prefix>gate-<n>` sentinel (holds the card, is not work, `gate` is a RESERVED epic key; recognised with the dispatcher's own `gates.has_gate_id`), and a blank id. Wired REPORT-ONLY into `task_factory.create_tasks` as a fourth pre-insert check — evaluated before any insert so the eventual refusal cannot half-land a batch, warns naming each unclaimed id and the id it should have carried, and any exception leaves seeding exactly as it was. Arming it is rem-hyg-04, and only after rem-hyg-03 has measured the fire rate. Mirrored to icdev/tools/kanban/. | library — `load_cards()`, `resolve(id, cards)`, `unregistered_prefixes(board_ids, cards)`, `check_batch(specs, cards)`, `suggested_id(id, card)` | `resolve` -> `{card, epic, prefix, claimed, reason}`; `check_batch` -> `[{task_id, reason, card, prefix, epic, suggestion, detail}]` |
| Merge Readiness (decision table + report) | tools/ci/merge_readiness.py | kpr-watch-01: THE merge-eligibility ladder, extracted from `pr_watcher._sweep_unlinked_prs` where it was a run of bare `continue` statements — draft, hold label, wrong base, not MERGEABLE, not passing, changes requested. Every refusal was SILENT except the label case and the whole sweep returned immediately under `--dry-run`, so the pipeline could MERGE a PR but could not ANSWER "which PRs are awaiting merge, and why is each one not merging": the actor existed, the observer did not. `classify_merge_readiness(pr, *, default_branch, linked_urls) -> (state, reason)` is PURE — no subprocess, no DB, no network, no clock, no LLM — over one `gh pr list --json` entry. States, in ladder order: `merged` / `linked` (a kanban task owns it) / `draft` / `held_label` / `wrong_base` / `conflicting` / `no_checks` / `ci_failed` / `awaiting_ci` / `changes_requested` / `ready`, plus `unknown` for a record with no url. `_sweep_unlinked_prs` CONSUMES it and merges only on `ready`, so the report can never drift into describing a merge policy the merger does not have — the same one-table-two-consumers shape CLAUDE.md mandates for `decide_discrimination`; do not write a second copy. Two distinctions it refuses to blur: `no_checks` (empty rollup, nothing ever reported — you may need a workflow) is not `awaiting_ci` (checks running — you need patience), and `mergeable=UNKNOWN` (GitHub still computing) carries a different REASON from `CONFLICTING` even though both refuse identically, so nobody rebases a branch that has no conflict. The CLI is READ-ONLY and proven so by AST in `tests/test_merge_readiness.py`: exactly one subprocess reference and exactly one argv, which must be `gh pr list`. Exit 2 when the report COULD NOT BE PRODUCED — an unreadable `gh pr list` must not print the same empty table as a repo with nothing open — and an unreadable kanban board sets `linked_lookup_ok: false` rather than silently calling every PR unlinked. Parity with the pre-extraction ladder is the acceptance test: 2,880 signal combinations plus a live sweep of all 9 open PRs on 2026-08-16, 0 mismatches. Mirrored to icdev/tools/ci/. | `--json`, `--state <s>` (repeatable), `--limit N`, `--default-branch`, `--from-json <path>` | `{default_branch, linked_lookup_ok, total, ready, counts{}, prs[{number,url,state,reason,ready,mergeable,is_draft,labels,linked}]}`; exit 0=reported / 2=could not run |
| Raw-INSERT Board Writer Census | tools/kanban/raw_insert_census.py | rem-hyg-05: `task_factory.py` opens with "Canonical task seeder — never use raw INSERT directly" and nothing had ever checked it. Measured 2026-08-16 over `tools/` + the `icdev/tools/` mirror: 231 raw board INSERT sites across 209 files, 219 of them debt once the seeder itself and `db/migrations/**` are excluded — roughly seven board writers in ten bypass the seeder, and 42 of those sites are the AUTONOMOUS path (`tools/genesis/reflexes/*`). A raw INSERT skips `VALID_TASK_TYPES` (which PG enforces and SQLite silently does not), the `_assert_real_board` refusal that stops a seed run landing in a throwaway worktree database, the gate-id/risk-marker checks and the dedupe — and reports success anyway. A gate INSIDE `create_tasks` cannot be the whole answer: it only ever sees the 30% that already call it. Census is per SITE (`<file>::<qualname>[<n>]`), not per file, so a grandfathered module cannot grow a second and third writer unobserved; line numbers are deliberately absent from the key so an edit above a site is not a census edit. ENUMERATED, never counted — a bare count can be held constant while the set churns, which is how the ungated-test gap regrew behind a green gate. The predicate is statement-SHAPED (`kanban_tasks` followed by a column list / VALUES / SELECT / DEFAULT VALUES / alias) because the unqualified form flagged its own caller five times on docstrings and log lines; measured at AST level it drops 16 of 255 matches and every one is prose. An f-string interpolation becomes `?` so a runtime column list keeps its shape; an interpolated TABLE name is the documented non-goal, and it was MEASURED before being accepted — of the 53 files that build an INSERT target with an interpolation, the only four that also mention `kanban_tasks` are the two `storage.py::translate_sql` copies and the two `coherence_checker.py` copies. Cross-check: a file whose TEXT holds the pattern while the AST attributes no site is an ERROR, so the census can never be silently narrower than a grep. Ceiling `raw_insert_max` in `args/board_writer_gate.yaml` MAY ONLY GO DOWN. Converting the 219 is rem-hyg-06; this only stops the set growing. Gate consumer: `coherence_checker.py --check board_writer_census`. Mirrored to icdev/tools/kanban/. | `--check`, `--json`, `--changed <paths>`, `--staged`, `--seed [--force]`, `--prune`, `--root` | `{total_sites, total_files, registered, unregistered[], raw_insert_max, stale[], text_only_files[], per_file{}, errors[], ok}` |
| Task Identity (seed-time card coverage) | tools/kanban/task_identity.py | rem-hyg-02: answers, for ONE task id, the question nothing asked at seed time — does a registered card own its prefix, and does an epic key claim it? Every number on a project card comes from `<task_prefix><epic_key>-%` patterns, never from `task_prefix` alone, so a row no epic matches is counted by NOTHING and a card all of whose rows are unclaimed vanishes entirely — indistinguishable from a project with no work. `check_project_card_coverage` finds that AFTER the fact and its own docstring names the gap: 'invisible at seed time, because task_factory does not consult the project registry'. Measured 2026-08-16 seeding HCX by hand: `hcx-` was absent from all 150+ `task_prefix` values while the BOARD already held 25 rows under it — reading the YAML proved nothing, and by the time a manual board query found it the scheduler had dispatched three of the new tasks. Pure functions, no DB/env/LLM; the only I/O is reading `args/projects.yaml` in `load_cards`, which fails OPEN. Nesting is NOT re-derived — `prefix_scope.child_prefixes` decides that `aadc-enh-01` belongs to `aadc-enh-` and not `aadc-`. Three zeroes that are never findings and are kept out of `ACTIONABLE_REASONS`: an unreadable registry (`no_registry` — else a missing config file becomes a finding against every id on the board), a `<prefix>gate-<n>` sentinel (holds the card, is not work, `gate` is a RESERVED epic key; recognised with the dispatcher's own `gates.has_gate_id`), and a blank id. Wired REPORT-ONLY into `task_factory.create_tasks` as a fourth pre-insert check — evaluated before any insert so the eventual refusal cannot half-land a batch, warns naming each unclaimed id and the id it should have carried, and any exception leaves seeding exactly as it was. ARMED by rem-hyg-04 behind `KANBAN_IDENTITY_CHECK=enforce\|report\|off` (`mode()`, same shape and spellings as `KANBAN_LANDED_CHECK`; an unrecognised value resolves to the default and LOGS it, because `=enforced` must not read as armed). DEFAULT `report`, and that is what rem-hyg-03's survey supports rather than arming left half-done: refusing every unclaimed id fired on 35.17% of 3,244 live rows, and the narrowing — `is_enforceable`, which exempts OPAQUE machine ids (`task-<hex>` from the dashboard's create-task API) but never `no_epic` — gives 10.85% lifetime and 15.81% over 30d, ten times the rate CLAUDE.md calls refusing routine work. `classify_shape`/`SHAPE_*` MOVED here from `identity_survey` so the survey's NARROWED column and the seeder's refusal are one predicate and cannot drift. `check_batch` findings carry `shape` and `enforced`, and an exempt finding is still REPORTED with `(REPORT ONLY ...)` appended — exempt from the refusal, never from the report. Nothing in this module raises; the refusal is `create_tasks`', before the first insert, naming each id, its suggested `<prefix><epic>-<N>`, args/projects.yaml and the way to stand it down. Mirrored to icdev/tools/kanban/. | library — `mode()`, `is_enforceable(reason, id)`, `classify_shape(id)`, `load_cards()`, `resolve(id, cards)`, `unregistered_prefixes(board_ids, cards)`, `check_batch(specs, cards)`, `suggested_id(id, card)` | `resolve` -> `{card, epic, prefix, claimed, reason}`; `check_batch` -> `[{task_id, reason, card, prefix, epic, shape, enforced, suggestion, detail}]` |
| Identity Survey (fire rate) | tools/kanban/identity_survey.py | rem-hyg-03: the fire-rate survey that must exist BEFORE rem-hyg-04 arms the rem-hyg-02 identity check. CLAUDE.md's rule, learned from the PreToolUse hook: over 96,818 real tool calls eight of twelve checks were refusing routine work (4.86%, worst one call in forty), and `check_write_outside_worktree` had shipped as a hard block with its rate never once observed because `\|\| true` discarded everything it returned — a check that is nominally enforcing but never measured is UNMEASURED, not proven. Replays `task_identity.resolve` over every id on the board. Measured 2026-08-16 against the live PostgreSQL board (3,243 rows, 163 cards): 2,041 claimed, 53 gate sentinels, 22 `no_epic` (all `pgrt-`), 1,127 `no_card` — a 35.43% fire rate that arming naively would carry. The narrowing is the finding: 789 of the 1,127 are OPAQUE machine ids (`task-<hex>` from the dashboard's own create-task API and `awareness/suggested_card_writer`, `mc-reflex-<hex>`, `cpmp-<hex>`) which were never card work, so refusing them would refuse routine work exactly as the eight PreToolUse checks did. Exempting them gives 11.10% lifetime and 4.23% over the last 7d, against 17 genuinely-unregistered card prefixes (prem- 72, cnr- 46, shx- 33, docmod- 32 …). `classify_shape` is a NAMED heuristic over id text and never decides whether an id is claimed — `-d<N>` decomposition suffixes are stripped first, so `mvs-audit-03-d1` counts as card work and `task-fd99a9c8ae-d5` does not. Scope caveat for rem-hyg-04: 95 modules `INSERT INTO kanban_tasks` directly and never reach the `create_tasks` seam, so arming there cannot see most of the opaque population. Two zeroes that are never a clean bill of health and report `measured: false` rather than 0%: an unreadable `args/projects.yaml` (`no_registry`) and an empty board (`empty_board` — the worktree trap, where a missing `.env` silently reads a throwaway SQLite DB; `--env-file` points at the real board). REPORT ONLY — no `--gate`, no writes, one `SELECT`. | `--json`, `--ids`, `--card <key>`, `--status <s>`, `--window <days>`, `--env-file <path>` | `{measured, unmeasurable_reason, board, registry, totals{claimed, gate_sentinels, no_epic, no_card, no_card_card_shaped, no_card_opaque, actionable, fire_rate, actionable_narrowed, fire_rate_narrowed}, windows[], by_card[], unregistered_prefixes{}, unclaimed[]}` |
| Lane Conflicts (seed/dispatch-time sibling contention) | tools/kanban/lane_conflicts.py | rem-hyg-07: asks at SEED time the question `pr_watcher`'s `hold_on_sibling_conflict` only asks about OPEN PRs — will two tasks fight over the same file? By PR time both sessions have already built: the cheapest outcome is a rebase and the usual one is a binned branch (#1684 dispatched a producer and its consumer together and 1,058 lines were discarded). Reads every non-terminal row, extracts the paths its description names, builds the dependency closure over BOTH mechanisms (scalar `depends_on_task_id` AND the `kanban_task_deps` junction, because `_deps_satisfied` ANDs them — consulting one alone reports a hand-serialized pair as a live race), and reports unserialized pairs sharing a non-coordination file. RANKED `live` vs `latent`: a backlog task whose dependency is unsatisfied cannot race today, and reporting the two identically is how a real finding is lost in noise. Gate sentinels are dropped via `gates.is_manual_gate`, so a path in a `RISK:` description is not contention. TWO EVIDENCE GRADES, never merged: `prose` (available at seed time, the only time it helps, and inherently a heuristic) and `branch` (`git diff --name-only origin/main...kanban/<id>`, exact but only once a branch exists — `--from-branches`); where a branch exists its paths REPLACE the prose guess rather than joining it. Each branch is compared to `origin/main` and NEVER to another branch: `git merge-tree` between two task tips reports conflicts the forge will never see, since the forge merges each into main in sequence (measured 2026-08-16, hcx-live-02 vs hcx-live-03 reported CONFLICT while against main hcx-live-03 was CLEAN). Six suppressions, each for a class OBSERVED on the live board rather than invented ahead of it — `command` (a path inside `python tools/ci/red_first_gate.py --gate` is a tool to RUN), `evidence` (a specimen in a caps-led `MEASURED`/`OBSERVED` paragraph, deliberately NOT rescued by a write verb since such a paragraph narrates writes that already happened), `precedent` ("Follow args/ci_test_backlog.txt"), `citation` (an explicit "see X" cross-reference, or a `docs/` path with no write verb on its line), `negated` ("Do NOT change ..."), and `coordination` (delegated to `tools/git/coordination_paths.py`, the same list the merge-time guard uses). Those rules took the live board from 3 `live` findings of which 0 were real to 0 `live` / 8 `latent`, every survivor matching a contention identified by hand. Wired REPORT-ONLY into `task_factory.create_tasks` as a fourth pre-insert check — evaluated before any insert so a future refusal cannot half-land a batch, and fail-open on any error. Arming it needs a fire-rate survey first, exactly as rem-hyg-03/04 do for the identity check. | `--json`, `--live-only`, `--from-branches`, `--task <id>`, `--base <ref>`; library — `check_batch(specs)`, `claimed_paths(text)`, `mentions(text)`, `branch_paths(id)` | `{measured, task_count, tasks_claiming_files, evidence_mode, counts{live, latent}, conflicts[{tasks[], shared_files[], severity, dispatch_state{}, status{}, evidence{}, detail}]}` |
| Sibling-Hold Survey | tools/ci/sibling_hold_survey.py | Fire-rate survey for `pr_watcher`'s `hold_on_sibling_conflict`, run BEFORE widening it. kpr-watch-07 showed GitHub does not apply `.gitattributes` merge drivers, so the union-merged paths `coordination_paths.py` excludes from the sibling check are real forge collisions — but widening the check is the change `GENERATED_PATH_MARKERS` records being burned by (one universal file made every open PR a sibling of every other, 2026-08-09). Replays each merged PR's merge instant under both postures. MEASURED 2026-08-17 over 120 PRs: current 35/120 held (29.2%, max clique 5) vs widened 78/120 (65.0%, max clique 13) — so do NOT widen. Union patterns are parsed from `.gitattributes`, never hardcoded, so the survey cannot drift from the rules it measures; `*` is matched not-crossing-`/` because over-claiming would make `widened` look milder than it is. Reports `held_by_unmergeable`, the question the historical replay structurally cannot answer (it only samples moments a merge happened, so 'nobody free' can only ever read 0). An unavailable corpus exits 2 rather than printing a clean survey. | `--limit N`, `--open-only`, `--json` | per-posture held share, max clique, stuck-blocker list |
| Dependency Gating (the dispatch rule) | tools/kanban/deps.py | kpr-fix-02: the ONE predicate that answers "which dependencies actually gate this task". The board carries TWO mechanisms and every dispatch decision used to require BOTH: `kanban_task_deps` is the deliberate fan-in graph, while `kanban_tasks.depends_on_task_id` is mostly SEEDING ORDER — the linear chain a seeder writes as it walks its batch — so the accidental declaration overrode the real one. MEASURED on the live board 2026-08-18: `cef-di-03/04/05/06` had all three real prerequisites done (`cef-rsv-01/02/03`) and were blocked solely by batch order; 16 backlog tasks, 15 dependency-blocked, ZERO dispatchable across an entire day, five independent migrations onto one already-built API forced to run one at a time. THE RULE: junction rows, when a task has them, ARE its declaration and the scalar adds no second gate; with no junction rows the scalar is the only declaration and is honoured in full. ONE EXCEPTION — a scalar pointing at a MANUAL GATE always holds, junction rows or not, because a gate is a HOLD rather than seeding order and releasing it would discard the one deliberate scalar on the board (`kpr-stale-02` carries both, so without the carve-out the guarantee would be accidental). Rendered THREE ways from one rule and tested against itself: `deps_satisfied`/`blocking_deps` (per task), `blocking_deps_bulk` / `blocking_dep_status_bulk` (three queries for a whole board — the per-task form on the dashboard list endpoint is an N+1), and `dep_clause_sql` (a WHERE fragment for the set-based dispatch query). `dep_clause_sql` returns `(clause, params)` and its manual-gate LIKE patterns are BOUND, never interpolated: a literal `%` becomes a psycopg format directive the moment any parameter is passed — including one RLS injects. Two cases the old copies disagreed on are now settled: a dangling junction dep BLOCKS in both renderings (the old SQL's inner JOIN dropped it silently), and SQL gate recognition is deliberately WIDER than `gates.is_manual_gate` — it may only ever hold MORE, never release more. CONSUMERS, all six enforcement sites and five reporters: `promote_backlog_to_scheduled`, `genesis/reflexes/kanban::_get_due_tasks` (SQL), `::_parent_is_done` and `::_orphan_rows` (which ROLLS A DONE TASK BACK, so a superseded scalar there re-dispatches finished work into a revert PR), `dashboard/api/kanban` done-guard + `is_blocked`, `ci/pr_watcher::_held_by_a_gate` (else a released task's PR sits in draft forever), `balance_scheduler`, `analyze_backlog`, `kanban/cli --list`, and `lane_conflicts.load_board` — whose edge set must narrow too, since an edge SUPPRESSES a conflict and keeping a superseded one would report a live race as safely serialized. Do NOT write a seventh copy. Import-free apart from `tools.kanban.gates`. Mirrored to icdev/tools/kanban/. | library — `deps_satisfied(id, conn)`, `blocking_deps(id, conn)`, `blocking_deps_bulk(ids, conn)`, `blocking_dep_status_bulk(ids, conn)`, `dep_clause_sql(alias)`, `parent_holds_done(id, conn)`, `effective_dep_ids(scalar, junction, scalar_is_gate=)`, `scalar_is_superseded(...)`, `junction_dep_ids(id, conn)` | `blocking_deps` -> `[dep_id | "<id> (missing)"]`; `dep_clause_sql` -> `(sql_fragment, (gate_id_pattern, gate_title_pattern))`; `parent_holds_done` -> `(may_complete, reason)` |
| Landed Check — Dispatch Survey | tools/kanban/landed_dispatch_survey.py | kpr-fix-03: the fire-rate survey CLAUDE.md demands before arming a check, for the population the pre-dispatch landed check actually acts on. The survey already in `landed_check.py` measured the BOARD on one afternoon (10 non-terminal rows, 0 fires) and the detector's COVERAGE over done tasks; neither answers what the gate would do to the DISPATCH STREAM. Replays every recorded `-> in_progress` scheduler transition against the commit history and asks whether the id was on main AT THAT MOMENT, then splits the fires by what happened next: `correct` (nothing carrying the id ever landed after, so the dispatch rebuilt delivered work) vs `wrong` (a further commit landed, so refusing would have withheld work the repo took). The WRONG bucket is an UPPER BOUND on purpose — a later commit is counted as real work without asking whether it was itself a revert — because a survey should err against arming the gate. Verdict 2026-08-18 over 6,218 dispatches: 9.20% would be refused, 6.53% correctly, 2.67% WRONGLY (29.0% of fires), which is above the 1.63% this repo already calls refusing routine work. Three discriminators were tested and ALL failed — landing age (7-24% wrong in every band), evidence tier (36.4% merge_ref / 25.5% subject), repeat count (flat 27-37% from the 1st re-dispatch to the 11th) — so the gate stays `warn`. Matching reuses `landed_check._classify` and `_grep_pattern`, so the survey cannot drift from the gate; the tokenised fast path is the SAME boundary predicate as the regex, with a fallback for ids the tokeniser would split. UNMEASURABLE (never a clean 0%) on a database with no dispatch history or an unreachable ref. Report only, no `--gate`. Mirrored to icdev/tools/kanban/. | `--json` / `--ref` / `--window-days` | `{measurable, dispatches, fires, fire_rate_pct, correct, wrong, wrong_share_of_fires_pct, top_rebuilt[], wrong_examples[]}` |
| Awaiting Merge — surfaces (panel + board CLI) | tools/dashboard/templates/_autonomy_status.html, tools/kanban/cli.py | kpr-watch-03: the kpr-watch-01 classification WHERE SOMEBODY WILL SEE IT. A report nobody opens is not observability, and for two cards the only place "why is that PR not merging" was answerable was a CLI you had to think to run. Two surfaces, both READ ONLY BY CONSTRUCTION: the "Awaiting Merge" section on Home (a PANEL inside the existing `_autonomy_status.html` partial, NOT a new page, so the 8-point completeness gate does not apply) behind `GET /api/merge-readiness` with no POST sibling and no merge/un-draft control (asserted by AST in `tests/test_merge_readiness_surface.py`), and `python tools/kanban/cli.py --awaiting-merge`. Both read `merge_readiness.collect_report`, the SAME gatherer `python -m tools.ci.merge_readiness` reads — one ladder, one gatherer, three surfaces. Per PR: number, branch, whether a task points at it (`task_id`, joined from `list_pr_tasks` and never allowed to change a verdict), the state, the reason, and the age in state. TWO VERDICTS FROM ONE TABLE: `state` is the merger's, unchanged and authoritative; `pipeline_state` is the same function called with `linked_urls=()`, because `state` short-circuits at the `linked` rung for EVERY kanban PR — the exact population the panel exists for — and grouping on it collapsed the board into one bucket labelled "a task owns it". Identical for an unlinked PR by construction, which is the test that proves it is not a second ladder. AGE IS A LOWER BOUND measured from the NEWEST observable event on the PR, not from `updatedAt`, which does not bump when a check completes (measured 2026-08-19: #1817's `updatedAt` was 45s OLDER than its own check's `completedAt`); `gh`'s Go zero value for an unfinished check is rejected rather than min()-ed away, and an unmeasured age is `None` printing "?", never 0. Groups are in `ATTENTION_ORDER` (`ready`, `behind_main` first) — presentation only, and it never reorders the ladder. The panel hides ONLY on a known-empty board: a failed report stays visible and says why, because a panel that disappears when it breaks is indistinguishable from a clean board. Server-side 120s cache (a `gh pr list` plus one /compare per ready PR) whose AGE is rendered, so a stale answer is never shown as live. Mirrored to icdev/. | `--awaiting-merge`, `--merge-state <s>` (repeatable), `--no-measure-behind`, `--json`; `merge_readiness --group` | `{ok, generated_at, cache_age_seconds, total, ready, counts{}, pipeline_counts{}, groups[{state,count,blocked_on_automation,prs[]}], prs[{number,head,task_id,state,pipeline_state,pipeline_reason,state_age_seconds,state_age_basis}]}`; exit 0=reported / 2=could not run |
| Merge Stall Alarm (eligible-but-unmerged) | tools/ci/merge_stall.py | kpr-watch-02: THE STATE WORTH PAGING ON. `merge_readiness` answers "why is this PR not merging" for every rung the ladder refuses on; it cannot answer the one case where the ladder refuses NOTHING — a PR classified `ready` that is STILL open on the next poll. Nothing is wrong with that PR: the actor should have merged it and did not, which is an automation-liveness problem with a different repair. Eligibility is asked by calling the SAME `classify_merge_readiness` with `linked_urls=()`, so the `linked` short-circuit cannot hide the task path (where 3 of the 4 known causes live) and ownership is carried apart as `door` — there is NO second copy of the ladder. SEVERITIES: `alarm` (eligible, aged out, NOTHING explains it) / `outage` (daemon down, or the forge refused this host's credentials — reported with NO threshold and attributed ONCE to the fleet, never N times to N innocent PRs) / `by_design` (sibling hold, enforced done-gate, landed hold, protected path, auto-merge off, CI-still-running) / `unmeasured` (eligible but nothing knows for how long — never a reassuring zero, and it can never alarm) / `ok`. AGE has two sources that are NEVER merged and are both always reported: `recorded` (from the append-only `pr_merge_eligibility_events`, migration 20260819011454, written per TRANSITION of `(state, head_sha)` so the newest row IS first-seen-ready — one indexed read, no aggregation, a handful of rows a day) and `ci_estimate` (`max(statusCheckRollup[].completedAt)`, a labelled PROXY that reads a PR whose hold was cleared after green as instantly hours old). A recorded row for a DIFFERENT head sha is refused — a force-push is a new merge opportunity whose clock restarts. CAUSE ATTRIBUTION reuses `audit_trail` (104,319 pr_watcher rows, 42,742 `wait` rows carrying the refusal's own reason) — no new writer, no new instrumentation; the existing record simply read. Patterns are DATA in args/merge_stall.yaml and every one was taken from a live row. FAIL-OPEN to `unattributed`: excusing a PR on missing evidence is how an alarm goes quiet. SURVEYED BEFORE ARMING over 150 merged PRs: the entire tail is attributed (n=30, max 116.37 min — 17 done-gate, 12 sibling, 1 forge outage) and the unattributed population (n=120) stops at 13.98 min. So at 20 min an alarm that IGNORES cause fires on 4.67% of routine merges and one that ATTRIBUTES first fires on 0.00%. `stall_after_minutes: 20` (not 15: both are 0.00%, and 15 leaves one minute of headroom above its own sample); `by_design_stall_after_minutes: 180`, because a hold that can never escalate is a category people stop reading. READ-ONLY against the forge — only `gh pr list` and `gh auth status`, proven by AST in tests/test_merge_stall.py — and it writes exactly one table. `pr_watcher.poll_once` records an observation beside its heartbeat: the heartbeat proves the WATCHER ran, this proves what it was looking at. Mirrored to icdev/tools/ci/. | `--json`, `--gate` (exit 1 on `alarm` only), `--survey` / `--survey-limit N`, `--stall-after MIN`, `--no-record`, `--from-json <path>`, `--state`-free (severity is the filter), `--limit`, `--default-branch` | `{generated_at, stall_after_minutes, total, eligible, alarms, outages, by_design, unmeasured, counts{}, causes{}, watcher{}, watcher_alive, forge_auth_ok, merger_enabled{}, recording{}, prs[{number,url,door,state,eligible,severity,cause,detail,age_minutes,ready_since,ready_since_source,ci_green_age_minutes,hold_reason}]}`; exit 0=reported / 1=alarm under --gate / 2=could not run |
| Draft Promotion Survey | tools/ci/draft_promotion_survey.py | kpr-watch-06: measures the `draft -> ready -> merged` round trip after the runner was inverted to open every kanban PR with `gh pr create --draft`. The inversion makes the hold a database row (`pr_watcher._mark_ready` promotes only on green CI + not-a-manual-gate + `tools.kanban.deps.blocking_deps` empty) instead of an external poller racing the 30s cycle — and it makes ONE new failure mode possible that is strictly worse than the old default: a draft nobody promotes is a stalled pipeline, and a stalled pipeline is QUIET. Four quantities kept apart: `opened_per_hour` (the control — a promotion regression does not change it, the runner keeps opening PRs), `promotions` (`pr_watcher.auto_ready` audit rows, written on SUCCESS only so the count is promotions and never polls), `merged`, and `stuck` (open kanban PRs still draft past `--stuck-hours` — THIS is the regression signal, and the one the old default structurally could not produce). A ZERO IS NOT A VERDICT: an unreachable `gh` or a window with no kanban PR reports UNMEASURABLE, and `promotions` is `None` — never 0 — when `audit_trail` cannot be read. Report only, no `--gate` (kpr-fix-03: a survey with a gate earns a `|| true`). Mirrored to icdev/tools/ci/. | `--window-hours`, `--stuck-hours`, `--limit`, `--json` | `{opened, opened_per_hour, merged, promotions, open_now, open_drafts_now, stuck_count, stuck[], unmeasurable}` |
| Status Churn (oscillation) | tools/kanban/status_churn.py | kpr-watch-11: two writers taking turns on one row, and nothing noticing. On 2026-08-19 `cef-ui-03` flipped done<->backlog 95 times in 5.5 hours — pr_watcher completed it because its PR had merged, the scheduler demoted it because its run had run out of budget, and EVERY INDIVIDUAL TRANSITION WAS LEGITIMATE, so no per-move guard could see it while the board read `scheduled` and the scheduler reported `idle`. kpr-dup-09 fixed that mechanism; this detects the SHAPE. A RETURN is a PAIR (`A -> B` then `B -> A`), deliberately not 'the task changed status a lot' — a task progressing backlog->scheduled->in_progress->pr_opened->done changes status five times and produces none. CONTESTED (2+ writers) is separated from single-writer churn and sorted FIRST, because they need different fixes: a fight needs a rule about who owns the row, a retry loop needs a budget. Measured live: 34 tasks oscillate, only 3 are contested, and one of those (prop-vv-02, scheduler vs stale-reaper) nobody knew about. SURVEYED BEFORE THRESHOLDING over 15,879 transitions — >=1 return is routine (11.9% of tasks), >=10 is 1.09%, below the 1.63% CLAUDE.md calls refusing routine work, and still catches the 316-return cases. UNMEASURABLE (never a clean zero) on a board with no transitions in the window. REPORT ONLY, no --gate: it measures the BOARD, not a diff, so failing a commit would block work the committer did not cause. | `--json` / `--window-hours` / `--min-returns` | `{measurable, transitions_scanned, tasks_with_any_return, oscillating, contested, tasks[]}` |
| Dispatch Admission | tools/kanban/dispatch_admission.py | autonomy-adm-01: ONE pre-dispatch gate, asked before a token is spent, keyed on PR IDENTITY rather than "is the id on main" — a task legitimately spans several commits across several PRs, which is why `landed_check` is wrong 29% of the times it fires. #1862 duplicated rem-hyg-14 (already merged as #1858) and, ten commits behind, would have deleted 5,545 lines across 76 files. NARROWED BY MEASUREMENT over all 6,528 recorded scheduler dispatches: "a merged PR exists for this branch" fires 3.80% and wrongly refuses 0.95% of dispatches; adding "and no PR was OPEN at that moment" gives 0.38%; adding "and exactly ONE prior merge" gives 2.99% fire / 0.35% wrong / 88.2% of fires correct. Both carve-outs are the same finding twice — a branch with work already in flight is a legitimately multi-PR task (refusing with a PR open was wrong 80.4% of the time; with two prior merges, 85.7%). DEFAULT `report` even though 0.35% is well under the 1.63% this repo calls refusing routine work: shipping a gate enforcing on day one against a survey written by the same change is the pattern that has bitten twice (kpr-fix-03). `KANBAN_DISPATCH_ADMISSION=enforce` arms it, `=off` disables. FAIL-OPEN — an unreachable forge is `unmeasurable` and never blocks. ONE RULE, TWO RENDERINGS: `--survey` replays history through the SAME `classify()`, so the survey cannot describe a gate that does not exist. | `--task <id>` / `--survey` / `--window-days N` / `--json` | Verdict (allow/refuse/unmeasurable) + reason + prior_merged/open_prs; survey returns fires, right, wrong and rates, `unmeasurable` (never a clean zero) on a board with no dispatches |
| Parent Split Seed | tools/kanban/seed_parent_split.py | xit-reg-01. Seeds the ICDEV[domain] split programme (docs/programmes/icdev-domain-split.md): three streams on three repos -- `xit-*` builds in ICDEV, `xcore-*` in the icdev-core repo, `xft-*` in the private ICDEV[FT] repo, the latter two registered in args/kanban_external_repos.yaml BEFORE the repos exist so they park rather than build here. Every stream is held behind its own `<prefix>gate-00` (parked in_progress, RISK line) and every work task declares `depends_on_task_id` on it. Routes through `task_factory.create_tasks` (never raw INSERT); `--claim <id>` seeds-and-claims for the building session. | python -m tools.kanban.seed_parent_split [--dry-run] [--claim ID]... | {created[], count, claimed[]} |
| Detector Findings Consumer | tools/kanban/detector_findings.py | autonomy-act-02: the consumer for three detectors imported by nobody on any runtime path — status_churn (kpr-watch-11), born_red_survey (rem-hyg-14), recovery_summary (rem-hyg-16). Each was built because a human found the defect by hand and each then waited for a human to run it by hand: declared-but-unconsumed reaching the self-observation layer. BUILDS NO DETECTOR. Runs the three on the Genesis cadence (detector_findings_reflex, 6h, green) and turns each finding into ONE card carrying its evidence — the detector's row verbatim, the exact derivation command, what to do and what 'done' looks like — never a bare alert. DEDUPES ON THE FINDING, not the run: one `detector_findings` row per (detector, subject, fingerprint), upserted with seen_count (the cef-ui-02 projection shape); a card is seeded on first sight and again only on RECURRENCE after its card closed (`-r2`, card_count); idempotency_key on the spec is the second lock inside create_tasks. UNMEASURABLE CLEARS NOTHING — only a measurable run that no longer reports a finding marks it `cleared`; `detector_runs` is the denominator (never_ran / unmeasurable / clean / findings). Per-run cap reported as cards_deferred, never silent. Cards land in `suggested` by default (seed_status). Seeds via task_factory.create_tasks, never raw INSERT. Migration 20260821050135. Mirrored to icdev/tools/kanban/. | `--json` / `--dry-run` / `--list` / `--stats` / `--detector` / `--status` / `--max-cards` / `--seed-status` | `{state, detectors{name: {state, findings, new, recurring, cleared, summary}}, findings_seen, cards_seeded[], cards_deferred, errors[]}` |
| Transition Log Completeness | tools/kanban/transition_log_gap.py | Is `kanban_status_transitions` COMPLETE? It is the primary evidence `status_churn` (kpr-watch-11), `landed_dispatch_survey` (kpr-fix-03) and `merge_stall` (kpr-watch-02) all read, and none of them can tell a move that never happened from a move that happened and was never written down. Measured 2026-08-21: of 3,338 tasks the board calls `done`, 3,024 have at least one transition row and 273 have NO recorded ARRIVAL — their last logged move leaves `done` or leaves them mid-flight and the board says done anyway. REPORTS THE TREND, NOT THE TOTAL: 2026-06 189, 2026-07 74, 2026-08 10 — mostly historical debt on a falling curve, where a bare 273 reads as an ongoing haemorrhage and gets triaged as one. A REFUSED MOVE IS NOT A DEPARTURE: `_move_task` records a blocked transition as `REFUSED_*`, meaning the guard fired and the task STAYED, and counting those as departures is the error that produced the first draft of this measurement (matched by PREFIX so a third guard added later cannot silently start inflating it). A task with NO transition rows at all is excluded — it has no history to contradict the board. NAMES NO CULPRIT: 39 sites UPDATE status outside `_move_task` AND the CLI recorder is best-effort (a failed INSERT is warned, not raised), so a gap can come from a bypassing writer or from a recorder that tried and failed; the gap is the fact. Report only, deliberately no --gate: it measures the BOARD, not a diff. | `--json` / `--window-days N` | `{state, done, with_any_transition, unlogged, by_month{}, sample[]}` ; exit 2 unmeasurable |
| Artifact Evidence (done -> tree) | tools/kanban/artifact_evidence.py | kpr-rvfy-04: the board records what a worker SAID and what a status writer DECIDED; nothing asked the TREE. Measured 2026-08-29: of 18 `ftp-*` tasks, FIVE were `done` by genesis_scheduler with completed_via_bypass=0 while their deliverable was absent from origin/main -- real completion 11 of 18, a board overstating itself by 45%. Read from `kanban_status_transitions` rather than from the cards, all five were completed on a `Verified (git-first)` reason and FOUR of them on `N uncommitted change(s) in worktree` -- not a landing, not even a commit. ftp-ezb-06 was completed on 24 of them while its worker still ran pytest; that session exited without committing and the work is gone (kanban/ftp-ezb-06 carries zero commits, `git fsck` finds no dangling one). The fifth, ftp-prd-07, came through the OTHER arm: `<dispatch baseline>..<branch>` counts every commit MAIN gained while the task ran whenever the branch sits at or below the default branch -- 18 'changed files' on a branch 0 commits ahead. `declared_artifacts` reads the paths a card says it will CREATE (a creation marker within 40 chars is required, so a path merely CITED for context is not counted, and shared registries like tools/manifest.md never are); `artifact_report` asks `git cat-file -e origin/<default>:<path>`, repo-aware through `repo_registry` so an ftp-* deliverable is looked for in ICDEV[FT]. TWO WAYS "not on main" is NOT a finding, both measured as false positives on the first board-wide run (300 done tasks / 14 days, its ONLY two findings and both wrong) and each answered by RE-DERIVING rather than a blocklist: a gitignored path (`git check-ignore`) can never be on a branch and is kept out of BOTH present and missing; a path written relative to a subdirectory resolves by a UNIQUE suffix match against the branch's tracked files, recorded under `resolved_relative`, while two or more matches is a guess and stays missing. After both, the same sweep reads 21 present / 0 findings / 279 unmeasurable. FOUR verdicts and `unmeasurable` is never folded into the others: present | partial | absent | unmeasurable (the card declares no artifact, or the repo/ref could not be read) -- and `artifact_present_pct` is None, never 100.0, over an empty denominator. A TASK ID FOUND IN A FILE IS NOT AN ARTIFACT: the naive survey (grep main for the id) is what produced the wrong answer, since three of the five ids are on main ONLY as forward references naming the card that WILL do the work; that tiering is CONSUMED from `landed_check.CONFIDENCE_FILE_CONTENT`, never re-derived. `delivery_evidence` is the done-gate's primitive -- dispatch record, branch, commits ahead -- returning True|False|None, `False` only when all three were read and all three were negative. REPORT ONLY, deliberately no `--gate`: it measures the BOARD, not a diff (kpr-fix-03). Exit 2 = the survey could not be produced. | `--survey` / `--task <id>` / `--prefix` / `--window-days N` / `--limit` / `--fetch` / `--json` | `{state, done_tasks, measurable_tasks, artifact_present_pct, counts{present,partial,absent,unmeasurable}, findings[], tasks[{declared,present,missing,ignored,resolved_relative}]}` |
| Orphan Requeue | tools/kanban/orphan_requeue.py | Turns `stranded_audit`'s `orphan_validating` findings into a bounded, proven, audited ACT (kpr-stale-05). Measured 2026-09-03: 14 cards sat in `validating` with no branch/worktree/worker after `worktree-isolation-guard` parked them when `git worktree add` timed out; lifetime, every exit from a guard park was a human (manual 41, cli 15) or nothing (9). `prove()` RE-DERIVES the orphan from primary data (row is `validating`; no `kanban/<id>` ref local or origin; no `.tmp/worktrees/<id>`; lease `free`/`litter` — `live`/`working`/unreadable refuse; the latest park's actor is a dispatch guard: worktree-isolation-guard / repo-aware-guard / dispatch-admission). Requeues through `requeue_task` (never a raw UPDATE) with actor `kanban_stranded_reflex` and the guard's own parking reason quoted; `max_requeues_per_run` (default 10, reflex config) caps it and the rest are `deferred` BY NAME, oldest park first. A row parked TWICE by the same guard within 24h is CARDED (`kph-repark-<id>`, suggested, both reasons verbatim) and never requeued a third time. `unmeasurable` with an error when the board cannot be read — never a clean zero. Library; consumed by `kanban_stranded_reflex`. Mirrored to icdev/tools/kanban/. | `act_on_orphans(findings, config, *, get_conn, branch_exists, worktree_exists, lease_state, requeue, file_card, now)`; `prove(task_id, conn, ...)` | `{state: unmeasurable\|clean\|acted, candidates, requeued[], carded[], deferred[], refused[{task_id, reasons[]}], cards[], max_requeues_per_run, requeue_status, error}` |
| Empty-Checkout Requeue | tools/kanban/orphan_requeue.py | The SECOND proof beside the orphan one (kpr-stale-06): a guard-parked `validating` row that is NEITHER orphan (it has a branch) NOR stranded (nothing unmerged) -- the shape `stranded_audit` counted CLEAN and nothing consumed. Measured 2026-09-03: task-det-e9a2e3ea16 sat there with `kanban/<id>` 0 commits ahead of origin/main and a `.tmp/worktrees/<id>` directory; surveyed over all 65 guard parks lifetime, 53 (81.5%) had their branch BEFORE the park (branch reflog), 11 cannot be told (branch deleted since), exits were manual 41 / cli 15 / pr_watcher 5 / none 4 -- never automation. `stranded_audit` now names such rows under `validating_with_branch` (still counted clean). `prove_empty_checkout` RE-DERIVES: guard park; EVERY ref matching the id has 0 `git cherry` commits ahead of origin/<default> AND is an ANCESTOR of it; the worktree is ABSENT or REGISTERED in `git worktree list --porcelain` on the task's own branch with `git status --porcelain` EMPTY and `rev-parse --show-toplevel` equal to the dir. An UNREGISTERED directory REFUSES (kpr-dup-10; and `git status` inside one describes the ENCLOSING checkout -- measured). Then audit INTENT (`worktree_cleaned` / `orphan_requeue.empty_checkout.intent`, raise_on_error=True; no row, no act) -> `git worktree remove` (never rmtree, never --force) -> `git branch -D` LOCAL refs only (an origin branch is never deleted) -> confirm each -> `requeue_task` (actor `kanban_stranded_reflex`) under the SAME `max_requeues_per_run` as the orphan act (`already_requeued`), same twice-parked-in-24h card rule. Mirrored to icdev/tools/kanban/. | `act_on_empty_checkouts(findings, config, *, already_requeued, dry_run, get_conn, ctx, branch_state, worktree_state, lease_state, requeue, file_card, audit, remove_worktree, delete_branch, now)`; `prove_empty_checkout(task_id, conn, ...)`; `probe_branch(task_id, ctx)`; `probe_worktree(task_id, ctx)`; `python -m tools.kanban.orphan_requeue --plan [--json]` (both proofs, acts on nothing) | `{state: unmeasurable\|clean\|acted, candidates, requeued[], acts[{task_id, removed_worktree, deleted_branches[]}], carded[], deferred[], refused[{task_id, reasons[]}], would_act[], cards[], max_requeues_per_run, already_requeued, requeue_status, dry_run, error}` |
| Union Resolver | tools/kanban/union_resolver.py | The union rung for a REAL rebase conflict on a DECLARED append-shaped file (mfx-sib-03). Between 2026-09-03 and 09-04 an operator resolved the sibling-append conflicts of the rmf-ui-* series ten times by hand -- a canvas blueprint gaining one route block per card, the `request.path in [...]` list in base.html gaining one token per card, a coverage table gaining one row, a Playwright spec gaining one test -- and every resolution was the union; twice a WRONG rule (a quoted-list rule on a TypeScript object literal, "keep both blocks" where the hunk boundary cut a test mid-body) shipped a broken file. Rules are chosen BY FILE from `union_resolver.files` in args/pr_watcher_config.yaml (path pattern to an ordered rule list: `keep_both_blocks`, `table_rows`, `quoted_list_line`, `adjacent_edits`; `other_side_when_empty` is universal), never guessed from content; an undeclared unmerged file REFUSES. The merge is a whole-file three-way over the index stages (`:1` base, `:2` the branch rebased onto, `:3` the card's commit), so a side's text is always a contiguous run of a real file and the mid-body cut cannot recur. Verified before anything is staged for push -- `ast.parse` + ruff on .py, typescript diagnostics or a string/comment-aware delimiter balance on .ts (the verdict records which), a Jinja parse on .html, no conflict marker + `git diff --check` on all, and each entry's declared pytest targets ONCE on the completed rebase; any failure refuses and puts the conflict back. Runs inside `rebase_recovery.rebase_and_push` after the doc-only additive resolver declines and before the abort, so it sits under the same per-base-era `max_rebase_attempts_per_task` budget; `pr_watcher._maybe_rebase` writes `pr_watcher.union_resolved` / `.union_refused` naming the rules used. | `--worktree <path>`, `--mode rebase|merge`, `--dry-run`, `--list-rules`, `--json` | `{outcome, files, rules_used, verifiers, tests, reason}` |
| Parked-Card Requeue Reflex | tools/genesis/reflexes/kanban_requeue_reflex.py | The OWNER of a `validating` park, SEPARATED from the whole-board stranded audit (mfx-own-03). kpr-stale-05/06 built the act; it was reachable only as a downstream consumer of `stranded_audit`, which walks EVERY terminal task. MEASURED on the live board 2026-09-05: 3,892 `done` rows against the 2 `validating` rows the act cares about; `kanban_stranded_reflex` recorded durations median 300.0s / max 1200.2s against its own 1200s watchdog, 24 `watchdog_timeout_1200s` failures, and 242 recorded runs carrying `orphan_requeue` ZERO times (5 of them since the act landed 2026-09-04 00:28Z); three consecutive timeouts then opened its circuit breaker, after which tools/daemon/base.py SKIPS the reflex entirely. Of 73 lifetime guard parks across 72 tasks the first exit was a HUMAN 63 times (manual 41, cli 22), `pr_watcher -> done` 5, still parked 5 -- and `kanban_stranded_reflex` EXACTLY ZERO. This reflex reads candidates straight off the board through `orphan_requeue.board_findings()` (one indexed `WHERE status='validating'` query) and runs the SAME two proofs -- it builds no proof of its own, so there is one statement of the rule. 30-minute cadence, own breaker, own 300s budget, shared `max_requeues_per_run`. An unreadable board is `unmeasurable` with `candidates: None` and `success: True` -- never a clean zero, and never a breaker-tripping failure. Registered in BOTH daemon.REFLEX_NAMES and args/genesis_config.yaml. Does NOT raise the guard's 30s worktree timeout and does NOT retry inside the guard (forbidden by the guard's own comment); does not touch `kanban_stranded_reflex`. Survey: docs/audits/mfx-own-03-validating-park-ownership-survey.md. Mirrored to icdev/tools/genesis/reflexes/. | `run(config, state, **seams)`; `python tools/genesis/daemon.py --reflex kanban_requeue_reflex --json` | `{success, metric_value: rows requeued, details: {orphan_requeue, empty_checkout_requeue}}` |
| Interactive Claim Keeper | tools/kanban/interactive_claim.py | mfx-own-02. Makes a claim from a PLAIN SHELL hold. `cli.py --claim` took `kanban:task:<id>` under a pid that exits on the next line and an UNREGISTERED session id, so every reader (dispatch reaper, startup recovery, `--release`, `restore_acts --plan`) read it as litter within seconds -- measured 2026-09-03, an operator holding rmf-ui-13 by hand while a second session repaired the same branch. `claim()` takes the lease synchronously under the caller's identity (the same refusal every claimant gets), then `leases.handover` re-binds it -- no release/acquire window -- to a dedicated `cli-claim-<task>-t<hex>` id and spawns a detached KEEPER (`-m tools.kanban.interactive_claim --keep`) that registers that id in agent_sessions (agent_type `cli`, the stated `--intent`), re-takes the lease under its own live pid and heartbeats every 60s until the TTL (`--ttl`, default 2h; `--claim` again RENEWS), `--release` (state file removed), a lost lease, or a terminal task status. NEVER the inherited service identity (claim-verif-33c9f4cd11): the keeper's env is rewritten before the registry is touched, and the `t<hex>` suffix can never parse as a `<name>-<pid>` service id. A dead keeper is OUR litter and the next `--claim` replaces it. `lease_liveness` is untouched: a kept claim is `live` by pid and, to a reader that cannot see the pid, by registered session. State/log per task under .tmp/coordination/claims/. `task_factory.claim_seeded_tasks` hands a seed's claim to the same keeper when the seeder is not a service (`service_identity._OWNED` empty). Mirrored to icdev/. | `claim(task_id, intent=, ttl_seconds=)` / `release(task_id)` / `keep(task_id)` / `status(task_id)`; CLI `--status <id>`, `--keep` (spawned) | `{claimed, renewed, keeper: running|failed|unconfirmed|none, session_id, pid, expires_at, reason}` |
| Sibling Overlap (dispatch-time serialization) | tools/kanban/sibling_overlap.py | mfx-sib-01: the MERGE door has serialized siblings since `hold_on_sibling_conflict`; DISPATCH did not. Ten `rmf-ui-*` cards -- one route per card, by design -- each appended to the same lines of the same canvas `blueprint.py`, the same nav dropdown and the same feature doc on 2026-09-03/04. Four were built concurrently and three of the four were GUARANTEED to conflict: whichever landed first made every open sibling CONFLICTING, `classify_conflict` read `real`, the `--force-with-lease` rebase aborted four times per card, five LLM resumes burned, then `pr_watcher.escalate` and a human unioned the hunks by hand -- ten times, ~6 hours. Holding at the merge door is the expensive place to find out, because by then the work is written. Refuses to dispatch a task while a sibling IN ITS OWN EPIC is `in_progress`/`pr_opened` and the two cards' DECLARED artifact paths overlap on a NON-ADDITIVE file. A HOLD IS A WAIT, NOT A PARK: the card stays `scheduled`, yields its selection slot (filtered BEFORE the slot truncation, the same reason `_drop_respawn_guarded` is), and is re-evaluated next cycle -- when the sibling reaches `done` the hold evaporates with no further action. The reason is recorded ONCE PER EPISODE on a `scheduled -> scheduled` row (actor `sibling-serializer`), never once per 60s cycle, and the per-cycle count is reported as `sibling_holds`. NEVER A SECOND COPY OF EITHER INPUT: declared paths come from `artifact_evidence.declared_artifacts`, and "safe to co-edit" from `coordination_paths.is_coordination_path`, which IS `pr_watcher._is_additive_path` -- so the dispatch door and the merge door cannot disagree about what a collision is. The predicate the admission calls and the predicate the survey replays are the SAME function. `scheduled` is deliberately NOT an in-flight status: two waiting cards holding each other is a deadlock, not serialization. SURVEYED BEFORE ARMING over 1,977 recorded dispatches (30 days to 2026-09-04): 451 had an in-flight same-epic sibling, 21 would have been HELD (1.06%, below the 1.63% CLAUDE.md calls refusing routine work and under the card's 2% ceiling), and 18 of the 21 (85.71%) went on to record a real merge conflict. It fires on exactly the ten rmf-ui cards. The cost is three NAMED holds -- cef-bck-01, rmf-ui-14, rmf-ui-15, 0.15% of dispatches -- delayed one cycle for a collision that would not have happened. `declared_artifacts` reads PROSE and under-approximates, so a card naming no path is never held: the honest failure direction, since an unheld pair costs a rebase while a held one costs throughput. UNMEASURABLE, never a clean zero, on a window with no dispatches. Toggle `serialize_overlapping_siblings` in args/genesis_config.yaml (default true) or `KANBAN_SERIALIZE_SIBLINGS=0`; fail-OPEN on every unknown, because a missed hold costs one rebase and a wedged scheduler costs the queue. Mirrored to icdev/tools/kanban/. | `--survey`, `--holds`, `--window-days N`, `--json`; library -- `find_holds(tasks, conn=)`, `overlap(a_title, a_desc, b_title, b_desc)`, `epic_of(id)`, `survey(window_days)` | `find_holds` -> `{task_id: Hold(sibling_id, sibling_status, epic, shared_paths, reason)}`; `survey` -> `{status, dispatches, with_in_flight_sibling, held, fire_rate_pct, held_that_conflicted, conflict_precision_pct, holds[]}` |
| Protected-Path Conflict Hold + Divert Survey | tools/ci/protected_conflict_survey.py | mfx-mrg-03: `pr_watcher` asked the protected-path question in ONE place on the task-linked path -- `_refuse_protected`, inside the MERGEABLE arm immediately before the un-draft and `_auto_merge` -- while `_maybe_rebase` and the resume ladder live in the `MERGE_CONFLICT` arm. Not "later in one ladder": a DIFFERENT BRANCH of it, so a PR conflicting from the moment it opens could never reach the rung that would refuse it, and the rung only fired once the PR was mergeable -- exactly when it is no longer needed to prevent wasted work. MEASURED on #2064 (mfx-mrg-01), which changed `tools/ci/pr_watcher.py`, the first entry in `protected_paths`: 63 `rebase_failed`, 5 `resume`, an `escalate`, and **0 of its 165 `pr_watcher.*` audit rows mention `protected`** -- the ledger, the panel and the escalation all named something else. The question is now asked and audited BEFORE any `_maybe_rebase` call and the PR is held before the resume ladder; the mergeable arm's refusal moved back ahead of the un-draft, where kpr-watch-05 said it belonged before a later fix moved the un-draft up and silently overtook it. WHAT IT DOES NOT SUPPRESS IS THE SURVEY'S FINDING, not a preference: this tool replays all 210 recorded conflict-ladder episodes through the SHIPPED predicate (`merge_readiness.protected_hits`, never a second copy) and the refusal catches 32 (15.24%) -- but holding one rung earlier, ahead of `_maybe_rebase`, would have taken a SUCCESSFUL rebase from 11 of them, 8 a single pushed rebase and nothing else before the PR merged. That is 3.81% of the population, above the 1.63% CLAUDE.md calls refusing routine work, so the bounded rebase STAYS and the budget is untouched: 0 false positives, 74 resumes and 21 escalated episodes saved. `unmeasurable` (the forge would not return a file list) is never folded into either side -- the production predicate is fail-closed, and reading an unreadable PR as clean would understate the very control being measured. REST, never GraphQL: the outage behind this card refused every `gh pr view` while `gh api repos/.../pulls/N` answered. Report only, deliberately no `--gate` (kpr-fix-03); exit 2 = the survey could not be produced, which is never a clean survey. Mirrored to icdev/tools/ci/. | `--json`, `--window-days N`, `--limit N`, `--no-forge`, `--facts-cache <path>` | `{state, protected_paths[], episodes_total, measured, unmeasurable, diverted, divert_rate_pct, spent_on_diverted{}, shapes{before_rebase{},shipped{}}, diverted_episodes[], sole_action_rebase_episodes[], unmeasurable_episodes[]}`; exit 0=reported / 2=could not run |
| Resume Delivery Evidence | tools/ci/resume_delivery.py | kpr-watch-13: does a `pr_watcher.resume` mean an agent was told, or only that a line was written? `_send_resume` appends one JSONL line to `.tmp/kanban/messages/<task>.jsonl` and the audit row said `injected resume context` -- a sentence about a file write phrased as a sentence about an agent. MEASURED 2026-09-06 02:59 UTC: **852 undrained `sender: pr_watcher` messages on disk against 850 lifetime `pr_watcher.resume` rows**. A drain DELETES the file, so a drain is traceless and cannot be counted -- but a count on disk that meets or exceeds every resume ever recorded proves none was drained, and the escalation that follows was declaring five ATTEMPTS THAT WERE NEVER MADE. THREE VERDICTS, re-derived from the FILESYSTEM and sharing no code with `queue_message` (whose success only ever meant `write()` did not raise): `undelivered` (a pr_watcher line is STILL in the queue -- proven unread; 850/850 of the lifetime corpus, 100.00%), `delivered` (a drain receipt records it -- `check_message_queue` now appends one line to `.tmp/kanban/message_receipts/<task>.jsonl` naming what/who/when, so a drain stops being traceless), `unmeasured` (the first injection for a task; or an empty queue with NO receipt -- a `.tmp` sweep, a pre-receipt reader and a worktree's own empty `.tmp` are indistinguishable, and reading that as `delivered` would be this card's own defect). `never_drained` is None, NEVER False, on a shortfall alone. NO ACTUATOR, pinned by AST: it never dispatches, never deletes, and never reads `max_resume_cycles_per_task` / `RESUME_COOLDOWN_SECONDS` -- more undelivered messages is not more attempts. The probe runs BEFORE the append (source-order test) or a message corroborates itself. Cost 0.112ms/probe, 0.367ms/receipt. Report only, no `--gate` (kpr-fix-03); exit 2 = the survey could not be produced, never a clean survey. Survey: docs/audits/kpr-watch-13-resume-delivery-survey.md. Mirrored to icdev/tools/ci/. | `--survey`, `--task <id>`, `--sender`, `--top N`, `--json`; library -- `probe_prior_delivery`, `summarize_delivery`, `escalation_note`, `record_drain`, `survey` | `Survey{queue_files, files_with_pending, pending_messages, receipt_files, receipted_messages, recorded_resumes, never_drained, state, notes[], worst[]}` / `DeliveryVerdict{verdict, detail, pending, receipted}` |
| Test-Fixture Predicate (undispatchable board rows) | tools/kanban/fixtures.py | task-444e9c3f6c: the ONE predicate answering "is this board row an E2E spec's throwaway fixture". `tests/e2e/kanban_pipeline.spec.ts` and `kanban_api.spec.ts` POST a task to the REAL board on purpose (a pipeline proof against a throwaway table proves nothing about the pipeline) and delete it in `afterAll`. That cleanup WORKS and is still not enough: the row is visible to the scheduler for as long as the spec runs, and the scheduler polls faster than the spec finishes. MEASURED from `kanban_status_transitions` 2026-09-06 -- `task-444e9c3f6c` was POSTed at 11:59:22 and was already `scheduled -> in_progress` at 11:59:24, with "dispatched: agent subprocess launched" at 11:59:38; a ~2 SECOND window. Its sibling `task-e69f3b0e42` was dispatched 12s earlier in the same run and the scheduler recorded "No git commits found on task branch - agent produced no committed file-level output" -- a whole worker session spent on a row that no longer existed; `task-7054abe88d` (2026-08-28) was parked `token_exhausted, retry 2/60` against a deleted row. Lifetime: 4 of 619 distinct dispatched task ids (0.65%) went to a card that no longer exists, 2 of them from that single run. The SPEC cannot close this -- only the board can, the same way it already declines to dispatch a manual gate. SURVEYED BEFORE ARMING: over all 3,950 lifetime board rows ZERO carry the `[E2E ` prefix, so it refuses nothing that has ever been real work (`SELECT COUNT(*) FROM kanban_tasks WHERE title LIKE '[E2E %'`). Recognition is a TITLE PREFIX, never a substring -- real cards mention e2e and Playwright constantly (`[AUTO-RUN] Playwright E2E Suite`, `Survey whether E2E (Playwright) can become a required check`) and every one must stay dispatchable. Keyed on the title, not the id, because the id is server-generated and opaque (`task-<hex>`). An absent title is NOT a fixture: freezing a card we cannot identify is worse than dispatching one we can. TWO DOORS, the same belt-and-braces `gates.is_manual_gate` gets -- `promote_backlog_to_scheduled` (the observed path) and `genesis/reflexes/kanban::_get_due_tasks` (the backstop, for a row that reaches `scheduled` by a dashboard move or decay-promotion). Import-free like `tools/kanban/gates.py`, so a CLI, scheduler, reflex or Flask route can all import it. Mirrored to icdev/tools/kanban/. | library -- `is_test_fixture(task_id, title)`, `FIXTURE_TITLE_PREFIX` | bool |
| Worktree Husk Sweep | tools/kanban/worktree_husks.py | A directory at `.tmp/worktrees/<task-id>` with NO `.git` marker and NO registration is a HUSK, its own class on a clock of HOURS, not the 7-day KANBAN_WORKTREE_STALE_AGE_DAYS one (mfx-own-04). A live worktree ALWAYS carries `.git` (`git worktree add` writes it first), so a husk cannot be one -- and re-reading the sweeper, it was never a candidate at all (`_sweep_candidates` returns only `.git` carriers) while `_worktree_is_disposable` refuses "entries but no .git" by design and the empty-checkout requeue proof refuses `worktree_unregistered` on the same directory: task-det-e9a2e3ea16 sat in `validating` behind 534 MB of exactly that, invisible to every cleanup path, while `kanban_requeue_reflex` reported 60/60 successes. The class KEEPS the in_progress guard, REQUIRES a board row (`data`/`tools` under .tmp/worktrees are residue of a root-computing bug and are refused), checks absence from a SUCCESSFUL `git worktree list`, and walks the WHOLE tree for its newest mtime (bounded; over budget is `age_unmeasurable`, never old). It is NEVER widened to a directory carrying `.git`, by AST test. prove -> audit (`worktree_cleaned` / `husk_sweep.remove.intent`, raise_on_error=True; no row, no act) -> ONE rmtree (read-only bits cleared, Windows long paths) -> confirm. Bounded per run OLDEST FIRST, deferred BY NAME. Surveyed 2026-09-06: 26 husks, 6.0 GB, 25 done + 1 validating, 0 in_progress (docs/audits/mfx-own-04-worktree-husk-survey.md). Kill switch KANBAN_WORKTREE_HUSK_SWEEP=0. Consumed by `_sweep_old_worktrees`. Mirrored to icdev/tools/kanban/. | `python -m tools.kanban.worktree_husks --survey [--json]`; `--plan`; `--apply <task-id> [--dry-run]`; `sweep_husks(base, repo_root_path, ...)`; `classify(path, base, listing, statuses)`; `survey()` | `{state: unmeasurable\|clean\|findings, in_scope[{task_id, proven, reasons[], task_status, age_hours, size_bytes}], out_of_scope[], roots[]}`; sweep `{state: unmeasurable\|disabled\|clean\|acted, applied[], unconfirmed[], deferred[], refused[], unmeasurable[]}` |
