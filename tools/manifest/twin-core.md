# Twin Core — Cross-Canvas Digital-Twin Unification (TWX)

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.
>
> `tools/twin_core/` is an **additive** layer that unifies the eight working canvas digital twins (NDC, PDC, BDC, SDC, DDC, ODC, IDC, Mission) behind one registry and one canonical verdict/violation schema — **without rewriting any of them**. Adapters translate each canvas's native twin output into the canonical schema, preserving `method`/provenance. Sequoia-Combine Pattern 4. Card: TWX (twx-core-01 = registry + schema + NDC/PDC reference adapters).

## Twin Core
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Twin Canonical Schema | tools/twin_core/schema.py | Canonical verdict (`pass\|warn\|fail`, + `unknown` sentinel), severity (`blocker\|critical\|high\|medium\|low`), category (`service_parity\|iam\|network\|compliance\|cost\|security`) and `target_csp` enums. Normalizes the 3 verdict families (pass/warn/fail, green/amber/red, pass/fail gates) and severity families (bare `high`, STIG `CAT1-3`, cATO `high/moderate/low`) without loss. Never fabricates a verdict; unknown → `unknown`. | `normalize_verdict/severity/csp`, `canonical_violation(...)`, `worst_verdict`, `twin_verdict(...)` | canonical violation dicts / envelope |
| Twin Registry | tools/twin_core/registry.py | Data-driven registry of thin per-canvas `TwinAdapter`s. Adapters self-register via `@register_twin`; `TwinRegistry.discover()` imports every module in `adapters/` by filesystem scan (no hardcoded canvas list). Display names cross-checked against `args/component_registry.yaml`. Base adapter degrades gracefully (canvases lacking `list_snapshots` return `[]`). | `TwinRegistry.get(key)/discover()/describe_all()` | `TwinAdapter` instances |
| NDC Twin Adapter | tools/twin_core/adapters/ndc.py | Wraps `tools/network/twin.py`. `method='heuristic'`. Maps `compliance_findings` → `network` violations; reads `network_twin_snapshots` for the list surface NDC's native module lacks. | `simulate_delta(project_id, topology_delta)` | canonical envelope |
| PDC Twin Adapter | tools/twin_core/adapters/pdc.py | Wraps `tools/pipeline/twin.py`. `method='static-analysis'`. Antipatterns → `security`, compliance-rule failures → `compliance`. Passes PDC's sha256 snapshot dedup + retention (pdx-perf-01) straight through. `latest_status` surfaces the newest persisted `pdc_simulations` verdict. | `simulate_delta(pipeline_id, delta_graph)` | canonical envelope |
| BDC Twin Adapter | tools/twin_core/adapters/bdc.py | Wraps `tools/boundary_canvas/twin.py`. Normalizes rating bands (`green/amber/red/unknown` → `pass/warn/fail/unknown`); `unknown` never green-washed. Carries `cod_method` (`heuristic`/`llm_debate`) through as violation `method`. `compliance` category. | `simulate_delta(project_id, control_delta)` | canonical envelope |
| SDC Twin Adapter | tools/twin_core/adapters/sdc.py | Wraps `tools/security_canvas/twin.py`. Attack paths → `security` violations (severity from path). `method='heuristic'`. | `simulate_delta(design_id, delta_graph)` | canonical envelope |
| DDC Twin Adapter | tools/twin_core/adapters/ddc.py | Wraps `tools/data_canvas/twin.py`. Verdict from `simulate_delta`, lineage-grounded violations from `quality_gate` → `compliance`. `method='lineage-analysis'`. | `simulate_delta(design_id, schema_changes)` | canonical envelope |
| ODC Twin Adapter | tools/twin_core/adapters/odc.py | Wraps `tools/observability_canvas/twin.py`. Coverage gaps → `security` violations; preserves `estimate`/`basis` provenance. `method='heuristic-estimate'`. | `simulate_delta(design_id, collector_delta)` | canonical envelope |
| IDC Twin Adapter | tools/twin_core/adapters/idc.py | Wraps the flat-file IDC IaC twin (`snapshot_writer.py` + `preapply_gate.py` — NOT a `twin/` package; see `idc-twin.md`). `simulate_delta` runs the pre-apply gate over Terraform `plan_json`, normalizing STIG `CAT1-3` → canonical severity. `take_snapshot` requires `graph=` (honest error otherwise). `method='iqe-gate'`. | `simulate_delta(project_id, plan_json)` | canonical envelope |
| Mission Twin Adapter | tools/twin_core/adapters/mission.py | Wraps `tools/mission_canvas/twin.py` (aggregator over DDC+PDC sub-twins). No native snapshot table. `method='aggregate'`. | `simulate_delta(mission_id, schema_changes)` | canonical envelope |
| QDC Twin (NEW) + Adapter | tools/qdc_canvas/twin.py, tools/twin_core/adapters/qdc.py | **New minimal twin (twx-cov-01).** Snapshots the quality-gate topology (`qdc_designs.graph_json`) with PDC-style sha256 dedup + bounded auto-snapshot retention (NOT append-only). `simulate_delta` verdict reuses the `qdc_gate_breach` reflex read (latest result per gate, failing = status NOT IN pass/skip) + latest UQS; removing a quality-gate node on a failing base = fail. Tables `qdc_twin_snapshots`/`qdc_simulations` (canvas init_db.py). Adapter `method='gate-analysis'`. IQE `qdc.twin_snapshots`; NL delta `twin_chat.quality_chat_to_delta`. | `take_snapshot/list_snapshots/simulate_delta(design_id, delta_graph)` | canonical envelope |
| AIML Twin (NEW) + Adapter | tools/aiml_canvas/twin.py, tools/twin_core/adapters/aimc.py | **Wave-2 twin (twx-cov-02).** Snapshots the AI/ML architecture graph (`aiml_designs.graph_json`) with PDC dedup/retention. `simulate_delta` reuses the canvas's real AI-governance assessments (`aiml_assessments`, latest per framework, failing = passed=0); removing an architecture node on a failing base = fail. Tables `aiml_twin_snapshots`/`aiml_simulations`. Registry key `aimc`, `method='assessment-analysis'`. IQE `aimc.twin_snapshots`; NL delta `twin_chat.aiml_chat_to_delta`. | `simulate_delta(design_id, delta_graph)` | canonical envelope |
| Twin Freshness Sweep (reflex) | tools/genesis/reflexes/twin_freshness_sweep.py | **twx-cov-02 gap-fill.** Single cross-canvas Genesis reflex (6h): runs the twin_core observer and publishes `twin.snapshot.stale` for any registered twin with a stale/absent newest snapshot. Fills the residual refresh gap (Mission, AIML) generically — future twins covered automatically. Registered in daemon REFLEX_NAMES + reflex_registry + genesis_config. Read-only, green tier. | `run(ctx)` / genesis daemon | stale-twin events |
| AADC Twin (NEW) + Adapter | tools/agentic_ai_canvas/twin.py, tools/twin_core/adapters/aadc.py | **New minimal twin (twx-cov-01).** Snapshots the agent topology (A2A mesh/roles/authority graph in `aadc_designs.graph_json`) with PDC dedup/retention. `simulate_delta` propagates an agent-failure cascade along directed edges, weighting the verdict by `safety_impacting`/`rights_impacting` flags. Tables `aadc_twin_snapshots`/`aadc_simulations`. Adapter `method='cascade-analysis'`. IQE `aadc.twin_snapshots`; NL delta `twin_chat.agentic_chat_to_delta`. | `take_snapshot/list_snapshots/simulate_delta(design_id, {"fail_nodes":[...]})` | canonical envelope |
| Twin Air-Gap Rules | tools/twin_core/airgap_rules.py | **twx-fed-01 (Sequoia/SHIFT Pattern 2).** Config-driven air-gap validation (`args/twin_airgap_rules.yaml`): no public egress, no external API deps, packages from internal mirrors only, images from internal registry only. `evaluate_airgap(target)` harvests hosts/images/markers from any design graph or IaC plan and emits `severity=deployment_blocker` (→ canonical `blocker`) violations. Deny-by-match (not allow-by-absence) with an internal allowlist. Wired into the NDC/PDC/IDC adapters (opt-in `airgap=True` or auto when `is_airgap_environment()`); any blocker escalates the verdict to `fail`. | `evaluate_airgap(target, source_canvas=...)` | canonical deployment-blocker violations |
| Twin Target Presets | tools/twin_core/target_presets.py | **twx-fed-02 (Sequoia Patterns 1+5).** Run a twin sim *against a target environment*. `args/twin_target_presets.yaml` presets (GovCloud E/W, IL5, air-gapped high-side, Azure Gov) bundle CSP+region+classification + compliance_framework + network_constraints. `evaluate_target(design, preset)` flags referenced services **not available in the target** (`service_parity` deployment-blocker, from the existing `context/cloud/csp_service_registry.json`), engages fed-01 air-gap rules for air-gapped presets, and warns when a preset/catalog is >180d stale (`reviewed_at`). PUBLIC-DATA-ONLY; customer catalogs via `ICDEV_CSP_CATALOG_PATH`. Wired into the IDC adapter (`target_preset=` kwarg) — unavailable service escalates verdict to `fail`. | `evaluate_target(target, preset)` / `list_presets()` | canonical service_parity/blocker violations |
| Twin Event Bridge | tools/twin_core/event_bridge.py | Wires twin_core into the cross-canvas event bus (`tools/canvas/event_bus.py`). Registry facades `simulate()`/`snapshot()` auto-publish `twin_simulation_completed`/`twin_snapshot_taken` with the canonical verdict payload (no change to the 8 twins). Two cross-canvas subscriptions: PDC `pipeline_deployed`→SDC attack-path refresh; SDC `sdc_threat_model_changed`→BDC crosswalk-drift re-run. Honors the bus's classification-aware security-context propagation. `recent_twin_events()` feeds the Observatory (twx-obs-01); `create_twin_drift_card()` writes a **quarantined** (`status='suggested'`) kanban card only, gated off by default (`ICDEV_TWIN_DRIFT_CARDS`). Registered at startup from `security_canvas/blueprint.py`. | `register_subscriptions()`, `simulate/snapshot(canvas, id, ...)`, `recent_twin_events(limit)` | event ids / envelopes / feed |
| Twin Observatory (page) | tools/twin_observatory/ (blueprint + observatory.py + constants.py), tools/dashboard/templates/twin_observatory/index.html | **twx-obs-01.** New dashboard page `/twin-observatory` — per-twin health grid (verdict, snapshot age, violations, refresh status, click-through) + cross-canvas `twin_*` drift event stream, over the `observe()` + `recent_twin_events()` surfaces. Read-only, no new tables. Registry-driven (`args/component_registry.yaml` key `twin_observatory`) — auto-wires IQE dispatch/nav/PATH_CANVAS (explicit path entry added above ndc's `^/twin`). Full 8-component gate incl. IQE (`tools/iqe/adapters/twin_observatory.py`, `POST /twin-observatory/api/iqe-query`, widget, 3 seed queries). | `/twin-observatory/` | HTML page + JSON `/api/data` |
| Twin Compatibility Report + ATO Acceleration | tools/twin_core/compat_report.py | **twx-fed-03 (SHIFT Patterns 7+8).** High-side/target-environment compatibility report for an IDC twin/deployment. `generate_compatibility_report(design, preset)` composes fed-02 `evaluate_target` (which engages the fed-01 air-gap rules) into an executive verdict + per-resource pass/warn/fail + required IAM/network changes + dependency replacements + ATO control-coverage/evidence checklist, under a `classification_manager` banner (never hardcoded). `map_iac_to_controls` maps IaC resource TYPES → NIST 800-53 controls (`args/iac_control_map.yaml`) with an inline `[source: <resource>]` TRUST citation per statement, and raises POA&M gap items (baseline via the existing crosswalk engine). `render_compatibility_report` emits an HTML artifact via `tools/viz`. `feed_cato_evidence(report, project_id)` is the ATO-acceleration wiring: upserts the IaC-grounded control statements into `project_controls` + POA&M gaps into `poam_items` — the tables `ssp_generator`/`poam_generator`/BDC cATO already read — so cATO picks up IDC-twin evidence WITHOUT re-implementing OSCAL/SSP/POA&M. `persist_report` snapshots to `twin_compat_reports` (migration 288; sha256 content-hash dedup + bounded retention per target — PDC pattern, NOT append-only; tenant_id+classification). Integration only, deterministic, no LLM. | `generate_compatibility_report(design, preset)` / `map_iac_to_controls` / `render_compatibility_report` / `persist_report` / `feed_cato_evidence(report, project_id)` / `python -m tools.twin_core.compat_report --design d.json --preset aws_high_side_airgap --json` | report dict / HTML / DB rows |
| Twin Observer | tools/twin_core/observer.py | Cross-canvas twin health: snapshot freshness/age, verdict distribution (window, from canvases that persist sims), violation counts by severity (from canvases that persist violations), and refresh-schedule adherence joined against `genesis_reflex_state`. Best-effort — missing canvas DB/table degrades to `unavailable`/`None`. Library + CLI. Data source for the Twin Observatory (twx-obs-01). | `observe(window_hours=24)` / `python -m tools.twin_core.observer --json` | health report dict |

Event-bus wiring lands in **twx-bus-01**; the Twin Observatory dashboard in **twx-obs-01**. Mirrored to `icdev/tools/twin_core/`.

**Adapter honesty invariants preserved:** the canonical schema *wraps, never obscures*, each twin's `method` provenance — BDC heuristic-vs-llm_debate labeling and unknown-verdict honesty, PDC dedup/retention, ODC `estimate=True` basis, DDC lineage-grounding, IDC STIG-CAT scale. No verdict is ever fabricated.
