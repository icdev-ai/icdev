# Genesis (Additional)

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## Genesis (Additional)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Goal Template Generator | tools/genesis/goal_template_generator.py | Generate goal templates from GKP artifacts | --json | Goal templates |
| Goal Learner | tools/genesis/goal_learner.py | Detect novel problem-solving not covered by existing goals, auto-generate FORGE goal files with version history and quality scoring | --scan --json | Generated goal markdown files + DB records |
| Synthesize Reflex | tools/genesis/reflexes/synthesize.py | Synthesize reflex: tool-chain pattern detection | --json | Pattern results |
| Daily Briefing Reflex | tools/genesis/reflexes/daily_briefing_reflex.py | rri. Hourly: for users whose briefing_time hour matches now, generate + deliver a briefing via tools/second_brain/briefing.py. Was REGISTERED in daemon.REFLEX_NAMES (#64) but its module never existed — the daemon silently skipped it; the rri card wired it over the existing engine. Degrades to skipped when second_brain is absent | `run(args, ctx)` called by Genesis daemon | {users_processed, delivered, errors, hour_utc} |
| cATO Monitor Reflex | tools/genesis/reflexes/cato_monitor.py | 6-hour continuous compliance monitoring reflex — discovers *.iqe queries under context/iqe/queries/compliance/, executes them via IQE Executor, and triggers POAM generation for new violations; scanner-tier, air-gap safe | IQE query files (auto-discovered) | Compliance violations + triggered POAM records |
| Canvas Indexer Reflex | tools/genesis/reflexes/canvas_indexer.py | Genesis daemon reflex that indexes 5 canvases (PDC/BDC/DDC/ODC/IDC) from SQLite sidecars into kg_nodes/kg_edges every 6 hours; exports success metrics as JSON | `run(config, trust)` called by Genesis daemon | JSON metrics dict {indexed_count, duration_ms} |
| Oracle Triage Reflex | tools/genesis/reflexes/oracle_triage.py | Deterministically promote/dismiss Oracle-suggested kanban tasks using file-exists (tool_not_in_manifest), Flask-route grep (route_not_listed), and migration-check (orphan_db_table) verifiers; no-lens heuristics for RCA/V&V/FR cards; runs every 3h after awareness cycle | `--run [--dry-run] [--json]` / `run(config, trust)` | JSON summary {promoted, dismissed, skipped, errors} |
| AADC Reflex | tools/genesis/reflexes/aadc_reflex.py | Genesis daemon reflex that scores the latest AADC design against 5 compliance frameworks (NIST AI RMF, OWASP LLM Top 10, OMB M-25-21, MITRE ATLAS, autonomy level), writes an assessment to aadc_assessments, emits a memory insight on regression, and creates a Kanban suggestion when gaps are found | `run(payload, ctx)` called by Genesis daemon | JSON result {design_id, scored, gaps, kanban_task_id} |
| Freshness Guardian Reflex | tools/genesis/reflexes/freshness_guardian.py | Hourly reflex (`FreshnessGuardianReflex`, `CADENCE_HOURS=1`) — evaluates all enabled `freshness` quality rules across all data designs; writes alert rows to `dd_freshness_alerts` (INSERT OR REPLACE per rule), appends run rows to `dd_quality_runs` (with `reflex_run` tag), and logs breach events to `genesis_audit`; skips rules with no stored `db_conn_json` | `run(context, db_conn)` called by Genesis daemon; `--dry-run --json` CLI | JSON `{checked, passed, failed, breaches}` |
| AIMC Orphan Refs Reflex | tools/genesis/reflexes/aimc_orphan_refs.py | 4h Genesis reflex — scans `aiml_nodes` for foundation-model nodes whose `model_id` is not present in the `FOUNDATION_MODELS` catalog constant; reports orphans and creates a Kanban chore suggestion when any are found (skipped in dry_run mode) | `run(payload, ctx)` called by Genesis daemon; `dry_run=True` in payload for read-only scan | JSON `{status, orphans_found, orphans, suggestion_id, catalog_size}` |
| DAT Refresh Reflex | tools/genesis/reflexes/dat_refresh.py | 6-hour reflex that recomputes the Diplomatic Tension Index (DTI) snapshots for all registered theaters plus the global aggregate; cooldown prevents double-firing within same cadence window | `run(config, trust)` called by Genesis daemon | JSON `{refreshed, skipped, duration_ms}` |
| Slides Reflex | tools/genesis/reflexes/slides.py | Weekly Friday 17:00 reflex that generates an ICDEV™ status PPTX deck via DeckEngine; emits a row to `genesis_outputs` (migration 188) on success for dashboard notification | `run(config, trust)` called by Genesis daemon | JSON `{status, deck_id, slide_count, pptx_path, generated_at}` |
| Retention Sweep Reflex | tools/genesis/reflexes/retention_sweep.py | 24h config-driven retention/archival framework (crx-db-03). Reads `args/retention_policies.yaml` mapping each high-churn operational table → `{keep_days, strategy: prune|archive, timestamp_column, id_column, archive_dest}`. Non-append-only tables (e.g. `agent_execution_traces`, `kanban_verifications`, `a2a_task_history`) are pruned past `keep_days` under a per-run `max_rows_per_table` cap; **append-only/audit tables are archive-to-cold ONLY (never pruned)** — enforced by parsing `APPEND_ONLY_TABLES` from `.claude/hooks/pre_tool_use.py` at runtime and FAILING CLOSED (archive-only) if unreadable. RAG/observability/PDC self-managed retention is excluded. `dry_run` defaults TRUE; every action logged to the append-only `retention_action_log`. | `run(ctx, conn)` called by Genesis daemon; `ctx={'dry_run': False}` to execute; `apply_retention()` is the pure/testable engine | JSON `{status, dry_run, total_affected, tables{}, metric_value}` |

| Calibration Report | tools/genesis/harness/calibration_report.py | Reports whether confidence predicts correctness, **per rule** — the actionable cut. Confidence in the awareness lens is not computed per prediction; it is a per-rule constant from `awareness_config.yaml`, so a band is a set union of unrelated rules (the live 0.9 band is 19 rows of `tool_not_in_manifest` + 1 of `route_not_listed`). Reads evidence where it actually is: the ORGANIC join `oracle_predictions.outcome ~ 'promoted:task-%'` → `kanban_verifications.result` (**biased** — only what the system chose to promote) and SAMPLED `harness_eval` rows under `sampled:*` (**unbiased**, from `confidence_sampler`). The two are never pooled. Renders `UNMEASURED` below `MIN_CALIBRATION_SAMPLES` rather than a point estimate, tracks `bypassed` as non-evidence (never a failure), and flags rules whose constant was edited (mixed `claimed_values`). Report-only — never moves a threshold. States when `harness_eval` is empty, since `check_gates` reads it and an empty table renders as a green gate. | `--json`; `build_report(conn)` / `render(report)` | JSON `{harness_eval:{total,labelled}, organic:{provenance,joined,rules[]}, sampled:{…}, min_samples}` |
| Code Reload | tools/genesis/code_reload.py | Lets a long-lived daemon pick up its own merged code changes without a human restart. Watches the mtimes of every already-imported in-repo module (via `sys.modules`, so the watch set is exactly the code the process actually loaded) and `os.execv`s the process in place when any of them changes — re-exec rather than exit, because supervision on this deployment is uneven and a daemon that exits under an unwatching launcher is worse than one running stale. | (library, called from daemon loops) | Re-exec on change; no-op otherwise |
| Finding Scope | tools/genesis/finding_scope.py | The discriminator for reflex card filing (#ctx-perf-07): does ONE fix close all of these, or N? A finding whose remedy is one CODE change files ONE card with every affected row carried as evidence; a DATA finding still files one card per row. Decides by declaration first (`Finding.declared_scope`, then `args/finding_scope.yaml` per source/category — the operator's lever, no code deploy) and only then by inference, opt-in per source with `infer_code_scope`: a check that fired on 100% of the rows it examined with a byte-identical remedy is describing itself, not the population. Card identity is a DETERMINISTIC key and never a title (title dedup DROPS distinct findings — PR #1504); the code-level key contains no subject, so a shrinking population is still one card. Aggregating is not deduping — every input finding is carried on some output card. Measured instance: cpmp_monitor pass 3 filed 7 [SUBCON] ISR/SSR cards for one missing FAR 19.702(a) applicability gate, and four sessions fixed the same bug (#1628 landed; #1629/#1633/#1635 closed redundant). | (library; `Finding`, `CardSpec`, `group(source, findings, population)`, `classify(...)`, `evidence_block(...)`, `load_config()`) | `list[CardSpec]` — one per code-level category, one per data-level row |
| Canvas Reassessment Reflex | tools/genesis/reflexes/canvas_reassess.py | rem-hyg-11. 24h. Re-runs each canvas's OWN assessment engine for designs whose newest assessment is missing or older than `stale_after_days` (7), and persists the score the engine ACTUALLY returned. Fixes the CAUSE of the Compliance Posture card reading 33-71 days stale: an assessment was only ever written by a human clicking assess in the canvas UI, so the newest evidence was whenever somebody last looked -- measured 2026-08-20, infra_designs held 84 designs behind 5 assessments. Deliberately does NOT reuse `auto_remediator.persist_verify_assessment`, which writes a hardcoded score=100.0/grade=A and cat1/2/3=0/0/0 (because `reassess_design` discards the engine score one call earlier); the posture card AVERAGES those rows and SUMS those cat columns, so building on it would fabricate perfect compliance into the database on a cadence. Score is checked `is None`, never falsiness -- three boundary designs score a real 0.0 and a falsiness check would skip them forever. Covers observability/boundary/infra; Security is out of scope (wider schema, design-id engine). Bounded by `max_per_run` (25) and what it skipped is reported BY NAME. | `run(ctx, conn)`; ctx: dry_run, stale_after_days, max_per_run | {reassessed, by_canvas, skipped_over_budget, errors, status} |
| Genesis Apps Registry | tools/genesis/apps_registry.py | xit-gen-01. Reads args/genesis_apps.yaml and produces the GENESIS_APPS table the /genesis dashboard and /api/genesis/* routes consume -- the same shape the routes always read, so nothing downstream changed. Replaces a dict literal in tools/dashboard/app.py that located every sibling system at `Path(BASE_DIR).parent / <name>` and ran its daemon with that directory as cwd and no existence check. Roots resolve the way args/kanban_external_repos.yaml does: the declared `root_env` wins, the sibling-directory `root_fallback` applies otherwise, and an app that is not on this machine is `available: false` -- `root_missing(cfg)` is the JSON answer every route returns for it instead of raising. Adds the `icdev-ft` sibling (ICDEV_FT_ROOT). Never raises at import: an unreadable YAML degrades to this repository alone with a warning. The launcher likewise starts the FathomDesk dashboard only while tools/trading/dashboard/app.py exists (ICDEV_TRADING_DASHBOARD_ENABLED=0 to skip). | library: load_genesis_apps(base_dir), resolve_root(entry, base_dir), root_missing(cfg) | dict keyed by app key |
| Asset Discovery Reflex | tools/genesis/reflexes/asset_discovery.py | rmf-disc-02. Passive network asset discovery, 24h. Sweeps the targets a deployment DECLARES in args/genesis_config.yaml, persists each sweep to nc_discovery_scans, imports what answered into ni_devices with source='discovery', and measures the inventory's provenance split every cycle. PASSIVE-ONLY BY DEFAULT: `ping` unless `allow_active_scan: true` — an SNMP community string or an SSH password presented to live infrastructure on a schedule with no human present is an operator decision, not a default. Ships with `targets: []`, and reports `unmeasured`/`no_targets_declared` rather than a clean run it did not make. NEVER seeds synthetic devices (a daemon fabricating inventory rows on a cadence manufactures its own evidence — that is the CLI's `--seed-synthetic`). Registered in BOTH daemon.REFLEX_NAMES and args/genesis_config.yaml. | `python tools/genesis/daemon.py --reflex asset_discovery --json` / `run(config, trust)` | {status, scan_id, targets_declared, addresses_expanded, devices_discovered, inventory, last_scan_age_hours, refusals, errors} |
| CI Runner Health Reflex | tools/genesis/reflexes/ci_runner_health.py | mfx-boot-02. Every 15 min. Intersects the forge's OFFLINE runner set (`gh api repos/<repo>/actions/runners`) with docker's RESTARTING set (`docker ps -a`) for the fleet declared in args/ci_runners.yaml (repo, compose dir, container / runner / compose-project names -- NO token). A container in BOTH whose log carries a registration-failure signature is re-registered through ONE `docker compose -p <project> up -d` with a token minted at that moment and passed in the ENVIRONMENT only (never argv, never a row). A container in one set only (offline-but-Up: a network event; Restarting-but-online: a race) is reported and never touched; a live compose-project label that disagrees with the declaration refuses (the 2026-08-30 cross-fleet adoption). prove -> audit (self_heal_triggered / ci_runner_health.reregister.intent, raise_on_error, BEFORE the act) -> apply -> confirm (docker running AND forge online; else applied_unconfirmed). Bounded per run (deferred candidates still proven and named), cooled per container from its own intent rows, never `docker rm` / `compose down`. Unmeasurable -- never ok -- when the declaration, docker or the forge cannot answer. Registered in BOTH daemon.REFLEX_NAMES and args/genesis_config.yaml. | `python tools/genesis/daemon.py --reflex ci_runner_health --json` / `python tools/genesis/reflexes/ci_runner_health.py [--apply]` (hand-run is a dry run unless --apply) / `run(config, trust)` | {status, classified, candidates, reregistered, deferred, outcomes, refused_by_reason, acted, reported, forge_readable, docker_readable, errors} |
| Lease Litter Reflex | tools/genesis/reflexes/lease_litter_reflex.py | kpr-stale-04. Hourly. Feeds every held `kanban:task:<id>` lease to the frozen restore act `reap_dead_lease` (autonomy-act-03), which re-proves dead-pid AND no-heartbeat, writes the intent row, releases and confirms -- and REFUSES a live holder. Carries NO reaping rule of its own (asserted by AST). Bounded by `max_reaps_per_run` (further candidates proven dry and reported `deferred`); an unreadable lease store reports unmeasurable, never ok. Registered in BOTH daemon.REFLEX_NAMES and args/genesis_config.yaml. | `python tools/genesis/daemon.py --reflex lease_litter_reflex --json` / `python tools/genesis/reflexes/lease_litter_reflex.py [--apply]` / `run(config, trust)` | {status, leases_held, reaped, would_reap, deferred, outcomes, refused_by_reason, acted, errors} |
