# Dashboard

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## Dashboard
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Web Dashboard | tools/dashboard/app.py | Flask web dashboard with role-based views, wizard, quick paths | --port, --debug | Web UI on port 5000 |
| Dashboard Config | tools/dashboard/config.py | Loads dashboard settings from args/monitoring_config.yaml and args/cui_markings.yaml with env-var overrides; exposes DB_PATH, PORT, DEBUG, CUI banners, BYOK, auth secret, classification | Environment variables (ICDEV_DB_PATH, ICDEV_DASHBOARD_PORT, etc.) | Module-level constants |
| Platform Health | tools/dashboard/platform_health.py | Aggregate health scoring across 10 ICDEV™ domains (database, agents, compliance, security, infrastructure, canvases, LLM, monitoring, CI/CD, marketplace); 60s in-process cache; bands: ≥90 healthy, ≥70 degraded, <70 critical | get_platform_health(), get_domain_health(domain), _invalidate_cache() | Composite + per-domain score/status/findings JSON |
| Canvas Aggregator | tools/dashboard/canvas_aggregator.py | Queries across all 7 canvas SQLite DBs (security, infra, observability, boundary, data, network, pipeline) to surface compliance summaries, finding counts, recent findings, and activity trends; 60s module-level cache | get_canvas_compliance_summary(), get_canvas_finding_counts(), get_recent_canvas_findings(limit), get_canvas_activity_trend(days), invalidate_cache(), close_canvas_connections() | Aggregated canvas data JSON |
| UX Helpers | tools/dashboard/ux_helpers.py | Jinja2 filters (friendly_time, glossary), error recovery dict, quick paths, wizard steps | register_ux_filters(app) | Template filters + globals |
| UX JavaScript | tools/dashboard/static/js/ux.js | Client-side glossary tooltips, timestamp formatting, accessibility, notifications, progress pipeline | Auto-init on DOMContentLoaded | ICDEV™ namespace |
| UX Stylesheet | tools/dashboard/static/css/ux.css | Tooltip, pipeline, wizard, quick path, breadcrumb, notification, accessibility styles | — | CSS |
| Charts Library | tools/dashboard/static/js/charts.js | Zero-dependency SVG chart library: sparkline, line, bar, donut, gauge with tooltips and animation | ICDEV™.lineChart(), ICDEV™.barChart(), ICDEV™.donutChart(), ICDEV™.gaugeChart() | SVG charts |
| Table Interactivity | tools/dashboard/static/js/tables.js | Table search, column sort, column filter, CSV export, row counter | Auto-init on DOMContentLoaded | Enhanced tables |
| Onboarding Tour | tools/dashboard/static/js/tour.js | Interactive overlay walkthrough for first-visit users, 6-step spotlight tour | ICDEV™.startTour(), ICDEV™.resetTour() | Tour overlay |
| Live Dashboard | tools/dashboard/static/js/live.js | Real-time SSE auto-refresh: connection status, smart debounced updates, event toasts | ICDEV™.connectSSE(), ICDEV™.disconnectSSE() | Live updates |
| Batch Operations JS | tools/dashboard/static/js/batch.js | Batch workflow UI: catalog display, execution progress, step status polling | ICDEV™.batchStartBatch(id, projectId) | Batch progress UI |
| Batch Operations API | tools/dashboard/api/batch.py | Flask blueprint: batch execute/status/catalog endpoints, background subprocess runner | POST /api/batch/execute, GET /api/batch/status | JSON batch status |
| Keyboard Shortcuts | tools/dashboard/static/js/shortcuts.js | Chord-based navigation (g+key), direct shortcuts, help modal overlay | ICDEV™.showShortcutsHelp() | Navigation + help modal |
| Mermaid Integration | tools/dashboard/static/js/mermaid-icdev.js | ICDEV™ Mermaid module: dark theme, click handlers, editor, SVG export, auto-init | ICDEV™.renderMermaid(), ICDEV™.initMermaidEditor(), ICDEV™.exportMermaidSVG() | Rendered diagrams |
| Diagram Definitions | tools/dashboard/diagram_definitions.py | Centralized Mermaid diagram catalog: 18 diagrams across 4 categories with role filtering | get_catalog_for_role(), get_diagram() | Diagram data |
| Diagrams API | tools/dashboard/api/diagrams.py | Blueprint: list/get diagram definitions, role-filtered catalog | GET /api/diagrams/, GET /api/diagrams/<id> | JSON diagram data |
| Proxy Middleware | tools/dashboard/proxy.py | WSGI strangler-fig proxy — routes static assets to Next.js build dir (prod) or live dev server (NEXT_DEV_URL); falls through to Flask for all other requests | WSGI app + env (NEXT_DEV_URL, NEXT_STATIC_ROOT, dev_mode) | WSGI middleware instance |
| Dashboard + Scheduler Launcher | tools/launch_dashboard.py | One-shot launcher: kills stale Python processes, verifies PostgreSQL connectivity, starts Dashboard (tools/dashboard/app.py) on ICDEV_DASHBOARD_PORT, polls /health, then starts Kanban Scheduler (tools/genesis/kanban_scheduler.py); writes logs to .tmp/ | --no-kill, --wait N, --json | PIDs, URLs, JSON status; exit 1 on PG failure |
| Component Registry | tools/config/component_registry.py | Loads `args/component_registry.yaml` and provides the single source of truth for canvases, child apps, feature toggles, and core extensions. Used by dashboard/app.py for blueprint registration, iqe_dispatch, and by tools/cli/enable.py for toggle derivation. | get_registry(), ComponentRegistry | Component objects, toggle maps, IQE adapter mapping, URL prefixes, nav context |
| Template Engine | tools/builder/template_engine.py | Jinja2 file-tree generator for ICDEV canvases and child apps. Reads a YAML manifest, renders template files with variable substitution, and runs validators. Foundation for configurable canvas/child-app scaffolding. | render_tree(template_dir, out_dir, variables) | Result dict with rendered files, errors, and validation failures |
| Scaffold CLI | tools/cli/scaffold.py | `icdev scaffold canvas <key>` subcommand that wraps template_engine.py to generate a new canvas skeleton. | --template, --out, --vars | Scaffolded file tree on disk |
| Changelog Parser | tools/dashboard/changelog.py | Parses Keep-a-Changelog formatted CHANGELOG.md files into structured release dicts; handles Unreleased blocks, section headings (Added/Fixed/etc.), and bullet entries | parse_changelog(path) | List of {version, date, sections} dicts |
| Core Profile | tools/config/core_profile.py | Loader for `args/core_profiles.yaml` enterprise deployment presets (local-dev, air-gap, saas-il4, il6-secret). Provides env-var-aware defaults for storage backend, LLM provider, classification, and CUI/BYOK flags. | load_profiles(), get_profile(name), profile_default(var, fallback), apply_active_profile_env_defaults() | Profile dict; applied env defaults |
| Profile CLI | tools/cli/profile.py | `icdev profile list/show/apply` subcommand for inspecting and applying core profiles to `.env`. | --env-file, --dry-run, --json | Written or previewed env overrides |

| AI Data Mapper | tools/data_canvas/ai_mapper.py | AI-assisted schema-to-schema field mapping engine: parse_schema (JSON Schema/CSV/DDL/OpenAPI3), score_field_pairs (name+semantic+type similarity), assign_status (confidence thresholds), generate_transforms (SQL/Python/dbt/XSLT). Gracefully degrades to name+type similarity when LLM unavailable. | parse_schema(raw, fmt), score_field_pairs(src, tgt), assign_status(conf), generate_transforms(session_id, pairs, artifact_type) | List of field descriptors; confidence-scored match pairs; transformation artifact text |
| Genesis Fleet Probe | tools/dashboard/genesis_fleet.py | qa-fail-1474518ac97ac6a2: bounds the /genesis fan-out. The page called `_genesis_run` once per app in a `for` loop, each spawning a Python interpreter with `timeout=15` and no cap on the page, so 8 declared apps meant a 120s worst case (measured 1.69s serial vs 0.89s bounded against the same eight real roots) -- which is what timed out the nav smoke test's 10s budget. Probes concurrently in a bounded pool, capped by `deadline_seconds`, with the per-app timeout DERIVED from the remaining budget so one app can never outlast the page. FOUR probe states, never merged: `ok` | `root_missing` (not on this machine, never probed) | `unmeasured` (probe started, did not answer -- previously rendered OFFLINE, identical to not installed) | `error` (probe ran and failed -- `_genesis_run` returns `{"error": "parse_failed"}` WITHOUT raising, so this previously rendered DISABLED, a broken probe reading as 'installed, switched off'). Library, no CLI; `probe` is injected so nothing here knows about subprocesses. Mirrored to icdev/tools/dashboard/. | gather_fleet_status(apps, probe, deadline_seconds=8.0, max_workers=8) | `{key: {**payload, _name, _available, _probe_state}}` in registry order |
| Nav Path Generator | tools/dashboard/nav_paths.py | mfx-sib-02: derives the two nav surfaces nothing used to generate -- the Compliance dropdown's `request.path in [...]` active-path list in base.html (from the dropdown's OWN menu links plus every literal 301 redirect app.py declares to one of them, so a route-migration card writes its menu link and its redirect and nothing else) and the `- Pages:` line in .claude/commands/start.md (from the app's url_map). Writes between markers in BOTH base.html copies and start.md. The url_map probe runs in a SUBPROCESS WITH A SCRUBBED ENVIRONMENT with every registry component forced ON -- blueprint registration depends on .env toggles, and the same checkout measured 4568 rules under an ambient environment and 2370 under a bare one, so an inherited env makes `--check` pass or fail by whose shell ran it. Legacy paths the derivation provably cannot see are DECLARED with a written reason in args/nav_paths.yaml, never lost in a template line. `--nav-only` is a template parse plus an AST walk (milliseconds, no Flask) and runs in CI Lint and in the pre-commit hook; the Pages half needs the app (~16s) and runs in test-gates. Mirrored to icdev/tools/dashboard/. | --write, --check, --nav-only, --pages-only, --json | Rewritten marker blocks; exit 0 clean / 1 drift / 2 could not derive |
