# LLM Provider Abstraction (Vendor-Agnostic)

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## LLM Provider Abstraction (Vendor-Agnostic)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| LLM Provider Base | tools/llm/provider.py | ABC base classes (LLMProvider, EmbeddingProvider), vendor-agnostic LLMRequest/LLMResponse, message/tool format translators | — | — |
| Context Compressor | tools/llm/compression/context_compressor.py | Headroom-inspired reversible token-budget compression (adapt-hd-02/03). Truncates middle messages when estimated token count exceeds threshold. Configured via args/llm_config.yaml `compression:` section (enabled:false by default). Router calls `_compress_request_context()` before fallback chain. Audit log of saved tokens. Exempt functions list. | CompressorConfig, LLMRequest | LLMRequest (possibly shortened) |
| LLM Router | tools/llm/router.py | Config-driven function→model routing with fallback chains, reads args/llm_config.yaml | function name | (provider, model_id, config) |
| LLM Router — invoke_for_role | tools/llm/router.py | `invoke_for_role(role_key, function, request)` — walks role's fallback chain with availability check, RL reranking, outcome recording, telemetry; raises LLMUnavailableError if all fail | role_key (e.g. cot_reasoner), LLMRequest | LLMResponse |
| LLM Router — get_models_for_category | tools/llm/router.py | `get_models_for_category(category)` — returns ordered preferred model list for a task category (coding, reasoning, writing, vision, etc.) filtered to registered models | category string | List[str] model names |
| LLM Router — get_diverse_models | tools/llm/router.py | `get_diverse_models(role_key, count)` — returns up to count distinct models from a role's chain, maximizing provider-family diversity; used by CoD to assign one unique model per debater slot | role_key, count int | List[str] model names |
| LLM Router — discover_ollama_models | tools/llm/router.py | `discover_ollama_models(force_refresh)` — queries /api/tags on probe_providers, returns model names not already in registry; cached for refresh_interval_seconds | force_refresh bool | List[str] raw model names |
| LLM Task Categories | args/llm_config.yaml | `task_categories:` section — capability matrix mapping task types (coding, reasoning, writing, vision, summarization, long_context, structured_output, agentic) to ordered preferred model lists | — | — |
| Ollama Model Discovery | tools/llm/router.py | `_register_discovered_models()` called at startup; auto-registers unknown Ollama models in-memory with default_capabilities; does NOT write to disk | — | in-memory model registry update |
| RL Router | tools/llm/rl_router.py | Q-Learning RL agent that ranks model chains by learned success/latency history (epsilon-greedy, TD(0)); persists Q-table to llm_rl_qtable in icdev.db | --stats, --reset, --function, --model, --json | Q-table stats |
| Bedrock Provider | tools/llm/bedrock_provider.py | AWS Bedrock LLMProvider: Anthropic models, thinking/effort, tools, structured output, retry/backoff | LLMRequest | LLMResponse |
| Anthropic Provider | tools/llm/anthropic_provider.py | Direct Anthropic API LLMProvider via anthropic SDK | LLMRequest | LLMResponse |
| OpenAI-Compat Provider | tools/llm/openai_provider.py | OpenAI-compatible LLMProvider: OpenAI, vLLM, Azure via configurable base_url | LLMRequest | LLMResponse |
| Ollama Native Provider | tools/llm/ollama_provider.py | Native Ollama REST API provider using /api/chat — faster than OpenAI-compat for local models, native vision support | LLMRequest | LLMResponse |
| Embedding Provider | tools/llm/embedding_provider.py | Embedding providers: OpenAI, Bedrock Titan, Ollama (nomic-embed-text) | text | float[] |
| LLM Config | args/llm_config.yaml | Master config: providers, models, per-function routing chains, embedding config, pricing | — | — |
| LLM Gateway | tools/llm/gateway.py | LLM Gateway/Proxy: pre/post-invoke security checks (injection detection, PII scrubbing, rate limiting), audit trail, gate check | --invoke, --check, --rate-status, --gate, --json | Gateway response + audit |
| LLM Gateway Config | args/llm_gateway_config.yaml | Gateway config: injection rules, PII patterns, rate limits, audit settings, gate thresholds | (data) | YAML config |
| Prompt Registry | tools/llm/prompt_registry.py | Prompt version control: version, activate, rollback, diff, A/B test prompt templates with audit trail | --register, --activate, --rollback, --diff, --ab-test, --list, --json | Prompt versions + diffs |
| Cost Intelligence | tools/llm/cost_intelligence.py | Cost intelligence: anomaly detection, monthly projection, optimization recommendations, edge-vs-cloud comparison | --report, --anomalies, --project, --optimize, --edge-vs-cloud, --json | Cost analysis + recommendations |
| Model Drift Monitor | tools/llm/model_monitor.py | Model drift monitor: quality scoring, latency/token tracking, statistical drift detection (Welch's t-test), gate check | --check, --report, --drift, --gate, --model, --json | Drift analysis + gate status |
| LLM Eval Runner (OPT-64) | tools/llm/eval_runner.py | Declarative YAML→side-by-side provider comparison (promptfoo pattern, MIT). Loads args/llm_evals/*.yaml, runs prompts across logical models via LLMRouter, evaluates 6 assertion types (contains/not_contains/regex/max_length/min_length/json_schema), writes markdown+json+html reports to reports/llm_evals/ | --eval NAME, --models, --output-dir, --json, --gate | EvalReport (md/json/html) |
| Cache Savings Analytics | tools/cache_savings/savings.py | Two-level cache cost model: response cache (avoided LLM calls) + context cache (prefix token savings). Aggregates hit rates, token savings, and USD cost delta per function | get_savings_stats(conn?) | dict: enabled, backend, summary, by_function |
| Cache Savings Dashboard | tools/cache_savings/blueprint.py | Flask blueprint at /cache-savings with KPI tiles, per-function table, IQE widget, and /api/cache-savings/tile compact summary for home page monitor card | GET /cache-savings, /api/cache-savings/stats, /api/cache-savings/tile | HTML / JSON |
| Cache Warmer | tools/cache_savings/warmer.py | Pre-warms LLM response cache on deploy using seed prompts per function; dry-run support | --dry-run, --function, --json | Warm result dict |
| Agent Loop Primitive | tools/llm/agent_loop.py | Reusable router-level agent loop (`run_agent_loop`) — automatic LLM re-prompting with native tool use: LLM→tool_calls→execute→tool_result→re-prompt→repeat until done. Tool-handler-agnostic; routes through LLMRouter.invoke using `LLMRequest.tools`/`LLMResponse.tool_calls`/`stop_reason` so it works with any tool-capable provider (Anthropic/OpenAI/Bedrock/Ollama). Raises `AgentLoopUnsupported` for the CLI bridge or `supports_tools=false` models. `DONE` sentinel terminates; `max_iterations` cap; `stop_event` aborts; `on_turn` callback for audit/persist. `initial_messages` seeds a pre-built message history (takes precedence over `user_prompt`/`resume_session_id`). Returns `AgentLoopResult`. Also exposes `run_agent_loop_with_rubric(router, rubric=, **kwargs)` — declares "what done looks like" up front; a separate tool-free grader LLM call judges each completed response against the rubric (`satisfied`/`needs_revision`/`failed`) and, on `needs_revision`, injects feedback and resumes the transcript via `initial_messages` for up to `max_grading_iterations` rounds (config: `args/llm_config.yaml` `agent_loop.rubric`). Returns `RubricLoopResult` (result, satisfied, grades, grading_attempts). | router, system_prompt, user_prompt, tools (OpenAI fn schema), tool_handlers, llm_function, max_iterations, stop_event, on_turn | AgentLoopResult (done, turns, final_content, tool_call_log, messages) |
| Agent Loop Approval Gate | tools/llm/approval_gate.py | Reversibility classification + halt-for-confirmation for every tool call `run_agent_loop` emits (ars-appr-01). `classify(tool_name, tool_input, tool_schema=)` returns a `Classification` of `reversible` / `irreversible` / `unknown`; **unknown requires approval** — an allowlist that fails open is decoration. Rule order is the security property: explicit schema `reversibility` > `irreversible.tools` name match > first-party `is_read_only` schema flag > `irreversible.patterns` regex over the flattened input > `reversible.tools` allowlist > UNKNOWN. Patterns run BEFORE the allowlist so an allowlisted shell tool cannot launder `git push --force` through itself. `build_approval_hook(...)` returns an `on_pre_tool_use` hook (composes with a caller-supplied one via `chain=`); `run_agent_loop` builds it by default. Modes `manual` (ask the injectable `Approver`; console on a TTY, fail-closed otherwise) / `deny` / `off` (audited escape hatch). Composes `tools.airgap.hook_compat.run_pre_tool_check` for outright-forbidden actions. Every decision — approved and denied — is INSERTed into the append-only `agent_approval_log` with actor + reason. Config: `args/approval_gate.yaml`; env `ICDEV_AGENT_APPROVAL_GATE`, `ICDEV_AGENT_APPROVAL_MODE`, `ICDEV_APPROVAL_ACTOR`. Canonical module: `icdev/tools/llm/approval_gate.py`; `tools/llm/approval_gate.py` is a pure re-export shim. CLI: none (library API). | tool_name, tool_input, tool_schema, approver, mode | Classification / GateOutcome / on_pre_tool_use hook |
| Context Compressor | tools/llm/context_compressor.py | Reversible context compression middleware for AI agent workflows. Reduces token usage 60-95% via SmartCrusher (text), CodeCompressor (code), and CacheAligner (prefix normalization) strategies. Uses the headroom library when installed, with a deterministic built-in fallback. Integrates with LLMRouter as a pre-request middleware and persists compression telemetry to `llm_context_compression_log`. Public API: `ContextCompressor.compress(messages, budget_tokens, content_type, cache_align)` → `CompressedContext`; `compress_messages()` one-shot convenience; `handle_compress_context()` MCP gateway handler. | `messages` (OpenAI format), `budget_tokens` (int), `content_type` (`auto`/`text`/`code`), `cache_align` (bool) | `CompressedContext` (compressed messages + original/compressed tokens + ratio + method + decompression map) |


## Context Budget (2026-07-26)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Context Budget | tools/llm/context_budget.py | Token accounting + evidence packing against a model's REAL context window. Replaces DIC's fixed `results[:5]` x 1000-char evidence slice, which sent ~1.2k tokens regardless of model. `floor_window_for_function()` takes the MINIMUM across a routing chain (two_tier, RL re-rank and the CLI bridge all reorder it at invoke time). Estimation deliberately over-counts — under-counting overflows a request, over-counting packs one fewer chunk. Public API: `estimate_tokens`, `context_window_for`, `floor_window_for_function`, `available_input_tokens`, `pack_evidence`, `config_gaps`, `routed_models`. | `--report`, `--check-config`, `--json` | JSON `{models, missing_context_window, default_window_tokens}` |
