# AI-ify Canvas (AI-ify)

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## AI-ify Canvas (AI-ify)

Scans source repositories for AI-augmentable code patterns and produces a scored, phase-banded modernization roadmap. Primary path: Semgrep CLI (multi-language, 8 pattern rules). Air-gap fallback: Python `ast` walker for `.py` and a regex/tree-sitter path for `.cs`. Scores each opportunity using a deterministic weighted multi-criteria model (`args/aiify_config.yaml`). Persists results to PostgreSQL (default) or SQLite (`data/aiify_canvas.db`). Integrates with AIMC (`/ai-ml/models/new`) and AADC (`/agentic-ai/topologies/new`) for downstream tooling. Feature flag: `ICDEV_AIIFY_ENABLED`. IQE collections: `aiify.opportunities`, `.scans`, `.roadmaps`. Classification: CUI // SP-CTI.

| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| AI-ify Pipeline Engine | tools/aiify/engine.py | Full 7-step pipeline orchestrator. (1) `init_db()`, (2) `run_readiness_check()`, (3) INSERT `aiify_scans` status=running, (4) `detect_patterns()`, (5) INSERT `aiify_opportunities` + `score_opportunity()` per hit, (6) INSERT `aiify_scores`, (7) `generate_roadmap()`, (8) UPDATE `aiify_scans` status=completed + audit log. Maps each `pattern_type` to an `ai_paradigm` and a recommended IL model. | `run_scan(input_type, input_ref, scan_context=None) -> dict` | `{scan_id, opportunities_count, scores_count, roadmap_id, status, pillar_scores, overall_readiness_score, icdev_checks}` |
| Pattern Classifier | tools/aiify/pattern_classifier.py | Semgrep CLI wrapper + Python AST fallback + C# regex/tree-sitter fallback. Detects 8 pattern types: `nested_conditionals` (if-depth ≥ 3), `regex_user_input`, `string_template_rendering`, `scheduled_cron`, `hardcoded_threshold`, `db_render_notify_chain`, `keyword_list_search`, `large_rule_table`. Config: `args/aiify_config.yaml` (pattern_min_depth, rule_min_keys, semgrep.rules_dir, semgrep.timeout_seconds). | `detect_patterns(target_path: str) -> list[dict]` | List of `{pattern_type, module_path, function_name, line_start, line_end, language, pattern_detail}` |
| Opportunity Scorer | tools/aiify/opportunity_scorer.py | Deterministic weighted multi-criteria scorer. Sub-scores: `value = usage_freq*0.40 + task_complexity*0.35 + automation_deficit*0.25`; `feasibility = data_avail*0.40 + il_model_exists*0.35 + (1-integration_complexity)*0.25`; `risk = reversibility*0.40 + compliance_impact_inv*0.35 + (1-dep_complexity)*0.25`; `composite = value*0.45 + feasibility*0.35 + (1-risk)*0.20`. IL-level penalty applied to compliance when `il_level=il5/il6`. Weights hot-reloaded from `args/aiify_config.yaml`; degrades to built-in defaults on missing file. CLI: `--opportunity-file PATH` or individual `--usage-freq / --task-complexity / ...` flags. | `score_opportunity(opportunity: dict, scan_context: dict) -> dict` · CLI: `python tools/aiify/opportunity_scorer.py [--opportunity-file PATH] [--json]` | `{value_score, feasibility_score, risk_score, composite_score, score_detail}` all in [0.0, 1.0] |
| PRD AI-Readiness Assessor | tools/aiify/prd_readiness_assessor.py | Pre-build "shift-left" sibling of the code scanner: scores how AI-ready an intake/requirements PRD is and infers AI-augmentation opportunities from requirement prose BEFORE build. Reads `intake_sessions` + `intake_requirements`. Six deterministic weighted components: `ai_mention_density` (0.20), `data_requirement_specificity` (0.20), `integration_clarity` (0.15), `acceptance_criteria_quantifiability` (0.20), `governance_declarations` (0.15, reuses `tools/requirements/ai_governance_scorer`), `il_model_appropriateness` (0.10). Maps phrases → AI-ify `pattern_type` (e.g. "classify"→manual_classification_ui, "route documents"→document_routing, "extract X from"→regex_user_input, "generate report/email"→string_template_rendering, "threshold alert"→hardcoded_threshold, "search by keyword"→keyword_list_search) then reuses `opportunity_scorer.score_and_assess` so PRD-stage and code-stage opportunities are scored identically. Deterministic path is always the fallback (air-gap safe); `use_llm=True` augments via LLM Router. CLI: `--session-id ... [--use-llm] [--json]`. | `assess_prd_for_ai_readiness(session_id, conn=None, use_llm=False) -> dict` · CLI: `python tools/aiify/prd_readiness_assessor.py --session-id SID --json` | `{score, components, gaps, opportunities, overall_ai_readiness, recommendations}` |
| Roadmap Generator | tools/aiify/roadmap_generator.py | Builds a prioritized roadmap from scored opportunities. Phase bands: P1 ≥ 0.7 (Quick Wins), P2 ≥ 0.5 (Core Modernization), P3 ≥ 0.3 (Long-Horizon Investments). Effort days from `context/aiify/pattern_catalog.json` midpoints. Emits `aimc_links` and `aadc_links` (agentic_trigger paradigm only). Persists to `aiify_roadmaps` via `get_connection()`. | `generate_roadmap(scan_id, opportunities: list[dict], scores: list[dict]) -> dict` | `{roadmap_id, scan_id, title, phases, aimc_links, aadc_links, total_effort_days, created_at}` |
| DB Initializer | tools/aiify/db/init_db.py | Dual-backend schema bootstrap. Backend priority: `AIIFY_STORAGE_BACKEND` → `ICDEV_CANVAS_STORAGE_BACKEND` → PostgreSQL default; SQLite fallback to `data/aiify_canvas.db` (`AIIFY_DB_PATH` override). Tables: `aiify_scans`, `aiify_opportunities`, `aiify_scores`, `aiify_roadmaps`, `aiify_audit_log`. CHECK constraints derived from `constants.PATTERN_TYPES`, `constants.AI_PARADIGMS`, `constants.SUPPORTED_LANGUAGES` — never hardcoded. Exports `SCHEMA_PG` and `SCHEMA_SQLITE`. | `init_db() -> None` · `get_connection() -> conn` · CLI: `python tools/aiify/db/init_db.py` | Schema created + stdout confirmation |
| Constants | tools/aiify/constants.py | Canonical enumerations for the AI-ify subsystem. Defines: `PATTERN_TYPES` (8 patterns), `AI_PARADIGMS` (7 paradigms), `SUPPORTED_LANGUAGES` (python, java, csharp, go, rust, typescript), `IL_LEVELS`, `SCORING_WEIGHTS`, `CHECK_PATTERN_TYPE`/`CHECK_AI_PARADIGM` SQL CHECK constraint strings. All SQL constraints must be derived from these constants. | (library) | `PATTERN_TYPES, AI_PARADIGMS, SUPPORTED_LANGUAGES, IL_LEVELS, SCORING_WEIGHTS, CHECK_PATTERN_TYPE, CHECK_AI_PARADIGM` |
| Agent Readiness Checker | tools/aiify/agent_readiness/checker.py | Pre-scan readiness assessment. Ported from kodustech/agent-readiness (TypeScript) with ICDEV IL/NIST extensions. Runs 11 pillars in weighted order: code_quality (1.0), documentation (1.0), testing (1.2), structure (0.8), dependencies (1.0), configuration (1.0), security (1.0), il_classification (ICDEV, 1.0), nist_controls (ICDEV, 1.0), stig_compliance (ICDEV, 1.0), append_only_audit (ICDEV, 1.0). Results stored in `aiify_scans.language_profile.agent_readiness_summary`. | `run_readiness_check(repo_path: str \| Path) -> dict` · CLI: `python -m tools.aiify.agent_readiness <repo_path> [--json]` | `{pillar_scores, overall_readiness_score, icdev_checks}` |
| IQE Adapter | tools/iqe/adapters/aiify.py | Registers three IQE query collections: `aiify.opportunities` (aiify_opportunities JOIN aiify_scores), `aiify.scans` (aiify_scans), `aiify.roadmaps` (aiify_roadmaps). Enables natural-language queries against AI-ify data via the IQE engine at `POST /api/iqe-query`. | (auto-registered on import) | IQE collections: opportunities, scans, roadmaps |
| Canvas Event Emitter | tools/aiify/event_emitter.py | Emits cross-canvas events to the `canvas_events` table when AI-ify completes a scan or detects a high-value gap. Two injection points: `emit_canvas_scored` (fired after `run_scan()` when the posture grade changes) and `emit_gap_identified` (fired when an unbuilt/partially-built opportunity exceeds the value threshold). Source canvas: `aiify`; target canvas: `dic`. Uses `get_connection()` with CUI classification by default. | `emit_canvas_scored(canvas_name, previous_grade, current_grade, current_score, scan_id="", tenant_id="", classification="CUI") -> bool` · `emit_gap_identified(canvas_name, opportunity_id, gap_type, value_score, tenant_id="", classification="CUI") -> bool` | `True` on successful INSERT into `canvas_events`; `False` on DB error (logged as warning, never raises) |
| Diagram Generator | tools/aiify/diagram_generator.py | Deterministic (no LLM) Mermaid flowchart builder for the AI-ify target architecture. Given a scan dict and a list of opportunities, renders a `flowchart TD` grouping current modules into three AI layers (GenAI, Classic ML, Agentic AI) with IL-aware model routing nodes. Caps output at `max_opps=25` nodes to stay Mermaid-renderable. Embedded by `blueprint.py` into the generated PRD via a fenced mermaid block and rendered inline in the wizard via mermaid.js. | `build_architecture_mermaid(scan, opps, title=None, *, max_opps=25) -> str` (raw Mermaid source) · `build_architecture_mermaid_block(scan, opps, title=None, *, max_opps=25) -> str` (fenced block) | Mermaid `flowchart TD` string |
| Innovation Signal Ingester | tools/aiify/signal_ingester.py | Innovation → AI-ify bridge (Option A). Queries `innovation_signals` for AI-augmentation-relevant categories (`ai_tooling`, `agentic`, `external_framework_analysis`, `aiify_opportunity`), cross-references against `aiify_opportunities` by pattern type, and upserts bridge records into `aiify_innovation_bridge`. Supports a configurable `min_innovation_score` threshold (default 0.60). Dual-backend DDL (PG + SQLite fallback). CLI: `--ingest` (run ingestion) or `--report` (list existing bridge records). | `ingest(min_innovation_score=0.60) -> dict` · CLI: `python tools/aiify/signal_ingester.py --ingest [--json]` · `python tools/aiify/signal_ingester.py --report [--json]` | `{"bridged": N, "skipped": M, "errors": [...]}` |
| Flask Blueprint | tools/aiify/blueprint.py | Flask Blueprint (`aiify_bp`, url_prefix `/ai-ify`) wiring all HTTP routes for the AI-ify canvas. Routes: `GET /ai-ify/` (index: scan history + latest opportunities + roadmap); `POST /api/scan` (trigger scan → `run_scan()`); `GET /api/scan/<id>` (poll scan results); `DELETE /api/scan/<id>` + `DELETE /api/scan/all` (cascade delete); `POST /api/send-to-kanban` (promote roadmap phases to kanban_tasks with epic + 4 atomic child tasks per opportunity); `POST /api/generate-prd` (build phase PRD markdown with HITL curation summary + Mermaid arch diagram); `POST /api/prd-dry-run` (score PRD, AI-boost if <0.6, preview kanban tasks); `POST /api/hitl-decision` (record accept/reject/clear for innovation/creative/research/prd signals); `POST /api/run-innovation` (async-trigger Innovation engine); `POST /api/iqe-query` (IQE natural-language query); backward-compat `aiify_compat_bp` 301-redirects `/ai-augmentation/*` → `/ai-ify/*`. | Flask `app.register_blueprint(aiify_bp)` | Registered blueprints `aiify_bp` + `aiify_compat_bp`; HTTP JSON responses for all canvas API calls |
| Version Anomaly Detector | tools/aiify/version_anomaly_detector.py | AI-ify `anomaly_detection` implementation for the `hardcoded_threshold` pattern (opportunity 467). Replaces hard-coded version-range assertions (e.g. `assert (3,0,2) <= ver < (6,0,0)`) with LLM-based compatibility reasoning so bounds don't go stale as upstream packages evolve. Uses `LLMRouter.invoke("version_compat_check", ...)` with a structured JSON prompt; falls back to a deterministic heuristic (semver distance) when no LLM is available. Result is cached in `aiify_version_compat_cache` table (PG primary, SQLite fallback). CLI: `--package NAME --version X.Y.Z [--context TEXT] [--json]`. | `check_version_compat(package, version, *, context="", timeout=30) -> dict` · CLI: `python tools/aiify/version_anomaly_detector.py --package chardet --version 4.0.0 --json` | `{compatible: bool, confidence: float, reason: str, recommendation: str, cached: bool}` |
| AAC -> AI-ify Table Rename | tools/aiify/maintenance/aac_to_aiify_rename.py | OPERATOR SCRIPT, deliberately NOT a migration. Copies data from the legacy `aac_*` tables into the renamed `aiify_*` tables without dropping or mutating the originals (the append-only `aac_audit_log` is never UPDATEd or DELETEd — NIST AU preserved). Moved out of `tools/db/migrations/` on 2026-08-15, where it sat as a bare `172_aiify_rename.py` that `MigrationRunner` cannot discover and had therefore never run. The fix for its siblings was to promote them into discoverable directories; the fix for THIS one was the opposite, because it writes to the AI-ify CANVAS database (`AIIFY_PG_DATABASE`) and not to icdev — letting the runner execute it would point a data move at the wrong database and record success against a schema it never touched. Run it deliberately. | (operator script) | Row counts copied per table |
