# Testing (Additional)

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## Testing (Additional)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| GovEval Benchmark | tools/testing/goveval.py | 7-dimension Gov/DoD compliance quality benchmark (LeanStral FLTEval-adapted, D-VL-9) | --project-id, --dimension, --gate, --trend, --compare, --json | Dimension scores + gate |
| Platform Check | tools/testing/platform_check.py | OS environment compatibility validation (D145) | --json | Compatibility report |
| Claude Dir Validator | tools/testing/claude_dir_validator.py | .claude directory governance validator (9 checks) | --json, --human, --check, --all | Alignment report |
| Theater Detector | tools/testing/theater_detector.py | Detects test theater anti-patterns (8 types: tautological_assertion, mock_dominated, fixture_theater, assertion_free, hardcoded_oracle, smoke_masquerade, always_green, spec_drift) using AST + regex; no LLM required, air-gap safe | --scan &lt;dir&gt; [--json] | Anti-pattern report with severity (block/warn/none) |
| QA Agent Runner | tools/testing/qa_agent_runner.py | ACE qa_agent execution primitive — runs the Playwright E2E suite in deadline-bounded batches (a killed single invocation emits no report at all), NAMES the spec files it did not reach, detects canvas coverage gaps, files kanban `fix` tasks for failures, persists results to ace_qa_runs/ace_qa_failures | --run [--canvas KEY] [--deadline-seconds N] [--batch-size N] --json, --discover-gaps --json, --status RUN_ID --json | QARunResult JSON (status: passed/failed/no_tests/incomplete) / gap list / run status |
| Selector Healer | tools/testing/selector_healer.py | AI-assisted Playwright locator repair — parses error output for broken selectors, proposes accessibility-tree replacements via vision LLM (confidence ≥ 0.7), applies via HITL-gated patch_file | --stderr-file FILE --json, --selector SEL --spec-file SPEC --screenshot PNG [--apply] --json | RepairProposal JSON |
| Perf Baseline Harness | tools/testing/perf_benchmark.py | Load + latency regression harness (crx-test-01). Benchmarks ~10 hot dashboard/API endpoints against a LIVE local dashboard, recording p50/p95/p99 (urllib, no deps); optional locust load profile (tools/testing/perf/locustfile.py, guarded import). Warn-only regression compare vs committed baseline (tests/perf/perf_baseline.json) with configurable tolerance; records storage backend (numbers only comparable like-for-like). Self-skips when no live server / locust absent — NOT wired into required CI. Pytest: tests/perf/test_perf_benchmark.py | --json, --base URL, --samples N, --update-baseline, --compare, --tolerance F, --metric p50_ms\|p95_ms\|p99_ms, --enforce, --load --users N --run-time 30s | Per-endpoint p50/p95/p99 JSON + regression report |
| A11y Sweep (Section 508) | tools/testing/a11y_sweep.py | Section 508 / WCAG 2.1 accessibility sweep — injects vendored axe-core (tools/testing/vendor/axe/axe.min.js, MPL-2.0) into ~15 dashboard pages via headless Selenium, reports violations by impact, diffs vs committed baseline (tests/e2e/a11y_baseline.json). Warn-only by default; fails on NEW critical only under ICDEV_A11Y_ENFORCE=1. Self-skips with no live dashboard. Pytest wrapper: tests/e2e/test_a11y_section508.py | --json, --base-url URL, --update-baseline, --enforce, --no-screenshot | Sweep report JSON (violations by impact + NEW-vs-baseline diff) |

| TSR Triage Diff | tools/testing/tsr_triage_diff.py | Compares two `pytest --tb=no -q -rfE` transcripts (shared checkout vs clean worktree) and classifies each test file `ambient` (fails clean-only), `real` (fails in both), `shared` (fails shared-only), or `unknown` (a confounder applies). Emits the markdown comparison table plus baseline counts. Diagnostic; used by the TSR epic (tsr-core-01-d3) to separate ambient-DB artefacts from genuine defects | --shared FILE --clean FILE --slice FILE [--confound FILE] [--hung FILE] [--unreached FILE] --out FILE | Markdown table + baseline/classification counts |
