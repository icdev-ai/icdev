# Git Worktree Parallel CI/CD (Phase 41)

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## Git Worktree Parallel CI/CD (Phase 41)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Worktree Manager | tools/ci/modules/worktree.py | Git worktree lifecycle: create (sparse checkout), list, cleanup, status | --create, --list, --cleanup, --status | WorktreeInfo |
| Worktree Paths | tools/git/worktree_paths.py | Single source of truth for WHERE a worktree may live; disjoint roots per actor, session-namespaced CLI paths, enforced by pre_tool_use | --path ACTOR SLUG, --check PATH, --audit, --json | Path / sanctioned bool / audit report |
| GitLab Task Monitor | tools/ci/triggers/gitlab_task_monitor.py | Poll GitLab issues for {{icdev: workflow}} tags, auto-trigger workflows | --interval, --dry-run, --once | Workflow launch |
| Manifest Merge Rehearsal | tools/git/manifest_merge_rehearsal.py | kax-conflict-03. Scripted two-branch git rehearsal that MEASURES which `tools/manifest/` layout survives the observed edit pattern (unrelated tasks each registering a new tool under the same topic). Builds a throwaway repo per scenario, cuts N branches, merges them back, and reports conflict / entries-kept / duplicate-entries. Layouts: `table-row` (baseline), `heading-block`, `union` (`.gitattributes merge=union`), `per-tool-file`. Runs both merge paths — `worktree` (local `git merge`) and `merge-tree` (bare `git merge-tree --write-tree`, the plumbing a forge runs server-side to compute mergeability), because a layout that is clean only in the worktree path still shows a PR as conflicted. Measured 2026-08-08: table-row and heading-block CONFLICT in both paths; union and per-tool-file are clean in both at 2/3/5 branches. Backs the `merge=union` decision recorded in `.gitattributes`. | --layout, --mode {worktree,merge-tree,both}, --branches, --keep, --json | Per-scenario conflict report + `conflict_free_layouts` |

| CI Test List Merge Rehearsal | tools/git/ci_test_list_merge_rehearsal.py | kax-conflict-07. Scripted multi-branch git rehearsal that MEASURES whether N branches each adding a gated test file can merge without a human. Layouts: `inline` (the CONTROL — allowlist as a line-continuation chain inside .github/workflows/icdev-ci.yml), `external` (moved to args/ci_test_files/core.txt, no merge driver), `external-union` (the shipped shape, `merge=union`). Runs both merge paths — `worktree` (local `git merge`) and `merge-tree` (bare, forge-side). Measured 2026-08-09: inline and external CONFLICT in both paths at 2/3/5 branches; external-union is clean in both with every entry kept. `--gate` also fails if the `inline` control stops conflicting, so a rehearsal that no longer reproduces the problem cannot report success. `--repo .` rehearses against a clone and the real shipped list. | --layout, --mode {worktree,merge-tree,both}, --branches, --repo, --keep, --gate, --json | Per-scenario conflict report + `conflict_free_layouts` |
| Gated Test List Resolver | tools/ci/gated_test_list.py | kax-conflict-07. Resolves the CI pytest allowlists in args/ci_test_files/*.txt that used to be inlined in icdev-ci.yml, and re-earns the "gate cannot silently shrink" property outside the YAML: `--check` exits 1 when the list is missing, EMPTY, below its recorded floor, names a path absent from the checkout, or lists a path twice (what a careless union merge leaves behind). `--extract-workflow` parses the inline pytest chains out of an arbitrary workflow file, which is how the before/after "no entry changed" diff was produced. Also owns the tsg-policy-01 RATCHET: `--check-coverage` exits 1 when a collectible test module under tests/ is in no allowlist, matches no documented exclusion in args/test_gating_gate.yaml, and is not in the grandfathered census args/ci_test_backlog.txt -- i.e. a NEW test file gated by nothing. `--prune-backlog` drops census lines that are now gated or gone. crx-test-05 added `--shard K/N`, which partitions the resolved list for the CI matrix -- crx-test-07 made that partition GREEDY LONGEST-FIRST BIN PACKING over the per-file durations in args/ci_test_timings/ (`partition()`, which computes the WHOLE partition and asserts multiset losslessness before handing back one shard, because a partition that drops files reports GREEN), falling back to the original ROUND-ROBIN (`files[k-1::n]`) whenever no snapshot is readable; a file absent from the snapshot is weighted at the MEDIAN and never dropped. Not contiguous chunks, which group tests/cortex/* into one shard, and never builtin `hash()`, because PYTHONHASHSEED is randomised per process so a hash partition would place a file in shard 2 on one runner and shard 4 on another, running some files twice and others never while reporting green. It narrows only the PRINTED targets: the floor, duplicate and existence checks always see the WHOLE list (a derived per-shard floor is strictly weaker), and it REFUSES to combine with --check-coverage or --prune-backlog, since sharding a census would silently shrink the ratchet guarding the ungated backlog. Optional pins: args/ci_test_files/shard_pins.txt. Policy: docs/ci/test-gating-policy.md. | --list {core,windows}, --check, --print, --out, --root, --json, --extract-workflow, --job, --min-targets, --check-coverage, --prune-backlog, --shard K/N | Resolved target list / validation report / coverage census |
| Shard Timing Snapshot | tools/ci/shard_timings.py | crx-test-07. Folds the `ci-junit-shard-*.xml` artifacts every gated shard already uploads into the per-FILE duration snapshot `gated_test_list.partition()` bin-packs by. Round-robin balanced FILE COUNT and said nothing about RUNTIME: on the first merged sharded pipeline (run 32352491214) shard 1 took 17m01s against 5m59s/5m43s/6m36s because it drew the repo-wide scanners, so `Test` cost 17 minutes to do ~7 minutes of work. Reads the JUnit XML and NOT `--durations`, which is a truncated top-25 of CALL time -- pytest's default junit_duration_report is `total`, so setup is included, and the four worst offenders spent 82.6s/33.3s/32.5s/26.8s in SETUP alone. ATTRIBUTION is resolved against the ALLOWLIST vocabulary because a JUnit classname is dotted and ambiguous (`tests.a.b` is either tests/a/b.py or a class in tests/a.py); the longest matching gated target wins and an unmatched classname is COUNTED in `unattributed`, never guessed, since a wrong path mis-weights one file AND leaves the real one imputed. REFUSES to write a snapshot with no attributed durations -- that would replace a good snapshot with one that silently reverts every shard to round-robin. `--balance` reports the partition the committed snapshot produces plus `lower_bound_seconds`, the heaviest single INDIVISIBLE unit: once the busiest shard sits at that bound the critical path is one file and raising N buys nothing (measured 2026-08-20: 699.2s of a 1791.2s suite is tests/cortex/test_chat_routing.py alone, 39% in four tests). `snapshot.json` is owned by the weekly .github/workflows/shard-timings.yml, which opens a PR; a task correcting one weight writes its own `<task-id>.json` fragment, merged newest-`generated_at`-wins, the same collision-free discipline core.d/ gave core.txt. | --from-junit, --write, --out, --name, --source, --generated-at, --show, --balance, --shards, --no-timings, --list, --root, --json | Per-file duration snapshot / merged-timing report / predicted shard balance |
| Changed-Test Isolation Run | tools/ci/isolation_run.py | trust-disc-02. Runs every test file the PR CHANGED in its own pytest process -- the "alone" half of "both alone and in-suite", which this pipeline never had (no test-order randomisation, no isolation run, in icdev-ci.yml, pytest.ini, setup.cfg or pyproject.toml). An order-dependent pass is otherwise invisible: a file is green because of what ran before it and stays green until an unrelated core.txt edit reshuffles the run. Resolves the changed set by diffing the merge base (needs `fetch-depth: 0`), filters through `gated_test_list.in_scope` so it and the census agree on what a test file is, and unions in uncommitted worktree edits so it is useful locally. Only ALLOWLISTED changed files can fail the job -- those are already green in-suite, so a standalone failure is real news; a changed file in no allowlist has never gated a merge and may be red for pre-existing reasons, so it is run and warned about instead. Exit 2 when the changed set cannot be resolved: a step that ran nothing must not read as green. | --run, --list, --base, --timeout, --root, --json, [-- pytest args] | Per-file alone/in-suite report; exit 0 clean, 1 gated failure, 2 unresolvable |
| Ungated Test Drift Reflex | tools/genesis/reflexes/ungated_test_drift.py | Genesis reflex (6h, green tier) that samples the test modules named in the `args/ci_test_backlog.txt` census -- the ones CI never runs -- and reports files that went from PASSING to FAILING. Transitions only: a file already failing is known debt and is not re-reported, and a run against a database with no baseline seeds silently instead of filing a card per file. Cards carry a deterministic id so a re-detection updates rather than duplicates, and a module that could not be run never overwrites a real verdict. Complements `tools/ci/gated_test_list.py --check-coverage`, which stops the census GROWING; this watches the files already in it. | (daemon-loaded via importlib) `run(trigger_data, context)` | `{success, metric_value, details}` -- checked / regressed / seeded counts |
| CI Skip Census | tools/ci/skip_census.py | trust-disc-03. `gated_test_list.py --check-coverage` answers "does CI run this file?" and nothing else; a gated file can be green on every PR and assert NOTHING, because it skipped -- tests/test_app.py's overview test did exactly that ("SQLite test DB lacks platform schema ... no such column: classification", the column the RLS predicate in get_connection() filters on, so every read of kanban_tasks raised) while the skip presented it as coverage. TWO HALVES: `--check` AST-scans every gated test file (plus the conftest.py beside it) for pytest.skip / importorskip / mark.skip[if] / unittest.skip* / skipTest and exits 1 on a site not enumerated BY NAME in args/ci_skip_census.txt with a written reason -- "flaky" and "TBD" are refused; `--from-report <junit.xml>` counts what the gated run ACTUALLY skipped, per file, and exits 1 on a gated file that skipped while declaring no site of its own (a conftest fixture or plugin the static scan cannot see). Census is ENUMERATED not counted, and `skip_census.skip_max` in args/test_gating_gate.yaml may only go DOWN. `--staged`/`--changed` is the pre-commit fast path; `--prune` drops entries whose site is gone; `--seed` is adoption-only and refuses to overwrite. Policy: docs/ci/test-gating-policy.md. | --check, --json, --changed, --staged, --from-report, --seed, --force, --prune, --root | Skip site census / per-file runtime skip report |
| Red-First Proof Gate | tools/ci/red_first_gate.py | trust-disc-01. ANVIL mandates RED -> GREEN and NOTHING ANYWHERE RECORDED THE RED -- a process instruction whose evidence is never captured is the `|| true` failure of D394 in a second form. For every test file the branch ADDS or MODIFIES, this checks out the merge base into a disposable `verify`-actor worktree, applies ONLY that test file on top, runs it there, and runs it here: it must NOT pass against the pre-change tree and must pass against this one. A changed test that still passes pre-change is either asserting current behaviour rather than required behaviour, or is not discriminating at all. The merge-base pytest output is captured into the JSON proof -- that artifact IS the recorded RED. Shares its decision table with `tools/security/reproduction_validator.py` (`decide_discrimination` + `DiscriminationVocabulary`) so the HTTP-replay gate and this one cannot drift apart on the one question they both ask; "fires" means "the predicate evaluated true" there and "the test FAILED" here. pytest exit 5 (no tests collected) is `no_tests`, never `passed` -- conflating those is the same error as the mandate this replaces. Exit codes: 0 clean, 1 a non-discriminating test, 2 the gate COULD NOT RUN (a gate that cannot run is not a gate that found nothing). Exemptions carry a written reason, never a bare count; a docstring-only edit is `not_applicable` because it adds no assertion. Policy: args/red_first_gate.yaml. | --base, --files, --root, --gate, --json, --out | Per-file discrimination proof + captured merge-base pytest output |
| Coordination Paths (shared exclusion list) | tools/git/coordination_paths.py | rem-hyg-07: the single source for "which repo paths are safe for two branches to touch at once", extracted from `tools/ci/pr_watcher.py` so the SEED-time check in `tools/kanban/lane_conflicts.py` and the merge-time `hold_on_sibling_conflict` guard ask the SAME question. A second divergent copy is worse than none: the two would report different collisions and no reader could tell which list was current. Keeps two reasons DELIBERATELY apart because they are different claims — `COORDINATION_PATH_MARKERS` are files many branches legitimately append to (only `tools/manifest*` and `args/ci_test_files/` are LITERALLY union-merged per `.gitattributes`; the rest is a heuristic about how the file is edited, and adding a path here does not make it auto-mergeable), while `GENERATED_PATH_MARKERS` are derived artifacts where a conflict is not a disagreement at all — re-running the generator over the merged tree yields the correct content. The generated class exists because ONE file (`docs/research/external-benchmark-map.generated.md`) deadlocked the whole board on 2026-08-09: every branch regenerated it, so the guard made every open PR a sibling of every other and refused all six. Import-free like `tools/kanban/gates.py`, so a CLI, watcher, seeder or Flask route can all import it. `pr_watcher` keeps its `_is_additive_path` / `_is_generated_path` names as re-exports, not a copy. | library — `is_coordination_path(path)`, `is_generated_path(path)`, `normalize(path)` | bool |
| Ungated Test Census | tools/ci/ungated_test_census.py | rem-tst-01. `gated_test_list.py --check-coverage` stops the ungated backlog GROWING and `ungated_test_drift.py` watches for regressions inside it, but nothing ever answered the question a promotion batch has to start from: which of the 1,794 modules in `args/ci_test_backlog.txt` are green TODAY? Runs every backlog module ALONE through `isolation_run.run_one` -- the same execution path the changed-test isolation run uses, so "alone" cannot mean two different things -- across a bounded worker pool, each child pinned to its own scratch `ICDEV_DB_PATH`/`PYTEST_DEBUG_TEMPROOT` and a root-only `PYTHONPATH` (these sessions inherit a PYTHONPATH aimed at the SHARED checkout, so an unpinned census silently measures another worktree's source). Four verdicts that may NOT be merged: `passed`, `failed`, `collection-error` (does not import -- an import fix, batchable with its siblings) and `no-tests` (pytest exit 5, which exits 0 and would read as green to anything that only checks the exit code -- promoting it widens the allowlist without widening coverage). `--deadline-s` caps wall clock and records every unstarted module as `not-reached`, and `--verify` re-derives measured + not-reached + out-of-scope == backlog size, so a partial census can never read as a complete one. MEASURES ONLY: it edits no allowlist, and it exits 0 whatever it finds -- a census that becomes a gate gets switched off before anyone reads it. It also measures only the ALONE half; a promotion batch still has to run the in-suite half itself. Artifacts: docs/testing/ungated_test_census.json + .md. rem-tst-04 adds the RED half: `--red-report` groups the `failed` rows by failure SHAPE (schema-drift, sql-dialect, import-or-attribute, http-auth, db-error-unspecified, error-dominant, assertion, runtime-exception, unclassified) rather than by raw first-failure text, which renders 93 reds as ~80 singletons and tells a fixer nothing. Group counts must sum to the census's own `failed` count or it exits 1. `unclassified` is a real bucket -- a classifier that always finds a home for a line pytest truncated is guessing, and `db-error-unspecified` is deliberately NOT folded into schema-drift, because those are different fixes. Artifact: docs/testing/ungated_red_modules.md. | --run, --limit, --workers, --timeout, --deadline-s, --out, --md, --checkpoint, --quiet, --summarize, --verify, --red-report, --red-md, --root | Per-module census JSON + markdown summary + grouped red report |
| Commands Doc Merge Rehearsal | tools/git/commands_doc_merge_rehearsal.py | kax-conflict-11. Scripted multi-branch git rehearsal that MEASURES whether concurrent appends to `docs/reference/commands.md` merge without a human. Third in the series after `manifest_merge_rehearsal.py` and `ci_test_list_merge_rehearsal.py`, and needed because this file is BLOCK-structured (fenced ```bash regions) rather than a table of rows, so "obviously append-only" was an assumption worth measuring. Scenarios: `append_tail` and `append_section` (the observed pattern — 14 of 14 recent branches that touched the file were PURE additions, none edited an existing line), plus `edit_same_line` as the diagnostic control union is NOT safe for. Runs both merge paths — `worktree` (local `git merge`) and `merge-tree` (bare, forge-side). Also checks the ```bash fences stay balanced and no block is dropped. `--without-union` inverts the gate: every scenario must CONFLICT without the rule, so a rehearsal that never creates a collision cannot report success. Measured 2026-08-16: clean at 2/3/5 branches in both paths. | --scenario, --mode {worktree,merge-tree}, --branches, --source, --gate, --without-union, --json | Per-scenario clean/conflicted report + `gated_clean` |
| Ungated Test Promoter | tools/ci/gate_promoter.py | rem-tst-06: `ungated_test_census.py` measures which of the ~1,700 ungated modules pass and deliberately promotes nothing; NOTHING consumed that measurement, so the committed snapshot aged (three days stale when this was written) and the backlog shrank only when a human hand-moved a file. This consumes it. A module is promoted ONLY when green BOTH ways: re-run ALONE in its own process (the census result is re-verified, never trusted — it may be days old and from a different tree) AND IN-SUITE, appended to the gated set and run in ONE process with it. Green-alone is NOT green-in-suite and this repo has the scar twice: four cortex/dashboard modules that flipped depending on run order via a shared app singleton, and kpr-watch-03 on 2026-08-19, whose CI failure read as flake and was an order dependency. FAIL-CLOSED ON THE BATCH: if the in-suite run fails, NOTHING from that batch is promoted — the failure may be an interaction BETWEEN two survivors, so promoting 'the innocent ones' could ship exactly the interacting pair; re-run with a smaller --limit to isolate. Writes a PER-RUN `core.d` fragment (never core.txt, measured by tsg-policy-03 as the repo's largest merge-collision surface), drops the modules from args/ci_test_backlog.txt, and RATCHETS `backlog_max` DOWN — it can never raise it. Report-only without --apply; never a gate. Scheduled weekly by .github/workflows/gate-promoter.yml, which opens a PR rather than pushing, so the promotion is proved by the same gated suite it modifies. | `--limit` / `--apply` / `--plan` / `--census` / `--json` | `{measurable, backlog_size, census_green, candidates[], promoted[], rejected[], in_suite, backlog_max}` |
| Closed-Census Growth Gate | tools/ci/census_growth.py | cef-ci-02. `gated_test_list.py --check-coverage` enforces `args/ci_test_backlog.txt` by COUNT, and a count is exactly what an ENUMERATED census exists to distrust -- the file's own policy comment says so ("a bare count can be held constant while the set churns"). Identity was tracked and never RATCHETED: nothing compared either closed census against its previous self, so the numeric ceiling's slack was the whole guard. Measured on main at 42f7ea894 that slack was 8 slots (`backlog_max` 1711 against 1703 entries, while `skip_max` was 81 against 81) -- enough to delete the core.d fragments naming eight gated CEF suites, among them tests/cortex/test_resolve_facade.py and test_resolve_trust_loop.py, append those paths to the census, and have --check-coverage report "446 gated ... 1711 grandfathered (ceiling 1711), 0 unlisted" and exit 0. Set-compares `args/ci_test_backlog.txt` and `args/ci_skip_census.txt` against the MERGE BASE: a closed census may LOSE names and must never GAIN one. Catches the swap case a ceiling structurally cannot -- one line out and one in, count unchanged, a suite gone from CI. Deliberately NOT a tighter ceiling: "ceiling must equal the count" red-lights main whenever two concurrent PRs each gate a backlogged file and each lower it by one, and census deletions land about five times a day here; set monotonicity has no such interaction because two PRs that each gained nothing merge to a tree that gained nothing. Reuses `isolation_run.resolve_base` and both existing census parsers rather than writing a third copy of either. SURVEYED BEFORE ARMING per the CLAUDE.md rule: every commit touching args/ci_test_backlog.txt since adoption (ceb10709b) is +0 -- 35 commits, 150 deletions, ZERO additions, so it would have refused nothing in the census's history; args/ci_skip_census.txt has had no post-adoption commit, so its rate is reported UNMEASURABLE rather than as a clean zero. A census ABSENT at the base is `introduced`, not grown. Exit 2 = could not compare (shallow checkout, needs `fetch-depth: 0`) and stays red: a gate that could not run is not a gate that found nothing. args/undeclared_import_census.txt and args/kanban_raw_insert_census.txt are the same discipline and are deliberately NOT registered -- unrelated to test gating and unsurveyed; adding one is a `CENSUSES` entry plus its own survey. | --check, --base, --root, --json | Per-census added/removed/introduced report + surveyed fire rate; exit 0 clean, 1 gained a name, 2 unresolvable |
| E2E Promotion Survey | tools/ci/e2e_flake_survey.py | crx-test-06. Answers whether `E2E (Playwright)` is reliable enough to become a REQUIRED check, and refuses to answer it with one number. Until crx-test-05 the job declared `needs: [test]`, so it only ever ran on branches whose unit suite had ALREADY passed and GitHub SKIPPED it on the rest -- the 25/25 green everyone quoted was drawn from the healthiest branches in the population, and the unhealthy ones were absent from the DENOMINATOR, the one place an absence is invisible. Splits the population STRUCTURALLY (a post-unblock run carries an `E2E Shard k of N` job -- a fact about the run itself, so it cannot drift the way a hardcoded cutoff date would) and never merges success / failure / cancelled (an infrastructure event, not a verdict) / skipped (never ran) / in_progress. `flake_rate` is None, NEVER 0.0, when nothing was exercised: 'measured clean' and 'never measured' justify opposite decisions. The biased pre-unblock block is reported but forced to `biased_ineligible` so it cannot print a state a reader would quote. REPORT ONLY with deliberately no --gate (kpr-fix-03: a survey shipped with a gate earns a `|| true`); exit 2 when the survey could not be produced, which is not the same as a clean survey. Claims: args/e2e_promotion.yaml. | --json, --limit, --from-json, --config | Per-population flake report + promotion_supported verdict |
| Born-Red Ungated Test Survey | tools/ci/born_red_survey.py | rem-hyg-14: `ungated_test_drift.py` reports TRANSITIONS (`was == 'pass' and now 'fail'`), so a file whose FIRST observation is a failure takes the `was is None` branch, seeds a 'fail' baseline and is never mentioned again — a test broken since the day it was written is structurally invisible to the one reflex watching the ungated backlog. Measured: `tests/test_proposals_ptw_blackhat_api.py` was 10/20 red from the day it landed (2026-07-07) and stayed red for six weeks. `gate_promoter` (rem-tst-06) drains the GREEN half of the backlog weekly; this measures the RED half so those files get fixed or deleted rather than sitting as invisible debt. FIVE states, never merged: `born_red` (every observation has been a failure — the finding), `regressed` (observed passing once; the drift reflex's half, deliberately not re-reported here), `history_unknown` (failing, no recorded first verdict — pre-migration rows), `passing`, and `unobserved` (NOBODY HAS EVER RUN IT — the drift reflex samples 40 files per 6h so a full sweep takes over ten days, and folding those into "no findings" is the exact reassurance this tool refuses). `born_red_count` is `None`, never 0, on a deployment that has recorded nothing. TWO DURATIONS, never merged: `observed_red_days` is PROVEN (seen failing then, not seen passing since) while `file_age_days` is an upper bound, and `red_days_basis` states which the rank used — `confirmed_at_birth` / `file_age_upper_bound` / `observed_only` / `refuted_at_birth`. `--confirm N` runs the top N at the commit that landed them on the default branch (`--first-parent`, so the tree is the one the file actually had to work in, not the feature branch that was green alone) in a detached worktree; three outcomes and the middle one is its own finding — `confirmed_born_red`, `passed_at_birth` (worked at landing, broke later SILENTLY: a regression the drift reflex missed because it never observed the pass) and `birth_unrunnable` (pytest exit 2/3/4/5 on the old tree — a statement about that checkout's dependencies, counted as neither). WRITES NOTHING: the reflex stays the only writer of `ungated_test_baseline`. Report only, deliberately no `--gate` (kpr-fix-03); exit 2 = the survey could not be produced, which is never a clean survey. | `--json` / `--out` / `--limit` / `--run N` / `--confirm N` / `--timeout` / `--root` | `{state, backlog_total, observed, unobserved, coverage_pct, counts{}, born_red_count, broke_after_birth_count, confirmations{}, findings[]}` |
| Gate Promoter Report Renderer | tools/ci/gate_promoter_report.py | Renders `gate_promoter` JSON into a commit message and a PR body. Split out of `.github/workflows/gate-promoter.yml` deliberately: multi-line prose built inside a YAML `run:` block is how that workflow became invalid YAML the first time — a heredoc whose body sits at column 0 terminates the block scalar, and the failure surfaces as a scanner error a hundred lines from the mistake. Rendering here also makes the text testable, which text inside a workflow never is. | `--from <report.json>` / `--commit-message <path>` / `--pr-body <path>` | Commit message and PR body text (stdout or the given files) |
