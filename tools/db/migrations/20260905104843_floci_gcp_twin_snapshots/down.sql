-- CUI // SP-CTI
-- Down for 20260905104843_floci_gcp_twin_snapshots (flx-gcp-01).
-- Not append-only: a snapshot of an emulator is a re-derivable observation of a
-- disposable local container, not an audit record.
DROP TABLE IF EXISTS floci_gcp_twin_snapshots;
