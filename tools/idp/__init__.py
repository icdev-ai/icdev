# CUI // SP-CTI
"""IDP — ICDEV Internal Developer Portal.

Catalog-plus-scorecard surface over ICDEV's own registered components.

Modules:
    component_facts — one fact row per registered component (the scorecard input)
    scorecard       — scorecard-as-code: YAML ladder + IQE rule expressions
"""
