# CUI // SP-CTI
"""Canonical asset identity (rmf-ident-01).

One row per physical asset, carrying a resolver onto each of the three
stacks that key an asset differently: DoD 7-pillar ZTA (project_id), NSA ZIG
(sha256(hostname)) and NDC/PVM (ni_devices.id).
"""
