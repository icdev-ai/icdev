# CUI // SP-CTI
"""ICDEV Internal Developer Portal (IDP).

Scorecard-as-code over the unified component registry. The rule language is
IQE (``tools/iqe``), not a bespoke DSL: a scorecard rule is an IQE query
evaluated against the ``idp.components`` collection.
"""
