# FathomDesk Trading Engine

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## FathomDesk Trading Engine
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Durable Compounders Scanner | tools/trading/analysts/durable_compounders.py | 3-pillar scanner (Balance Sheet 35%, Customer Loyalty 35%, Cycle Resilience 30%) with SHA-256 hash-gated cache (7-day TTL in ad_durable_compounders_cache) and drift detection (per-field threshold alerts). CLI: `--scan --limit 20 --min-score 65 --tier fortress --json`; `--drift --json`. API: GET /api/quality-scores/durable-compounders + /drift. Dashboard: "Durable Compounders" tab on /quality-scores. | --scan [--force] [--limit N] [--min-score N] [--tier fortress\|solid\|watchlist] [--json] | Scored companies with cache_hits/recomputed/drifted telemetry |
| Durable Compounders Migration | tools/db/migrate_durable_compounders.py | Creates ad_durable_compounders_cache (ticker PK, 3-pillar scores, fundamentals_hash, computed_at, drift_flagged, drift_reason). Idempotent. | python tools/db/migrate_durable_compounders.py | Migration result JSON |
| Fundamental Analyst | tools/trading/analysts/fundamental.py | Fundamental analysis agent (SMA, valuation, trends) | --json | Analysis results |
| Sentiment Analyst | tools/trading/analysts/sentiment.py | Keyword-based sentiment analysis agent | --json | Sentiment scores |
| News Analyst | tools/trading/analysts/news.py | News analysis agent that categorizes market news into earnings, macro, sector, regulatory, and corporate action keywords | --json | Categorized news signals |
| Quality Scorer | tools/trading/analysts/quality.py | 7-dimension fundamental quality scorer (valuation, profitability, growth, balance sheet, capital allocation, moat, PE/NAV mispricing) with Piotroski F-Score composite and implied-ROE gap analysis. Model v2: `compute_pe_nav_metrics(f)` → implied_roe/roe_gap/nav_quadrant/sector_tier; `nav_tier_for_sector(sector)` → 1/2/3 fidelity tier. `--sample --json` for demo. | --ticker SYM [--sample] [--save] [--population] [--json] | Quality scores with pe_nav_analysis block |
| Technical Analyst | tools/trading/analysts/technical.py | Pure-Python technical analysis agent: RSI, EMA, and other indicators without external TA-Lib dependency | --json | Technical signals |
| Market Data | tools/trading/data/market_data.py | Alpaca market data fetch and cache layer | --json | Market data |
| Perspective Scorer | tools/trading/analysis/perspective_scorer.py | ICDEV™'s INTaaS bull/bear multiperspectivity scorer | --json | Perspective scores |
| Signal Generator | tools/trading/analysis/signal_generator.py | Weighted composite signal generator | --json | Trading signals |
| Order Manager | tools/trading/execution/order_manager.py | Multi-broker order placement and tracking. Routes via `tools/trading/brokers/router.py` to the first available configured broker (Alpaca → Tradier → Coinbase Advanced → Kraken) or an explicit `provider=` arg. Simulation fallback when no broker is configured. | --json | Order status |
| Broker Router | tools/trading/brokers/router.py | Multi-broker factory (`get_broker(provider, user_id)`) and capability registry. Auto-selects first available broker for equities or crypto. `list_available_brokers()` returns status of all 4 supported brokers. | N/A (library) | Adapter instance / broker list |
| Tradier Adapter | tools/trading/brokers/tradier_adapter.py | Tradier paper/live REST adapter — stdlib-only, bearer-token auth. Paper sandbox at `https://sandbox.tradier.com/v1`; live at `https://api.tradier.com/v1`. Equities and options (single-leg + option chain). Credentials: `TRADIER_ACCESS_TOKEN` + `TRADIER_BASE_URL`. | python tools/trading/brokers/tradier_adapter.py --account | Account / quote |
| Coinbase Advanced Adapter | tools/trading/brokers/coinbase_adapter.py | Coinbase Advanced Trade API v3 adapter — stdlib-only, HMAC-SHA256 signed (CB-ACCESS-KEY/SIGN/TIMESTAMP). Crypto spot only, live only (no sandbox). Credentials: `COINBASE_API_KEY` + `COINBASE_API_SECRET`. | python tools/trading/brokers/coinbase_adapter.py --account | Account / candles |
| Kraken Adapter | tools/trading/brokers/kraken_adapter.py | Kraken REST adapter — stdlib-only, HMAC-SHA512 signed (API-Key/API-Sign). Crypto spot only, live only (no sandbox). Credentials: `KRAKEN_API_KEY` + `KRAKEN_API_SECRET` (base64-encoded). | python tools/trading/brokers/kraken_adapter.py --account | Balance / ticker |
| Schwab Adapter | tools/trading/brokers/schwab_adapter.py | Charles Schwab / thinkorswim REST adapter (replaced TD Ameritrade API, sunset Sep 2024) — stdlib-only, OAuth2 bearer token. Equities + options, live only. Access token stored as `api_key`, refresh token as `secret`. Auto-refreshes on 401 if client_id/secret available. Credentials: `SCHWAB_ACCESS_TOKEN` + `SCHWAB_REFRESH_TOKEN`. Note: Fidelity has no public API. | python tools/trading/brokers/schwab_adapter.py --account | Account / quote |
| NinjaTrader 8 Adapter | tools/trading/brokers/ninjatrader_adapter.py | NinjaTrader 8 ATI (Automated Trading Interface) TCP adapter — stdlib-only, local socket connection. Equities, options, and futures (ES, NQ, CL, etc.). `is_available()` probes port 36973 without sending a command. Account name stored as `api_key`; host:port as `base_url`. Paper = "Sim101". NT8 must be running locally with ATI enabled (Tools → Options → ATI). Helpers: `equity_symbol("MSFT")` → "MSFT STOCK", `futures_symbol("ES","SEP",2024)` → "ES SEP24". | python tools/trading/brokers/ninjatrader_adapter.py --accounts --json | Account / position |
| Tradovate Adapter | tools/trading/brokers/tradovate_adapter.py | Tradovate REST adapter — stdlib-only, cloud-based futures broker (no local install). Username+password → Bearer token (90-min TTL, auto-renewed at 75 min). Supports CME/CBOT/NYMEX/COMEX futures (ES, NQ, RTY, YM, CL, GC, ZB, 6E, etc.). Demo at `demo.tradovateapi.com/v1`, live at `live.tradovateapi.com/v1`. Market data (quotes/bars) via `md.tradovateapi.com/v1`. `api_key` = username, `secret_key` = password. | python tools/trading/brokers/tradovate_adapter.py --accounts --json | Account / positions / bars |
| Signal Decay | tools/trading/signal_decay.py | Exponential decay weighting for trading signals. `compute_signal_decay(age_days, decay_lambda=0.05)` returns e^(-λ*age) clamped at WEIGHT_FLOOR (0.01). `batch_decay(signals)` applies weight in-place to a list of signal dicts. Constants: DECAY_LAMBDA=0.05, WEIGHT_FLOOR=0.01. | N/A (library) | float / mutated list |
| IBKR Adapter | tools/trading/brokers/ibkr_adapter.py | Interactive Brokers Client Portal Gateway v1 adapter — stdlib-only, session-based auth (no API key). Gateway must be running locally and authenticated via browser. SSL cert verification disabled for localhost (self-signed). Supports equities, options, futures, crypto, forex, bonds. `is_available()` probes /v1/api/iserver/auth/status. `base_url` stored as credential (default `https://localhost:5000`). Paper accounts have DU-prefixed account IDs. | python tools/trading/brokers/ibkr_adapter.py --accounts --json | Auth status / account list |
| tastytrade Adapter | tools/trading/brokers/tastytrade_adapter.py | tastytrade REST adapter — stdlib-only, username+password → session token auth (POST /sessions). Session token cached on instance with auto-retry on 401. Sandbox via `api.cert.tastyworks.com`. Supports equities, options (full chains), futures, crypto. `api_key` = username, `secret_key` = password. `dry_run_order()` validates orders without submitting. | python tools/trading/brokers/tastytrade_adapter.py --accounts --json | Account / positions / option chains |
| Position Tracker | tools/trading/execution/position_tracker.py | Position synchronization with Alpaca | --json | Position data |
| Risk Checker | tools/trading/execution/risk_checker.py | Pre-trade risk validation (position limits, VaR) | --json | Risk assessment |
| Pulse Article Generator | tools/trading/pulse/article_generator.py | Pulse article generator from analysis results | --json | Article draft |
| Portfolio Strategist | tools/trading/strategist/portfolio_strategist.py | Autonomous long-term investment strategy agent — 4-tier allocation (core/tactical/opportunistic/hedge) from multi-timeframe performance, macro regime, KG centrality, scenario resilience, expert consensus | --run --json | Strategy allocation |
| Trading Runner | tools/trading/runner.py | Main orchestrator for FathomDesk trading engine analysis cycles — full lifecycle: analyze → persist → queue signal → trigger Pulse article. Runs 5-layer DAG (macro, analysts, debate, signal, risk/approval) for a ticker. | --ticker SYM, --json | Full analysis result with run_id, signal, confidence, signal_id, article_id |
| Trading DB | tools/trading/db.py | FathomDesk database layer — persistent storage for portfolios, positions, orders, signals, and analysis runs (ad_ prefix tables) | N/A (library) | DB connection/helpers |
| Workflow Builder | tools/trading/workflow.py | DAG workflow builder for FathomDesk — constructs 5-layer analysis DAG (analysts → debate → signal → risk → approval) for a given ticker | ticker | Workflow dict |
| News DB | tools/trading/news/db.py | DDL + CRUD helpers for ad_news_items, ad_news_scenario_links, ad_news_clusters tables (all append-only). CLI: --migrate, --json | --migrate --json | Table list / item count |
| RSS Ingestor | tools/trading/news/rss_ingestor.py | Polls RSS/Atom feeds from args/news_feeds.yaml, deduplicates by sha256(source\|\|link)[:16], stores in ad_news_items. Per-feed exception isolation. HTML-strips summaries (OPT-58). | --once \| --start [--interval N] [--json] | Ingest summary JSON |
| News Classifier | tools/trading/news/classifier.py | Rule-based news classifier: category, impact_level, net_direction, mentioned_tickers from keyword maps in analysts/news.py | --backfill-all \| --id ID [--json] | Classification stats |
| Scenario Matcher | tools/trading/news/scenario_matcher.py | Maps news items to scenario_engine SCENARIO_TEMPLATES via keyword matching. Wires match_and_run() to scenario_engine.run_scenario() | --batch \| --id ID [--run] [--json] | Match results |
| News Aggregator | tools/trading/news/aggregator.py | Clusters news by (category, scenario_key), computes cumulative scores with time decay and source reliability, promotes through emerging/cluster/regime tiers | --cluster --promote [--window N] [--json] | Cluster stats |
| News Reasoner | tools/trading/news/news_reasoner.py | INTaaS multiperspective intelligence: author intent, omission detection, macro contradiction detection, cross-signal divergence. LLM-powered (qwen3.5) with deterministic fallback | --item ID \| --cluster CAT \| --divergences [--json] | Intelligence assessment |
| News Entity Extractor | tools/trading/news/entity_extractor.py | Keyword-based extractor that pulls KG-matchable subjects (countries, commodities, tickers, private companies, event hints) from news headline+summary text. Powers the supply-chain impact trace on the /news page. No LLM — fully deterministic, grounded in KG node table. | text string (CLI: positional args) | dict with countries, commodities, tickers, private_companies, event_hints |
| News Pattern DB | tools/trading/news/pattern_db.py | DDL + CRUD for append-only `ad_news_patterns` table — stores typed pattern detections (regime_shift, crackdown, etc.) from NewsPatternAnalyzer. Helpers: `ensure_tables`, `insert_pattern`, `list_patterns`, `latest_pattern`. Cooldown-aware dedup via `latest_pattern` timestamp check. CLI: --migrate, --json | --migrate \| --json | Pattern list / count |
| News Pattern Analyzer | tools/trading/news/pattern_analyzer.py | Detects news cluster patterns from recent `ad_news_items` and emits typed pattern dicts: `regime_shift` (≥70% bearish items in a category, min 4 items in 24h) and `crackdown` (≥4 bearish regulatory items citing the same ticker). Cooldown guard prevents re-emission within 4h. Persists via pattern_db. No LLM — fully deterministic. | --analyze [--json] | List of pattern dicts with pattern_type, category, severity, recommendation |
| Trading Oracle | tools/trading/oracle/runner.py | Anticipatory intelligence engine — 6-lens prediction system (Signal Convergence, News Intelligence, Regime Trajectory, Portfolio Stress, News Impact, Fundamental Mispricing). Detects multi-lens convergence events, persists predictions with confidence + horizon, daemon reflex every 30m. | --sweep \| --predictions \| --convergence [--json] | Predictions, convergence events, recommended actions |
| Fundamental Mispricing Lens | tools/trading/oracle/lens_fundamental_mispricing.py | Trading Oracle lens: PE/NAV implied-ROE gap framework. Signals: `fundamental_undervalued` (roe_gap > 4%), `fundamental_overvalued` (roe_gap < −4%), `nav_premium_warning` (fully_priced quadrant + near-zero gap). Sector-tier-weighted confidence (Tier 1=1.0×, 2=0.8×, 3=0.55×). Regime-modulated (momentum penalty, value/neutral boost). | N/A (library; registered in oracle runner) | List of TradingPrediction with direction, confidence, roe_gap evidence |
| News Impact Lens | tools/trading/oracle/lens_news_impact.py | Trading Oracle lens that forecasts ticker moves from aggregated news-driven supply-chain impact. Reads `ad_news_impact_traces`, bands severity (medium/high/critical), scores confidence from magnitude+diversity+volume, and emits macro geopolitical danger prediction when danger score ≥ 40. | N/A (library; `python tools/trading/oracle/lens_news_impact.py` for direct run) | List of TradingPrediction dicts with confidence, severity, direction, recommended_action |
| Systemic Radar | tools/trading/market_intel/systemic_radar.py | Systemic Risk & Opportunity Radar (SROR) — aggregates 8 indicator families (market stress, macro leading, monetary liquidity, news sentiment, supply chain/geo, cross-asset divergences, institutional flow, historical patterns) across 3 time horizons (immediate/near-term/medium-term) into composite danger/opportunity scores with regime classification. 100% deterministic, no LLM. | --compute \| --latest \| --history \| --alerts [--json] | Composite scores, regime, family breakdown, alerts |
| Cross-Asset Divergence | tools/trading/market_intel/cross_asset_divergence.py | SROR Family 6 detector — flags 5 classic dislocation patterns: stocks-vs-bonds, gold+dollar both rising, oil crash vs equity rally, VIX-equity divergence, breadth vs index, credit vs equity. Deterministic. | --detect [--json] | Divergence list + aggregate scores |
| Crisis Fingerprints | tools/trading/market_intel/crisis_fingerprints.py | SROR Family 8 historical pattern matcher — counts 7 recession precursors, computes cosine similarity to 2008 GFC / 2020 COVID / 2022 rate shock fingerprints, extracts HMM transition probabilities to risk-off states. | --analyze [--json] | Precursor count, similarity scores, HMM risk probs |
| Cross-Asset Rotation | tools/trading/market_intel/cross_asset_rotation.py | 7-ratio inter-market rotation engine with 3-week and 8-week OLS trend slopes: gold/S&P, copper/gold, bonds/equity, oil/equity, DXY/commodity, gold/oil, real yield. Multi-signal rotation scoring (0–5): ≥3 → ROTATION_ALERT, ≥4 → HARD_EXIT. Deterministic — yfinance + FRED fallback to sample data. CLI: `--compute [--json]`. | --compute [--json] | Rotation dict with 7 ratios, trend labels, magnitude, rotation_signal |
| Expert Advisory Agents | tools/trading/market_intel/expert_agents.py | 6-agent Committee of Investment Specialists (CIS) — Fundamental, Technical, Sentiment, Macro, Quant, Risk scorers derive deterministic conviction levels from pipeline outputs (signals, analyst runs, macro context) without LLM calls. `run_expert_analysis(ticker)` → per-agent opinions. `synthesize_recommendation(ticker, opinions)` → CIS verdict. Owns 5 DB tables: ad_expert_opinions, ad_cis_recommendations, ad_daily_briefs, ad_risk_profiles, ad_expert_recommendations. | `run_expert_analysis(ticker)`, `synthesize_recommendation(ticker, opinions)` | List of opinion dicts / CIS recommendation dict |
| Bond ETF Data | tools/trading/data/bond_etf_data.py | Weekly bond ETF data layer (TLT, HYG, LQD, AGG + SPY benchmark) via yfinance. Computes 4-week and 13-week momentum, classifies bond market regime: CREDIT_STRESS (HYG lags LQD >5pp), DURATION_SELLOFF (LQD+TLT negative + SPY positive), FLIGHT_TO_SAFETY (TLT positive + SPY negative), NEUTRAL. Deterministic fallback to SHA-256-seeded sample anchors. Entry points: `get_bond_etf_snapshot()`, `classify_bond_etf_regime()`. | N/A (library) | `{regime, active_signals, momentum}` |
| Macro Data | tools/trading/data/macro_data.py | Layer 0 macro context provider — fetches FRED + yfinance indicators (fed funds, CPI, M2, yield curve, ISM, credit impulse, geopolitical risk, supply chain risk, stagflation/deflation risk, sector impacts). `fetch_macro_context()` → GREEN/YELLOW/RED regime. `fetch_extended_macro()` → additional valuation signals (Fear & Greed, Buffett indicator, QE/QT, fiscal deficit). `compute_fear_greed()` → composite 8-component fear/greed index 0–100. `compute_buffett_indicator()` → Wilshire/GDP ratio + signal. 100% deterministic with sample fallback. | N/A (library; `fetch_macro_context()`, `fetch_extended_macro()`, `compute_fear_greed()`, `compute_buffett_indicator()`) | Macro context dict with regime + 30+ indicators |
| Donchian Channel Detector | tools/trading/ta/donchian.py | Computes upper, lower, and mid Donchian bands over a rolling window of n bars; returns a DonchianSignal with breakout direction (up/down/none) and band prices. No LLM — fully deterministic. | N/A (library; `detect_donchian_breakout(prices, n=55)`) | `DonchianSignal(direction, upper, lower, mid)` |
| Liquidity Trap Detector | tools/trading/ta/macro_liquidity.py | Pure-Python macro liquidity-trap detector. Returns `active=True` when all three conditions hold: `fed_funds < 1.0`, `m2_velocity_change < 2.0`, `cpi_yoy < 2.5`. Confidence is the fraction of conditions satisfied (0.0–1.0). No LLM — fully deterministic. | N/A (library; `detect_liquidity_trap(macro_data)`) | `{"active": bool, "confidence": float}` |
| Trap Detector | tools/trading/ta/traps.py | Detects bull and bear trap patterns from OHLCV bar data. Bull trap: weak-volume breakout above S/R resistance + re-entry within `max_reentry_bars` + lower high. Bear trap: mirror (weak-volume breakdown below support → recovery → higher low). Requires ≥5 bars. Calls `swings.py` + `sr.py` internally. No LLM — fully deterministic. | N/A (library; `detect_traps(bars, max_reentry_bars=3, volume_threshold=0.7)`) | `[{"pattern": "bull_trap"\|"bear_trap", "broken_level": float, "breakout_bar": int, "reentry_bar": int, "lower_high_bar"\|"higher_low_bar": int, "volume_ratio": float, "confidence": float}, ...]` |
| Swing Detector | tools/trading/ta/swings.py | Threshold-based zigzag swing high/low detector. Two-phase algorithm: confirms initial direction via a threshold_pct move from the running extreme, then scans for alternating reversals. Output guaranteed to alternate high/low. No LLM — fully deterministic. | N/A (library; `find_swings(bars, threshold_pct=1.5)`) | `[{"type": "high"\|"low", "price": float, "bar_index": int}, ...]` |
| Support/Resistance Detector | tools/trading/ta/support_resistance.py | Clusters swing highs/lows (from swings.py) into S/R levels and scores their strength. Merges touches within cluster_pct% of the running centroid; normalises strength 0.0–1.0 against the max-touch cluster. Calls find_swings automatically when swings not pre-supplied. No LLM — fully deterministic. | N/A (library; `compute_sr(bars, swings=None, cluster_pct=0.5)`) | `[{"price": float, "strength": float, "touches": int, "type": "support"\|"resistance"}, ...]` sorted by strength desc |
| Double Pattern Detector | tools/trading/ta/patterns/double.py | Detects double-top and double-bottom chart patterns. Scans consecutive swing triplets (same-kind, opposite-kind, same-kind); both same-kind swings must be within tolerance_pct of their mean. Returns start/end bar indices, neckline swing, and avg_price. No LLM — fully deterministic. | N/A (library; `detect_double(bars, swings=None, tolerance_pct=3.0)`) | `[{"type": "double_top"\|"double_bottom", "high_1"\|"low_1", "high_2"\|"low_2", "neckline", "start_bar", "end_bar", "avg_price", "tolerance_pct"}, ...]` |
| Triple Pattern Detector | tools/trading/ta/patterns/triple.py | Detects triple-top and triple-bottom chart patterns. Requires ≥5 total swings; matches three consecutive same-kind swings whose prices all fall within tolerance_pct of their mean. Higher-conviction signal than double patterns. No LLM — fully deterministic. | N/A (library; `detect_triple(bars, swings=None, tolerance_pct=3.0)`) | `[{"type": "triple_top"\|"triple_bottom", "swing_1", "swing_2", "swing_3", "start_bar", "end_bar", "avg_price", "tolerance_pct"}, ...]` |
| Wedge Pattern Detector | tools/trading/ta/patterns/wedge.py | Detects rising-wedge and falling-wedge chart patterns via OLS regression on swing highs (resistance) and swing lows (support). Rising wedge: both slopes positive, support steeper. Falling wedge: both slopes negative, resistance less steep. Returns slope/intercept for both trendlines + bar range. No LLM — fully deterministic. | N/A (library; `detect_wedge(bars, swings=None, min_touches=2)`) | `[{"type": "rising_wedge"\|"falling_wedge", "slope_high", "intercept_high", "slope_low", "intercept_low", "start_bar", "end_bar"}]` |
| Regime Lens | tools/trading/market_intel/regime_lens.py | Single source of truth for SROR + news context. Returns normalized RegimeContext with helper multipliers (position_size_multiplier, short_margin_multiplier, buy_confidence_floor, hedge_tier_floor, opportunistic_tier_cap, threshold shifts). Consumed by runner.py, signal_generator.py, risk_checker.py, portfolio_strategist.py to make every layer regime-aware. | [--json] | RegimeContext dict |
| Alerts DB | tools/trading/alerts/db.py | Storage layer for user-defined FathomDesk alerts — DDL + CRUD for `ad_alert_rules` (rule definitions: price_pct_change, composite_score, confidence, signal_direction, news_impact, regime_change) and `ad_alerts_log` (append-only fired-alert log). Supports SQLite + PostgreSQL via get_connection(). | N/A (library) | DB connection/helpers |
| Alert Evaluator | tools/trading/alerts/evaluator.py | Daemon-cadence (1m) rule evaluator — for each enabled alert rule: computes current value (price %, composite score, confidence, news impact, regime, signal direction), applies comparison + threshold, fires insert into `ad_alerts_log` and updates `last_triggered_at` if triggered and outside cooldown. Phase 4 follow-up: virtual subjects (WATCHLIST_ANY/AVG/TOP, PORTFOLIO_DRAWDOWN/DRIFT) resolve at evaluation time via `virtual_subjects.resolve_for_rule()`. Public entry point: `evaluate_all(user_id)` returns `{evaluated, fired, skipped_cooldown, errors, at}`. | N/A (library; import evaluate_all) | Summary dict |
| Virtual Alert Subjects | tools/trading/alerts/virtual_subjects.py | Phase 4 follow-up — resolves placeholder subjects (WATCHLIST_ANY, WATCHLIST_AVG, WATCHLIST_TOP, PORTFOLIO_DRAWDOWN, PORTFOLIO_DRIFT) referenced by persona alert_defaults. WATCHLIST_ANY expands to the user's watchlist and fires once per tick on the first triggering ticker (no alert spam). Aggregates compute a single scalar: WATCHLIST_AVG/_TOP fold over `ad_signals` composite scores; PORTFOLIO_DRAWDOWN reads `ad_pf_daily_snapshots` peak-vs-current; PORTFOLIO_DRIFT delegates to `analytics.rebalance.build_plan().summary.total_abs_drift_pp`. Returns `{kind: not_virtual|expand|aggregate|skip, ...}`. | `resolve_for_rule(rule, user_id)` | Resolution dict |
| Page Readings | tools/trading/analytics/page_readings.py | Deterministic reading engines for 8 FathomDesk dashboard pages (/market, /signals, /news, /radar, /oracle, /evolution, /advisor, /strategist). Each generator returns a standardized dict `{profile, summary, observations, considerations, regime, tone_counts}` consumed by the shared JS `renderReading()` helper. Thresholds hot-reloaded from `args/reading_config.yaml` via `reading_config.py`. No LLM — fully deterministic. | N/A (library; import generate_*_reading functions) | Reading dict with profile, observations, considerations, regime, tone_counts |
| Portfolio Reading | tools/trading/analytics/portfolio_reading.py | Deterministic PM-style narrative reading of a portfolio given risk/return metrics, current holdings, and macro regime. Classifies beta, Sharpe, outperformance, and drawdown; produces profile (one-line characterization), bulleted observations with tone+evidence, PM-style considerations, and regime alignment (match/mismatch/neutral). Thresholds hot-reloaded from `args/reading_config.yaml` via `reading_config.py`. No LLM — fully deterministic. | N/A (library; import generate_reading) | Dict with profile, observations, considerations, regime, summary, tone_counts |
| Portfolio PDF | tools/trading/analytics/portfolio_pdf.py | ReportLab Platypus PDF renderer for the Portfolio Brief — mirrors the /portfolio dashboard view with profile, snapshot stats, risk metrics (Sharpe/Sortino/Calmar/drawdown/alpha/beta), tone-colored observations, considerations, and holdings table. Supports tenant white-label branding (logo + name injection). Entry point: `render_brief(state, metrics, reading, regime, period, tenant)` → raw PDF bytes. No LLM — fully deterministic. | N/A (library; import render_brief) | PDF bytes |
| Reading Config | tools/trading/analytics/reading_config.py | Config loader for FathomDesk reading engines — loads `args/reading_config.yaml` with file-mtime hot-reload caching, deep-merges YAML overrides onto hardcoded `_DEFAULTS`. Provides `cfg(*path, default)` for dotted-path access and `regime_set(name)` for set-based regime label lookups. Consumed by page_readings.py and portfolio_reading.py. No LLM — fully deterministic. | N/A (library; `from tools.trading.analytics.reading_config import cfg`) | Typed scalar/list/set values from reading_config.yaml or safe defaults |
| Reading Voice | tools/trading/analytics/reading_voice.py | Reading-voice transformations — post-processes a Reading dict (profile/observations/considerations/summary/tone_counts) for a voice config (omit_metrics, rephrase, show_considerations, profile_format). Supports per-page overrides via `voice_overrides` in the user profile. No LLM — fully deterministic. Public API: `apply_voice(reading, voice)` → new dict; `apply_active_voice(reading)` → fetches active profile and applies; `get_voice_for_page(page_key, profile)` → voice config respecting per-page override then global fallback; `apply_page_voice(reading, page_key, profile)` → single-call convenience for API endpoints. | N/A (library; `from tools.trading.analytics.reading_voice import apply_voice, apply_active_voice, get_voice_for_page, apply_page_voice`) | Reading dict with `_voice_applied` debug metadata |
| Rebalance Engine | tools/trading/analytics/rebalance.py | Target allocation and rebalance engine (Phase 4 R2, Passive persona). Computes drift from target allocations in `ad_target_allocations` table and generates ranked rebalance trade suggestions with tax-lot awareness. No LLM. | N/A (library; `get_rebalance_plan(user_id, portfolio)`) | Rebalance plan dict with suggested trades and drift analysis |
| Today's Digest | tools/trading/analytics/today_digest.py | Today's action digest (Phase 4 R1) — composes a prioritized plain-English list of what a user should consider doing today based on signals, alerts, regime, and portfolio state. The democratize feature for retail/casual users. No LLM. | N/A (library; `build_digest(user_id, tenant_id)`) | Digest dict with prioritized action items |
| Supply Chain Correlation Engine | tools/trading/market_intel/correlation_engine.py | Supply-chain cluster correlation manager (Phase 7.10). Defines 8 supply-chain clusters (travel_hospitality, housing_mortgage, energy_transport, tech_growth_liquidity, defensive_yield, industrial_commodity, financial_credit, healthcare_pharma) with lead-lag relationships and 4 monetary macro catalysts (cheap_money_environment, stock_buyback_surge, qe_money_printing, treasury_yield_compression). Scores cluster states, detects active monetary catalysts, computes portfolio correlation concentration (HHI), surfaces correlated peer tickers with lead-lag info. Tables: `ad_supply_chain_correlations`, `ad_cluster_states`, `ad_monetary_catalyst_states`. No LLM — fully deterministic. | N/A (library; `run_full_scan()`, `get_correlated_peers(ticker)`, `score_portfolio_correlation_concentration(positions)`) | Cluster state dicts, catalyst activation scores, HHI concentration score |
| Correlation Monitor Reflex | tools/genesis/reflexes/alphadesk_correlation_monitor.py | Genesis GREEN-tier reflex (2h cooldown) that detects cluster divergence, monetary catalyst activations, and lead-lag trigger signals across all 8 supply-chain clusters. Emits GKP artifacts to the kanban knowledge bridge when confidence ≥ 0.70. | `run(config, ctx)` (called by daemon) | `{status, detections, artifacts, cooldown_remaining}` |

## FathomDesk Auth Sub-System (Phase 2A)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Auth DB | tools/trading/auth/db.py | Auth storage layer — DDL + CRUD for `ad_users` (argon2id hash, tenant_id) and `ad_sessions` tables. Forward-compat with MFA fields. Supports SQLite + PostgreSQL via get_connection(). | N/A (library) | DB connection/helpers |
| Auth Passwords | tools/trading/auth/passwords.py | Argon2id password hashing — NIST SP 800-63B compliant. Self-describing hash encodes cost params for future cost increases. Entry points: `hash_password(plain)`, `verify_password(plain, hash)`. | N/A (library) | Hash string / bool |
| Auth Email | tools/trading/auth/email.py | Email delivery for auth flows — dev backend writes to `.tmp/sent_emails/*.eml`; prod backend uses SMTP. Entry points: `send_verification(email, token)`, `send_reset(email, token)`. | N/A (library) | Delivery status |
| Auth OAuth | tools/trading/auth/oauth.py | Social OAuth (OIDC) — Google canonical implementation; Microsoft + GitHub follow same shape. Handles authorization URL generation, callback token exchange, and upsert of ad_users row. | N/A (library; routes wired in blueprint) | User dict |
| Auth Password Reset | tools/trading/auth/reset.py | Password reset flow — generates `secrets.token_urlsafe(32)` (256-bit) tokens stored in `ad_password_resets` table with 1h TTL. Entry points: `request_reset(email)`, `apply_reset(token, new_password)`. | N/A (library) | Reset status dict |
| Auth MFA | tools/trading/auth/mfa.py | TOTP + backup codes (Phase 2A.3) — manages `ad_user_mfa` table. Generates encrypted TOTP secrets (via auth/crypto.py), validates TOTP codes (with drift window), generates + verifies one-time backup codes. | N/A (library; `setup_totp`, `verify_totp`, `use_backup_code`) | MFA status dict |
| Auth Crypto | tools/trading/auth/crypto.py | AES-GCM secret-at-rest helper (Phase 2A.3) — encrypts TOTP secrets (and BYOK API keys in Phase 2A follow-up) before DB storage. Entry points: `encrypt(plaintext)`, `decrypt(ciphertext)`. Key sourced from env. | N/A (library) | Encrypted/decrypted bytes |
| Auth WebAuthn | tools/trading/auth/webauthn.py | WebAuthn / Passkeys (Phase 2B) — NIST AAL3-capable factor for hardware keys (YubiKey, Google Titan) and platform authenticators (Touch ID, Windows Hello). Manages `ad_webauthn_credentials` table. Registration + assertion flows. | N/A (library; routes wired in blueprint) | Challenge / assertion result |
| Auth API Tokens | tools/trading/auth/api_tokens.py | Personal API tokens (Phase 4 R4, Quant persona) — stateless programmatic-access tokens for a user's own account. Pass as `Authorization: Bearer <token>`. CRUD for `ad_api_tokens` table; token is SHA-256 hashed at rest. | N/A (library; `create_token`, `verify_token`, `revoke_token`) | Token dict |
| Auth Admin CLI | tools/trading/auth/admin_cli.py | Operator-side CLI for FathomDesk auth — bypasses email flow to create users, set passwords, and trigger resets in dev/ops environments. Writes links to `.tmp/sent_emails/*.eml`. | --create-user, --set-password, --reset EMAIL | Status message |
| BYOK Credentials DB | tools/trading/credentials/db.py | BYOK storage layer — per-user encrypted API credentials (`ad_user_credentials` table). One row per (user_id, provider); keys encrypted via auth/crypto.py (Path A) OR per-user DEK (Path B / Phase 2C via `ad_user_vault`). Supports upsert, get, delete, list. Vault CRUD (`get_vault_row`, `insert_vault`, `update_vault_password_wrapping`, `update_vault_recovery_wrapping`, `delete_vault`, `set_vault_mode_for_credential`, `replace_credential_blobs`) for Phase 2C lifecycle. SQLite + PostgreSQL via get_connection(). | N/A (library) | Credential dict |
| Vault (BYOK Path B) | tools/trading/credentials/vault.py | Phase 2C — envelope encryption with password-derived KEK. `provision()` generates fresh salt + master_dek + two wrapped copies (password + recovery). `decrypt_dek_with_password/recovery()` decrypts during login or recovery. `stash_in_session()` puts DEK into signed Flask session cookie — only path the server reads the DEK. `rewrap_dek_with_new_password()` + `rotate_recovery()` support password change + recovery rotation. HKDF-SHA256 + AES-GCM via `cryptography` package; `is_available()` gates vault UI on that import. | N/A (library; `provision`, `decrypt_dek_with_password/recovery`, `stash_in_session`, `get_from_session`, `clear_session`, `encrypt_with_dek`, `decrypt_with_dek`, `rewrap_dek_with_new_password`, `rotate_recovery`, `generate_recovery_code`) | DEK bytes / wrapped blobs |
| SecretStore backend | tools/trading/credentials/secret_store.py | Phase 2D — pluggable credential storage backend. `SecretStore` ABC + `LocalEncryptedStore` (delegates to Path A/B in `credentials.db`) + `HashiCorpVaultStore` (KV v2 via `hvac`, lazy client, graceful degrade when hvac missing or Vault unreachable). Selected by `ICDEV_SECRET_BACKEND` env (`local` default or `vault`). `get_active_store()` factory is cached + reset via `reset_cache()`. `is_vault_backend()` helper for callers that need to branch behavior. When active backend is `vault`, `credentials.db` short-circuits local crypto and delegates; a stub row in `ad_user_credentials` mirrors provider presence so the Settings UI still shows configured providers. | N/A (library; `get_active_store`, `active_backend_name`, `is_vault_backend`, `reset_cache`) | SecretStore instance / health dict |
| BYOK Credential Resolver | tools/trading/credentials/resolver.py | Single read path for credentials — resolution order: per-user DB credential → env var fallback. Ensures all code paths use BYOK keys when available without duplicating lookup logic. | `resolve(user_id, provider)` | Credential string |
| BYOK Credential Tester | tools/trading/credentials/tester.py | Connection tester — verifies credentials without spending real budget. Uses no-op API calls (e.g. anthropic: POST /v1/messages with max_tokens=1) to confirm auth works. | `test(user_id, provider)` | `{ok: bool, latency_ms, error?}` |
| Vault Migration CLI | tools/trading/credentials/migrate_to_vault.py | One-shot migration of Path A (operator-keystore-encrypted) credentials to HashiCorp Vault backend (Phase 2D). Reads all `vault_mode=0` rows from `ad_user_credentials`, decrypts via `ICDEV_KEYSTORE_KEY`, and writes to the active `HashiCorpVaultStore`. Idempotent — skips rows already present in Vault or with `vault_mode=1` (per-user DEK, requires live session key). Requires `ICDEV_SECRET_BACKEND=vault` + Vault auth env vars. | `python tools/trading/credentials/migrate_to_vault.py [--dry-run] [--user-id UID] [--json]` | `{dry_run, total, migrated, skipped, errors, details}` |

## FathomDesk Supporting Sub-Systems
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Trading Oracle DB | tools/trading/oracle/db.py | Trading Oracle DB layer — DDL + CRUD for append-only `ad_trading_predictions` and `ad_trading_convergence_events` tables (NIST AU). `save_prediction()`, `list_predictions()`, `save_convergence_event()`. | N/A (library) | DB connection/helpers |
| Options Oracle DB | tools/trading/options/oracle_db.py | Options Oracle database layer — DDL + CRUD for `ad_oracle_option_decisions` (every scan hit + enter/skip/blocked verdict), `ad_oracle_option_positions` (open/closed oracle-managed positions), `ad_oracle_trade_events` (append-only trade event audit trail; NIST AU), and `ad_oracle_genesis_runs` (per-reflex run log). Includes incremental migrations. Key helpers: `log_decision`, `open_position`, `close_position`, `log_trade_event`, `list_decisions`, `list_positions`, `get_open_tickers`, `count_open_positions`, `start_run`, `finish_run`, `get_strategy_performance`, `mark_pending_close`, `clear_pending_close`, `link_decision_to_position`. | N/A (library) | DB connection/helpers |
| User Profile DB | tools/trading/profile/db.py | Storage layer for FathomDesk user profile (persona-aware UX). Forward-compatible with multi-user Phase 2 — `user_id` column already in place. CRUD for `ad_user_profile` table. | N/A (library) | Profile dict |
| Persona Presets | tools/trading/profile/presets.py | Persona preset loader + applicator — hot-reloads `args/persona_presets.yaml` on file-mtime change. `list_personas()` for onboarding picker; `apply_preset(profile, persona)` for one-shot profile initialization. | N/A (library) | Persona list / updated profile |
| Lessons Catalog | tools/trading/lessons/catalog.py | Lesson catalog + content loader (Phase 4 R5 → 6.5). Reads `args/lessons_catalog.yaml` (metadata, level groupings, per-lesson `prerequisites:`, `quiz:` blocks) + `docs/lessons/{level}/{slug}.md`. Phase 6.5 additions: chain-based level gating (`is_level_unlocked` — next level unlocks when all prior-level lessons completed), per-lesson prerequisites (`is_lesson_unlocked`), inline quiz grading (`grade_quiz` — multi-choice, pass threshold default 70%), quiz attempt audit in `ad_user_quiz_attempts` (append-only NIST AU). `curriculum_summary(user_id)` packs level-unlock state + per-user progress + gating hints for `/lessons` page. | `list_lessons`, `load_content`, `get_progress`, `mark`, `is_level_unlocked`, `is_lesson_unlocked`, `get_quiz`, `grade_quiz`, `record_quiz_attempt`, `best_quiz_attempt`, `curriculum_summary` | Catalog / progress / quiz result |
| News Impact Store | tools/trading/news/impact_store.py | News supply-chain impact trace store — shared persistence + query layer for the supply-chain impact pipeline. Powers Oracle lens_news_impact, Expert agents scoring, and /news page impact traces. | N/A (library; `save_impact_trace`, `get_traces_for_ticker`) | Impact trace list |
| Dashboard CSV Export | tools/trading/dashboard/csv_export.py | Shared CSV streaming helpers for FathomDesk dashboard exports — server-side streaming for large datasets (200+ tickers heatmap, nested news entities). Entry point: `stream_csv(rows, headers, filename)` | `stream_csv(rows, headers, filename)` | Flask streaming response |
| Share Link Tokens | tools/trading/share/tokens.py | Signed share-link tokens (Phase 4 R3, Advisor persona) — generates read-only links to portfolio PDF briefs for clients without FathomDesk accounts. Signed with HMAC; TTL-validated at consumption. | `create_share_token(user_id, portfolio_id, ttl_hours)`, `verify_share_token(token)` | Signed token string / decoded payload |
| Tenant DB | tools/trading/tenancy/db.py | Tenant storage layer (Phase 3.1) — DDL + CRUD for `ad_tenants` table. 'default' tenant always exists for legacy data. Supports tenant creation, lookup, update. SQLite + PostgreSQL via get_connection(). | N/A (library; `get_tenant`, `create_tenant`, `list_tenants`) | Tenant dict |
| Legacy User ID Migration | tools/trading/migrations/add_user_id_to_legacy_tables.py | Migration — adds `user_id TEXT` + `tenant_id TEXT` to legacy FathomDesk tables (ad_portfolios, ad_positions, ad_orders, ad_pf_daily_snapshots, ad_strategy_runs, ad_strategy_holdings, ad_cis_recommendations, and others). Idempotent via ALTER TABLE IF NOT EXISTS. | `--run`, `--dry-run`, --json | Migration status dict |

## FathomDesk Billing (Phase 5A/5B)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Plan Tiers | tools/trading/billing/tiers.py | Plan-tier catalog + quota enforcement (Phase 5A). Hot-reloads `args/plan_tiers.yaml`. Public API: `tier_for_tenant()`, `quota()`, `feature()`, `check_quota()` (raises `QuotaExceeded`), `usage_summary()`, `stripe_price_id()` (YAML → env fallback). | N/A (library) | Tier/usage/price dict |
| Billing Admin CLI | tools/trading/billing/admin_cli.py | Operator tier management — list tiers/tenants, set tier, show usage. `python -m tools.trading.billing.admin_cli <cmd>`. | `list-tiers` \| `list-tenants` \| `set-tier --tenant X --tier Y` \| `usage --tenant X` | JSON |
| Billing DB | tools/trading/billing/db.py | Stripe billing DB layer (Phase 5B) — DDL + CRUD for `ad_stripe_customers`, `ad_stripe_subscriptions`, `ad_stripe_events` (append-only NIST AU), `ad_stripe_invoices`. Idempotency via `event_already_processed()`. | N/A (library; `upsert_customer`, `upsert_subscription`, `record_event`, `upsert_invoice`, `list_invoices`) | Customer/subscription/invoice dict |
| Stripe Client | tools/trading/billing/stripe_client.py | Thin wrapper around Stripe SDK (Phase 5B). Lazy-imports `stripe` so /api/billing/* fails soft on air-gap. `ensure_customer()`, `create_checkout_session()`, `create_portal_session()`, `construct_webhook_event()`, `tier_from_price_id()`. Raises `StripeNotInstalled`/`StripeNotConfigured`. | N/A (library) | Stripe session dict / verified event |
| Stripe Webhook Dispatcher | tools/trading/billing/webhooks.py | Event router for Stripe webhooks (Phase 5B). Handles `customer.subscription.{created,updated,deleted}` and `invoice.payment_{succeeded,failed,finalized}`. Syncs tenant `plan_tier`, records every event in `ad_stripe_events` (audit), sends dunning email on payment failure. Idempotent. | `handle_event(event_dict)` | Result dict |

## FathomDesk Progression / Gamification (Phase 6.1)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Progression DB | tools/trading/progression/db.py | DDL + CRUD for `ad_user_progression` (level + XP + opt-in flag + paper-trading counters) and `ad_xp_events` (append-only NIST AU, idempotent via UNIQUE(user_id, dedup_key)). | N/A (library; `get_progression`, `append_event`, `apply_xp_delta`, `list_events`, `count_events_today`) | Progression / events dicts |
| XP Engine | tools/trading/progression/engine.py | Level + XP engine. Hot-reloads `args/xp_rules.yaml` + `args/progression_config.yaml`. `grant_xp()` enforces opt-in policy, daily caps, dedup; promotes level when thresholds cross. Calls achievements evaluator after every successful grant. `grant_xp_safe()` never raises — use at hook sites. | `grant_xp(user_id, reason, dedup_key, context, persona, amount_override)` | GrantResult dict |
| Achievements Evaluator | tools/trading/progression/achievements.py | Badge catalog + predicate-driven evaluator (Phase 6.2). Hot-reloads `args/achievements_catalog.yaml`. `evaluate_for_user()` runs every un-earned badge's predicate; satisfied ones are inserted into `ad_user_achievements` (UNIQUE(user_id, achievement_id)) + bonus XP granted. Predicate types: xp_event_count, xp_total_threshold, order_count, distinct_sectors, portfolio_value, lesson_track_complete, level_reached. | `evaluate_for_user(user_id)`, `achievements_summary(user_id)` | Earned list / full catalog with per-user status |
| Graduation Gate | tools/trading/progression/graduation.py | Live-trading graduation gate (Phase 6.6 — **compliance-sensitive**). Hot-reloads `args/graduation_criteria.yaml`. Evaluates 5 criteria (paper_days, analyses_completed, achievements_earned, sharpe_floor over 90d from `ad_pf_daily_snapshots`, risk_disclosure acknowledged). `enforce_live_order(user_id)` is called by `alpaca_adapter.submit_order()` before every NON-paper endpoint hit — raises `NotGraduatedError` when the user hasn't graduated. Env var `ICDEV_GRADUATION_ENFORCED=false` disables the gate globally (dev/CI only). `unlock_if_eligible()` flips the gate when every criterion passes; idempotent. Risk-disclosure modal copy is operator-replaceable. | `evaluate(user_id)`, `is_unlocked`, `unlock_if_eligible`, `acknowledge_risk_disclosure`, `enforce_live_order`, `is_enforced` | Status dict w/ per-criterion breakdown |
| Challenges DB | tools/trading/challenges/db.py | DDL + CRUD for `ad_challenge_attempts` (Phase 6.3). Mutable rows — status transitions active → completed / expired / abandoned. Helpers: `create_attempt`, `get_active_attempt`, `list_active_attempts_for_user`, `list_attempts_for_user`, `close_attempt`. | N/A (library) | Attempt dict |
| Challenges Engine | tools/trading/challenges/engine.py | Challenge lifecycle + predicate engine (Phase 6.3/6.3.5). Hot-reloads `args/challenges_catalog.yaml`. `start_attempt()` provisions an isolated sandbox paper portfolio (6.3.5) + snapshots live-paper state as legacy fallback; `evaluate_attempt()` runs predicate every call and closes the attempt if satisfied or if deadline passed. Predicate types: return_pct_over_period, beats_benchmark, holdings_whitelist, scenario_completion, hold_for_days — all evaluate against the sandbox when present, else fall back to the live-paper overlay. Completion awards bonus XP via `grant_xp_safe(reason='challenge_completed')`. | `start_attempt(user_id, tenant_id, challenge_id)`, `abandon_attempt`, `evaluate_user_attempts`, `summary` | Attempt / summary dict |
| Sandbox Portfolio DB | tools/trading/challenges/sandbox_db.py | Per-attempt isolated paper portfolio storage (Phase 6.3.5). Three tables: `ad_sandbox_portfolios` (1:1 with attempt — mutable cash + status), `ad_sandbox_positions` (mutable; qty/avg_cost/mv per ticker), `ad_sandbox_orders` (append-only NIST AU — every fill). Never touches the live `/portfolio` account. | N/A (library; `create_portfolio`, `get_portfolio`, `upsert_position`, `append_order`, `list_orders`, `archive_portfolio`) | Portfolio / position / order dicts |
| Options Chain Data | tools/trading/options/chain.py | Phase 7.5 — options chain data layer. Primary source: Alpaca snapshots endpoint (`/v1beta1/options/snapshots/{underlying}`) returning bid/ask/last + IV + Greeks per contract; fallback: yfinance when Alpaca creds missing (Greeks degrade to None). 5-minute in-process cache. OCC symbol parse/format helpers (`parse_occ_symbol`, `format_occ_symbol`). | `fetch_chain(ticker)`, `fetch_contract_quote(symbol)` | Chain dict or contract dict |
| Compliance Audit Aggregator | tools/trading/compliance/audit_aggregator.py | Phase 7+ compliance dashboard — unified read-only viewer across every append-only audit table (12 registered: audit_trail, ad_credential_audit, ad_stripe_events, ad_mfa_attempts, ad_password_reset_tokens, ad_tenant_invitations, ad_trade_audit, ad_xp_events, ad_user_achievements, ad_sandbox_orders, ad_sandbox_daily_snapshots, hook_events). Hot-reloads `args/nist_au_crosswalk.yaml` which declares per-table column mappings + NIST 800-53 AU control tags. `query_audit()` normalizes heterogeneous rows into a common schema + applies filters (since/until/user/category/source/search). `summary()` returns 24h/7d/30d activity stats per category + severity. `to_csv()` for ATO evidence export. | `query_audit`, `summary`, `crosswalk`, `to_csv`, `list_tables` | Normalized row list / summary dict / CSV string |
| Options Strategies | tools/trading/options/strategies.py | Phase 7.5 follow-up B (extended Phase 7+) — strategy library + payoff-at-expiry engine. Hot-reloads `args/options_strategies.yaml` (13 canonical strategies: long/short single legs, covered call, cash-secured put, bull/bear spreads, iron condor, straddle, long call butterfly, iron butterfly, calendar spread, diagonal spread, calendar butterfly). `build_legs()` expands a template into concrete OCC symbols given user-picked strikes + (single or `expiries={near, far}`) expiry. `strategy_uses_multi_expiry()` helper reports multi-expiry templates. `compute_payoff()` samples P&L across a price range at expiry, returns Chart.js-ready `{x, y, breakevens, max_profit, max_loss}`. Pure math — no network. | `list_strategies`, `get_strategy`, `build_legs`, `strategy_uses_multi_expiry`, `compute_payoff` | Strategy catalog / payoff dict |
| Tax-Lot DB | tools/trading/taxes/lots.py | Phase 7+ family-office tax-lot storage. Three tables: `ad_tax_lots` (mutable — `remaining_qty` decrements on sell), `ad_tax_realizations` (append-only NIST AU — one row per lot consumed on a disposal; auto-computes `holding_days` + `is_long_term > 365`), `ad_tax_wash_sale_flags` (append-only; flag-only, does NOT disallow). | N/A (library; `create_lot`, `list_open_lots` (FIFO/LIFO order), `get_lot`, `consume_lot`, `append_realization`, `list_realizations`, `append_wash_sale_flag`, `list_wash_sale_flags`) | Lot / realization / flag dict |
| Tax-Lot Engine | tools/trading/taxes/engine.py | Phase 7+ — tax-lot consumption engine + ST/LT gains report. `on_buy()` creates a lot. `on_sell(method='FIFO'|'LIFO'|'specific_id')` allocates the sell across open lots, writes one realization per lot consumed, and checks ±30-day wash-sale window (via `_check_wash_sale` helper) — flags but does not disallow. `tax_report(year=None)` summarizes short-term/long-term gain/loss + net + wash-sale flagged disallowed-loss totals. Holding > 365 days → long-term; wash-sale detection is substring-identical-ticker only (ETF/option overlap NOT modeled). | `on_buy`, `on_sell`, `tax_report` | Sell summary / tax report dict |
| Market Hours | tools/trading/challenges/market_hours.py | US equities regular-session gate for the sandbox (Phase 6.3.5 follow-up #3). `is_market_open()` checks weekday + 9:30–16:00 ET. Env `ICDEV_SANDBOX_MARKET_HOURS_AWARE=false` disables enforcement. DST handled via rough 2nd-Sunday-March / 1st-Sunday-November boundaries so air-gap boxes without `zoneinfo` tz data still work. Holidays NOT modeled (paper sim scope). | `is_market_open`, `is_enforced`, `market_status` | Boolean / status dict |
| Sandbox Engine | tools/trading/challenges/sandbox_engine.py | Lean paper-sim execution (Phase 6.3.5 + follow-ups). `ensure_sandbox()` provisions on attempt start with operator-tunable starting cash ($100k default) + frozen SPY benchmark price. `place_order()` accepts `order_type` ∈ {`market`, `limit`, `stop`} + optional `limit_price`/`stop_price`: market orders fill immediately with friction (`slippage_bps` default 5 against trader, `commission_usd` default 0 flat per-fill); limit/stop queue into `ad_sandbox_pending_orders`. `check_pending_orders(attempt_id)` + `check_pending_orders_all_active()` evaluate triggers, fill via market-order path on crossover, cancel-with-note on validation failure. `cancel_pending_order()` + `cancel_all_pending_for_attempt()` support user cancel + mass-cancel on close. Long-only; validates cash + holdings. `sandbox_snapshot()` mark-to-markets on read. `snapshot_day()` + `snapshot_day_all_active()` write the daily series into `ad_sandbox_daily_snapshots` — idempotent per day; feeds continuously-held predicates. Invoked by `challenge_snapshot_daily` (every 24h) and `challenge_order_watcher` (every 15 min) reflexes in the trading daemon. | `ensure_sandbox`, `place_order`, `sandbox_snapshot`, `archive_sandbox`, `snapshot_day`, `snapshot_day_all_active`, `check_pending_orders`, `check_pending_orders_all_active`, `cancel_pending_order`, `cancel_all_pending_for_attempt`, `starting_cash_for` | Snapshot / fill / pending / sweep dict |

## FathomDesk Greeks Deep Dive + Quick Wins (Phase 7.8)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Options Pricing (Black-Scholes) | tools/trading/options/pricing.py | Phase 7.8 — pure-Python closed-form Black-Scholes-Merton pricer + Greeks (via `math.erf`, no numpy). Graceful on degenerate inputs (T≤0 returns intrinsic; σ≤0 returns discounted forward intrinsic; negative spot returns 0). Theta per-day, vega + rho per-1%. Powers `compute_payoff_at_time` + portfolio greeks fallback + future rolling calculator. | `bs_price(option_type, S, K, T, r, sigma, q=0)`, `bs_greeks(...)` | Per-share price / `{delta, gamma, theta, vega, rho}` |
| Time-T Payoff | tools/trading/options/probability.py::compute_payoff_at_time | Phase 7.8 — payoff curve at an INTERIM time (not just expiry). Each leg priced with Black-Scholes at `dte_remaining_days`; signs per action; falls through to expiry intrinsic at T=0. Same `{x, y, max_profit, max_loss, breakevens}` shape as `strategies.compute_payoff` so the frontend swaps frames seamlessly. `proposal_builder` produces 5 frames (+1d, 25%, 50%, 75%, expiry) for the AI Assist time-slider. | `compute_payoff_at_time(legs, spot_range, dte_remaining_days, iv_annual_pct, r_annual=0.04)` | Payoff dict |
| Portfolio Greeks | tools/trading/options/portfolio_greeks.py | Phase 7.8 — aggregate Δ/Γ/Θ/ν across a user's open option positions. Multiplies per-contract greeks by 100 × signed qty. Stale positions (no cached `last_greeks_json`) contribute zero and are counted in `stale_count` so the /portfolio card can surface "how much of the book is guessing." Read-only over `ad_sandbox_option_positions`. | `compute_portfolio_greeks(user_id)` | `{net_delta, net_gamma, net_theta, net_vega, position_count, stale_count, as_of}` |
| Greek Profile Classifier | tools/trading/options/greek_profile.py | Phase 7.8 — maps aggregate portfolio Greeks to named strategy profiles and ranks all 20 canonical strategies by greek_fit_score. `classify_profile(greeks)` returns a named profile string (long_volatility, short_volatility, bullish_directional, etc.). `rank_by_greek_fit(portfolio_greeks, market_view)` scores each strategy by Greek alignment + directional/IV/horizon view match, returns list sorted by fit score descending. No LLM — fully deterministic. | `classify_profile(greeks)`, `rank_by_greek_fit(portfolio_greeks, market_view)`, CLI: `python tools/trading/options/greek_profile.py --classify --json` | Named profile string / ranked list with `greek_fit_score` per strategy |
| Shareable Trade URLs | tools/trading/options/share.py | Phase 7.8 — encode/decode a minimal proposal payload into a URL-safe base64 token. No user_id, no secrets, no server-only state. 2 kB size cap. Recipient's browser auto-fills AI Assist + submits; the SERVER re-runs `parse_intent` + `build_proposal` + preflight — URL payload is untrusted. | `encode_proposal(proposal, intent)`, `decode_proposal(token)` | Token string / `{intent, proposal_stub}` |

## FathomDesk Probability & Compare (Phase 7.7)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Options Probability | tools/trading/options/probability.py | Phase 7.7 — Monte Carlo POP + price-cone percentiles. Geometric-Brownian-Motion: S_T = S_0·exp((μ − σ²/2)·T + σ·√T·Z). Default 10 000 paths, risk-neutral drift, deterministic seed keyed on (underlying, expiry, sorted-strikes) so re-renders are stable. Shares `strategies._leg_pnl_at_expiry` so overlay math matches the payoff chart. Returns None when DTE < 1 (intraday) or inputs malformed. | `compute_pop(legs, spot, iv_annual_pct, dte_days, underlying?, expiry?)` | `{pop_pct, expected_pnl, percentile_prices, pnl_distribution, n_samples, iv_used_pct, model}` or `None` |
| Options Compare API | tools/trading/dashboard/app.py (routes) | Phase 7.7 — `POST /api/options/ai-assist/compare` returns N full proposals side-by-side (primary + alternates with full payoffs + POP + preflight, or an explicit `strategy_ids` list). `proposal_builder.build_for_strategy` helper bypasses `rank_strategies` to build a proposal for a specific strategy id. Server-side preflight on every proposal — never trust client-cached. No LLM — pure deterministic diff. | HTTP POST `{intent_text, underlying?, qty?, strategy_ids?, iv_percentile?}` | `{intent, proposals:[{proposal, preflight}]}` |

## FathomDesk AI-Assisted Options (Phase 7.6)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Options Intent Parser | tools/trading/options/intent_parser.py | Phase 7.6 — translates a natural-language thesis ("Bullish AAPL through earnings, limited risk") into a strictly-typed schema dict. LLM path (any provider via LLMRouter.chat) primary; deterministic keyword matcher fallback per `args/options_intent_schema.yaml` for air-gap / `ICDEV_NO_LLM=true` deployments. Never raises — every failure path returns a best-effort dict with `source='rule'`. Whole-phrase matching (regex word boundaries) prevents "undefined" from substring-matching "defined" hints. | `parse_intent(text, underlying=None)` | `{direction, horizon, iv_view, risk_cap, underlying, raw_text, source}` |
| Options Strategy Selector | tools/trading/options/strategy_selector.py | Phase 7.6 — deterministic strategy ranker. Rule-table maps `(direction, risk_cap)` → priority list over the 13 canonical strategies in `args/options_strategies.yaml`. IV view + horizon are scoring overlays; intraday is a hard-filter whitelist. Returns top-3 with per-candidate reasons array. NO LLM — auditable per NIST AU. | `rank_strategies(intent)` | `[{strategy_id, score, reasons}, ...]` up to 3 |
| Options Strike Picker | tools/trading/options/strike_picker.py | Phase 7.6 — delta-target strike picker + horizon expiry bucketizer. `pick_expiry(horizon, chain)` selects the chain expiry nearest to bucket midpoint per `args/options_intent_schema.yaml::horizon_dte_buckets`. `pick_strikes(strategy_id, chain, expiry, expiries=?)` picks each leg's contract at the target delta from `args/options_strike_targets.yaml` (defaults: short=0.30Δ, long=0.15Δ, ATM=0.50Δ). Falls back to strike-ladder offset when delta missing. Multi-expiry strategies (calendar/diagonal/calendar-butterfly) consume `expiries={'near','far'}`. | `pick_expiry`, `pick_strikes` | `{expiry, dte, warnings}` / `{strategy_id, legs, warnings}` |
| Options Proposal Builder | tools/trading/options/proposal_builder.py | Phase 7.6 — end-to-end orchestrator: `rank_strategies → fetch_chain → pick_expiry(+far_if_multi) → pick_strikes → compute_payoff`. Returns the top proposal with Chart.js-ready payoff + rationale-stub (structured facts, NO prose — the /api propose endpoint attaches the LLM-written prose downstream) + up to 2 alternates. | `build_proposal(intent, qty=1, chain=None)` | Proposal dict or `{status:'error'}` |
| Options Preflight Gates | tools/trading/options/preflight.py | Phase 7.6 + fd-floor — hard-block risk inspector between proposal + execute. **Gate 0** (new): per-strategy Sharpe/drawdown floor loaded hot from `args/fathomdesk_strategy_floors.yaml`; blocks live / warns paper when backtest metrics below floor or no backtest artifact found. Gates 1–5: max_loss vs equity cap; undefined-risk tier; DTE range; IV-regime warn band; proposal-emitted warnings. Returns `{allowed, warnings, blocks, meta}` (meta includes `gate0_backtest`, `gate0_floor`) — NEVER executes. | `run_preflight(proposal, user_id?, equity?, is_live=False, options_tier?, iv_percentile?)` | Preflight dict |
| Strategy Risk Floors | args/fathomdesk_strategy_floors.yaml | Per-strategy Sharpe min and max-drawdown floor config consumed by preflight Gate 0. Hot-reloadable. Defaults: `sharpe_min=1.5`, `max_drawdown_pct=15.0`. Per-strategy overrides: `naked_put` (sharpe 2.0, dd 10%), `calendar_butterfly` (sharpe 1.8, dd 12%). Strategies not listed inherit defaults. | N/A (config file) | YAML |
| FathomDesk Constants | tools/fathomdesk/constants.py | `BacktestResult` dataclass (ticker, strategy_id, strategy_type, sharpe_ratio, max_drawdown_pct, win_rate, ev_per_trade, trade_count, backtest_start, backtest_end, paper_default, created_at) + `STRATEGY_TYPES` list of 20 canonical strategy IDs. Used by reflexes to emit canonical backtest artifacts to `memory/institutional/`. | N/A (library) | Dataclass / list |
| Signal Decay | tools/fathomdesk/signal_decay.py | FathomDesk signal decay — applies Hippo exponential decay formula `w(t) = max(weight_floor, e^{-lambda * age_days})` to `ad_signals`. Config hot-reloaded from `args/signal_decay_config.yaml` (lambda, weight_floor, batch_cadence_hours); falls back to module defaults if file is missing or malformed. Entry points: `load_config()`, `decay_weight(age_days, cfg=None)`. No LLM — fully deterministic. | N/A (library) | Float weight ∈ [weight_floor, 1.0] |
| Signal Tuner | tools/fathomdesk/signal_tuner.py | FathomDesk signal threshold optimisation with dry-run support. `dry_run()` previews the 4-step tuning pipeline (load_thresholds → filter_signals → score_backtest → emit_report) without writing to the database. Thresholds loaded from `args/signal_thresholds.yaml` via `signal_generator.load_thresholds()`. Accepts optional pre-loaded signals list; returns step descriptors, threshold values, and input signal count. No LLM — fully deterministic. | N/A (library; `dry_run(signals=None)`) | Dict with dry_run flag, steps, thresholds, steps_count, input_signals |
| Options Order Submitter | tools/trading/options/option_order.py | Multi-leg options order submission with limit-order fill-chasing. NEVER uses market orders (wide spreads). Strategy: submit limit at mid-price → every 30s check fill → step limit $0.02 toward natural per retry → cancel after 3 misses. Paper mode simulates fill via uniform random(mid, ask) for half-spread friction. Live mode submits as multi-leg linked order via `order_manager.submit_multileg_order`, then per-leg cancel/resubmit loop. | N/A (library; `submit_option_legs(legs, qty, mode, ticker)`) | `SubmitResult` with `status` (filled/partial/missed/error), `net_debit`, `order_ids`, per-leg `LegFill` list |
| Options Coach DB | tools/trading/options/coach_db.py | Phase 7.6 — CRUD helpers for `ad_options_coach_events` (append-only NIST AU; migration 020). Supports `insert_event`, `set_recommendation` (in-row enrichment of the nullable `recommendation` column — rows never deleted, other columns immutable), `list_events`, `latest_event_per_position`, `event_exists_within` (dedupe guard). Uses `get_connection()` + `sql_placeholder()` — SQLite + PostgreSQL portable. | N/A (library) | Event dict / bool / list[dict] |
| Options Coach Engine | tools/trading/options/coach_engine.py | Phase 7.6 — pure-rule scanner over `ad_sandbox_option_positions`. Emits events when thresholds cross: `profit_target` (50% of max profit), `loss_threshold` (2× debit or credit), `dte_warning` (≤7 DTE), `dte_roll_window` (8–21 DTE), `iv_crush` (≥20% drop — skipped until opened_iv captured). Dedupes per (position, event_type) within 24h. NO LLM — `coach_llm.explain_event` attaches recs asynchronously. Loaded by daemon reflex `options_coach` (10m cadence). | `scan_position(position)`, `scan_user(user_id)`, `scan_all_users()` | List of emitted events / daemon summary |
| Options Coach LLM | tools/trading/options/coach_llm.py | Phase 7.6 — LLM recommendation writer. Strictly grounded prompt (≤3 sentences, must end with clear action: close / adjust / hold, MAY NOT introduce new numbers). Deterministic rule-template fallback per event type when LLM unavailable. `attach_recommendation(event_id)` fetches event → generates rec → persists via `coach_db.set_recommendation`. Daemon reflex calls this for every new event whose `recommendation` column is still NULL. | `explain_event`, `attach_recommendation` | Recommendation string |

## FathomDesk Crypto + Day-Trader Realtime (Phase 7+)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Crypto Universe | tools/trading/market_intel/universe.py | Phase 7+ — spot crypto pairs integrated into the multi-asset universe. `CRYPTO` dict covers BTC/ETH/SOL/AVAX/DOGE/LTC/LINK/BCH/UNI/AAVE against USD. `is_crypto_symbol()` + `normalize_crypto_symbol()` helpers route any matching ticker (including bare bases like "BTC") to the Alpaca v1beta3/crypto/us endpoint. No perps, lending, or staking (spot-only). Futures remain broker-blocked (see memory:project_fathomdesk_futures_deferred). | N/A (library) | Dict / bool / canonical pair |
| Crypto Quote + Bars | tools/trading/data/market_data.py | Phase 7+ — `fetch_crypto_quote(ticker)` + `fetch_crypto_bars(ticker, ...)` route to Alpaca's v1beta3 crypto endpoint (`latest/quotes`, `latest/trades`, `bars`) via `alpaca_adapter.get_crypto_latest_quote/_trade/_bars`. `fetch_latest_quote()` + `fetch_bars()` auto-route crypto tickers to the crypto path; sample-fallback when Alpaca unavailable. 24/7 — bypasses equities market-hours gate. | `fetch_crypto_quote(ticker)`, `fetch_crypto_bars(ticker, timeframe, limit)` | Quote / bar list |
| Day-Trader Hot-Keys (JS) | tools/trading/dashboard/static/js/hotkeys.js | Phase 7+ — browser-side hot-key engine gated on `AD_PROFILE.flags.keyboard_shortcuts_visible` or `requires_realtime`. Bindings: `j`/`k` row navigation on `.ad-navigable` tables (requires `data-ticker` on rows), `b`/`s` quick buy/sell (calls `window.adOpenQuickOrder` if defined, else URL-hops to /orders), `/` focus command palette, `?` toggle cheatsheet, `Esc` close overlays. Input-safe (INPUT/TEXTAREA/contentEditable bypass). | N/A (client-side) | No-op for non-day-trader personas |
| Day-Trader Fast-Poll (JS) | tools/trading/dashboard/static/js/fastpoll.js | Phase 7+ — browser-side 5-sec auto-refresh gated on `requires_realtime`. Elements marked `data-adfp-endpoint="/api/..."` with optional `data-adfp-target` + `data-adfp-render` trigger periodic fetch + render. Pauses when `document.hidden`. Override interval via `window.AD_FASTPOLL_MS`. `/portfolio` + `/signals` use persona-aware intervals (5s day-trader vs 60s default). | N/A (client-side) | No-op for non-day-trader personas |

## FathomDesk Leagues & Leaderboards (Phase 6.4)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Leagues DB | tools/trading/leagues/db.py | DDL + CRUD for tenant-scoped leagues (Phase 6.4). Two tables: `ad_leagues` (mutable — name/description/visibility editable; UNIQUE(tenant_id, slug)), `ad_league_members` (mutable captain/member role; UNIQUE(league_id, user_id)). Standings are computed on-demand in the engine — no cache table. | N/A (library; `create_league`, `get_league`, `list_leagues_for_tenant`, `add_member`, `remove_member`, `list_members`, `count_members`, `generate_join_code`) | League / member dicts |
| Leagues Engine | tools/trading/leagues/engine.py | Leagues lifecycle + standings compute (Phase 6.4). Hot-reloads `args/leagues_config.yaml` (windows, scoring weights, limits). `create_league()` auto-joins creator as captain + generates join code for `code`-visibility leagues. `join_league()` respects visibility policy (public, private requires captain-add, code requires matching join_code). `compute_standings()` aggregates `ad_xp_events.amount` + completed `ad_challenge_attempts` per member within a time window (weekly=7d / monthly=30d / all_time=since league.created_at) and ranks by `xp * xp_weight + wins * challenge_bonus`. | `create_league`, `join_league`, `join_by_code`, `add_member_as_captain`, `remove_member`, `delete_league`, `compute_standings(league_id, window)`, `summary_for_user` | League / standings dict |
| Options Greek Coach | tools/trading/options/coach.py | Phase 7.9 — portfolio Greek exposure coach: alerts when net delta/theta/vega/gamma exceed configurable thresholds (args/options_coach_thresholds.yaml); per-alert-type 4-hour cooldown; fires alerts to ad_coach_alerts table | `check_greek_exposure(user_id, conn=None)` | list of alert dicts {user_id, alert_type, severity, message, triggered_at} |

## FathomDesk Phase E — Factor Modeling + DeFi Coverage
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Trap Sweep Reflex | tools/genesis/reflexes/fathomdesk_trap_sweep.py | Genesis GREEN-tier reflex (4h cooldown). Scans `ad_signals` for direction flips (BUY→SELL = bull_trap, SELL→BUY = bear_trap) with confidence ≥ 0.50. Deduplicates per-ticker within 24h via `ad_trap_events`. Sentiment elevation: bearish oracle weight (<0.4) near resistance adds +0.15 to confidence. Writes to `ad_trap_events`; uses `ad_reflex_cooldowns` per-ticker to prevent re-emission within 4h. Air-gap safe — no LLM in hot path. | `run(config, session)` (Genesis daemon) | `{success, metric_value: traps_inserted, details: {run_id, events, trap_config}}` |
| Base Trading Lens | tools/trading/oracle/base_lens.py | Abstract base class for Trading Oracle lenses. Defines the `TradingPrediction` dataclass (id, lens, subject_type, subject_id, prediction_type, confidence, sentiment_weight, direction, recommended_action, evidence) and the `BaseTradingLens` ABC with mandatory three-phase pipeline: `analyze()` → `score()` → `propose()`. `run()` executes the full pipeline. All six concrete lenses (Signal Convergence, News Intelligence, Regime Trajectory, Portfolio Stress, News Impact, Fundamental Mispricing) inherit from this base. | N/A (library; ABC — subclass only) | `list[TradingPrediction]` |
| Regime Trajectory Lens | tools/trading/oracle/lens_regime_trajectory.py | Trading Oracle lens that forecasts macro regime shifts from SROR snapshots + macro context. Detects: regime shift imminent (danger crossing CRISIS/DANGER threshold), opportunity window opening (danger falling + opportunity rising), stagflation escalation, deflation escalation (balance-sheet-recession tail: M2 contraction + velocity collapse + breakeven compression), yield curve deepening, cross-asset HARD_EXIT override (+100 danger), cross-asset ROTATION_ALERT (+15 danger). Inherits `BaseTradingLens` three-phase pipeline. | N/A (library; `RegimeTrajectoryLens().run()`) | `list[TradingPrediction]` |
| Factor Data | tools/trading/factors/factor_data.py | Fama-French 5-factor ETF proxy fetcher. Daily factor returns via yfinance ETF proxies: Market (SPY−Rf), SMB (IWM−SPY), HML (IWD−IWF), RMW (QUAL−SPY), CMA (VLUE−SPY); risk-free from ^IRX. Stores in `ad_factor_returns` (date PK). Falls back to deterministic sample data (SHA-256 date seed) when yfinance unavailable. Entry points: `fetch_factor_returns(days)`, `store_factor_returns(returns)`, `get_latest_factor_returns(days)`. CLI: `--fetch [--days N] [--latest N] [--json]`. | `--fetch [--days N] [--latest N] [--json]` | List of daily factor return dicts |
| Factor Regression | tools/trading/factors/factor_regression.py | OLS regression for Fama-French 5-factor loadings per ticker. Pure Python stdlib normal equation — no numpy/pandas. Computes beta_market, beta_smb, beta_hml, beta_rmw, beta_cma, R-squared, and annualized alpha. Stores in `ad_factor_loadings` (ticker PK). Reads factor returns from `ad_factor_returns`, fetches ticker returns via yfinance (deterministic fallback). Entry points: `compute_loadings(ticker, days)`, `store_loadings(loadings)`, `get_loadings(ticker)`, `compute_all_loadings(tickers)`. CLI: `--ticker SYM [--days N] [--demo] [--json]`. | `--ticker SYM [--days N] \| --demo [--json]` | Loading dict `{ticker, beta_market, beta_smb, beta_hml, beta_rmw, beta_cma, r_squared, alpha_annualized}` |
| Alpha Calculator | tools/trading/factors/alpha_calculator.py | CAPM + Fama-French 5-factor alpha decomposition. Decomposes a ticker's actual return into per-factor systematic contributions plus residual alpha. Reads `ad_factor_returns` / `ad_factor_loadings`; falls back to factor_data/factor_regression if empty. Stores decomposition in `ad_alpha_decomposition`. CLI: `--ticker SYM [--days N] [--demo] [--json]`. | `--ticker SYM [--days N] \| --demo [--json]` | Decomposition dict with factor contributions and residual alpha |
| Cost Model | tools/trading/factors/cost_model.py | Transaction cost model for trading systems. Estimates round-trip costs (half-spread + market impact via Kyle's lambda: `0.1 * sqrt(qty / ADV)`). `estimate_round_trip_cost(ticker, qty, avg_daily_volume, bid_ask_spread)` → cost pct. `net_alpha_after_turnover(gross_alpha_pct, turnover_pct, round_trip_cost_pct)` → net alpha. Pure stdlib, no external deps. CLI: `--demo [--json]`. | `--demo [--json]` | Cost pct float / net alpha dict |
| Election Phase Classifier | tools/trading/factors/election_phase.py | US Presidential Election Cycle phase classifier. Maps any date to its 4-year cycle position (POST_ELECTION/MIDTERM/PRE_ELECTION/ELECTION) with historical DJIA average returns (Stock Trader's Almanac, 1896+) and regime premium multipliers (PRE=1.30×, ELECTION=1.05×, POST=0.85×, MIDTERM=0.75×). Detects the "Sweet Spot" (midterm Q4 through pre-election Q2). Pure stdlib. Reference epoch: 2024. `classify(dt)` → `PhaseInfo`. CLI: `--date YYYY-MM-DD [--json]`. | `--date YYYY-MM-DD [--json]` | `PhaseInfo` dataclass with phase, cycle_year, is_sweet_spot, avg_annual_return_pct, premium_multiplier |
| Regime Premiums | tools/trading/factors/regime_premiums.py | Time-varying regime-conditional factor premiums. Connects FathomDesk macro regime (GREEN/YELLOW/RED) to expected factor returns using FF5 loadings from `ad_factor_loadings`. Per-regime annualized premiums: GREEN (market_rf=8%, smb=2%, hml=3%, rmw=3%, cma=2%), YELLOW (4/0/1/4/3%), RED (-5/-3/-2/5/4%). Stores computed expected returns in `ad_regime_premiums`. Includes Markov transition matrix blending when regime is uncertain. `compute_expected_return(ticker, regime)` → expected return dict. CLI: `--ticker SYM --regime GREEN\|YELLOW\|RED [--json]`. | `--ticker SYM --regime R [--json]` | `{ticker, regime, expected_return_annual_pct, market_contrib, smb_contrib, hml_contrib, rmw_contrib, cma_contrib}` |
| Signal Validator | tools/trading/factors/signal_validator.py | Harvey-Liu-Zhu (2016) signal validation with dynamic analyst weights. Tracks predicted vs. actual direction per analyst (fundamental/technical/sentiment/news/macro/perspective), computes t-statistics against the null of 50% accuracy, and applies t>3.0 threshold to distinguish skill from data-snooping. Analysts failing the threshold are floored at 0.05 weight; passing analysts weighted proportionally to validated hit rate (ceiling 0.40). Stores records in DB; exposes `get_weights()`, `record_prediction(analyst, direction)`, `record_outcome(ticker, actual_direction)`. CLI: `--weights [--json]`, `--hit-rate ANALYST [--json]`, `--demo [--json]`. | `--weights \| --hit-rate ANALYST \| --demo [--json]` | Weight dict / hit-rate dict |
| Skill Tracker | tools/trading/factors/skill_tracker.py | Bootstrap-based luck vs. skill validation for trading systems. Shuffles trade returns 10 000 times (default) to build a null Sharpe distribution, then checks whether actual Sharpe is statistically above the null. `validate_skill(returns, n_bootstrap, seed)` → `{skill_percentile, verdict (genuine\|inconclusive\|overfit), actual_sharpe, bootstrap_median, bootstrap_95th}`. Pure stdlib, no external deps. CLI: `--demo [--json]`. | `--demo [--json]` | Skill assessment dict |

## FathomDesk Backtesting & Multi-Agent Analysis (Phase D)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Backtester | tools/fathomdesk/backtester.py | Event-driven OHLCV bar replay with FIFO position tracking and realized P&L. `score(signals)` computes classification metrics (precision, recall, F1) for a signal list against actual bar moves. `BacktestSession.run(bars, strategy_id)` performs bar-by-bar replay with FIFO lot accounting: each bar fires `_sma_signal()` (SMA-20 golden/death cross), closes lots via `_close_fifo()`, and builds an equity curve. `_compute_backtest_metrics()` derives Sharpe ratio, Calmar ratio, and max drawdown % from the equity curve. Persists one row per run to `ad_backtest_runs` via `get_connection()`. Risk-free rate hot-reloaded from `args/fathomdesk_config.yaml`. No LLM — fully deterministic. | N/A (library; `from tools.fathomdesk.backtester import BacktestSession, score`) | `{"run_id", "sharpe", "calmar", "max_dd_pct", "win_rate", "realized_pnl", "trade_count", "open_lots", "trades"}` |
| Backtest Migration | tools/db/migrations/057_ad_backtest_runs.sql | Migration 057 — creates `ad_backtest_runs` table (NIST AU append-only): `id` TEXT PK, `ticker`, `strategy_id` (NOT NULL), `backtest_start`/`end` (NOT NULL), `sharpe_ratio`, `calmar_ratio`, `max_drawdown_pct`, `win_rate` (REAL), `trade_count` (INT), `triggered_by` (TEXT), `created_at` (TIMESTAMPTZ). Indexes on ticker, strategy_id, created_at. Never UPDATE or DELETE. | SQL migration (apply via db migration runner) | Table + 3 indexes |
| Base Analyst Agent | tools/fathomdesk/agents/base_analyst.py | Abstract base class for all FathomDesk analyst agents. Declares `analyze(**kwargs) -> dict` as the mandatory contract; all subclasses must return at minimum `{score: float 0-1, signals: list[str], reasoning: str, confidence: float 0-1}`. Constructor wires `self._llm` via `llm_factory.get_llm(agent_name)` for per-agent LLM provider override. Subclasses set `agent_name` at class level before calling `super().__init__(ticker, as_of_date)`. | N/A (library; ABC — subclass only) | Structured analyst report dict (implemented by subclasses) |
| Fundamentals Agent | tools/fathomdesk/agents/fundamentals_agent.py | Valuation-based analyst lens for the multi-agent panel. `FundamentalsAgent.analyze()` fetches fundamental data via `FathomDeskDataGateway.fundamentals()`, then sends the JSON payload to the configured LLM (fundamentals_agent Sonnet override) for scored evaluation. Extends `BaseAnalystAgent`. Graceful fallback on data-fetch or parse errors (returns score=0.5, confidence=0). | N/A (library; `FundamentalsAgent(ticker, as_of_date).analyze()`) | `{score: float 0-1 (>0.5=bullish), signals: list[str], reasoning: str, confidence: float}` |
| Technical Agent | tools/fathomdesk/agents/technical_agent.py | Price-action and signal-based analyst lens for the multi-agent panel. `TechnicalAgent.analyze()` reads pre-filtered signals via `signal_generator.generate()` against `args/signal_thresholds.yaml`, then sends surviving signals to the configured LLM (technical_agent ollama/llama3 override) for scored evaluation. Returns HOLD with confidence=0 when no signals pass the threshold gates. Extends `BaseAnalystAgent`. | N/A (library; `TechnicalAgent(ticker, as_of_date).analyze(signals=[...])`) | `{score: float 0-1, signals: list[str], reasoning: str, confidence: float, direction: BUY\|SELL\|HOLD}` |
| Macro Agent | tools/fathomdesk/agents/macro_agent.py | Macro-regime analyst lens for the multi-agent panel. `MacroAgent.analyze()` reads macro context via `fetch_macro_context()` (regime, QE/QT phase, credit impulse) and recent trap events from `ad_trap_events`, then sends combined context to the configured LLM (macro_agent Sonnet override) for scored evaluation. Both data sources degrade gracefully on failure. Extends `BaseAnalystAgent`. | N/A (library; `MacroAgent(ticker, as_of_date).analyze()`) | `{score: float 0-1 (>0.5=bullish), signals: list[str], reasoning: str, confidence: float}` |
| Research Manager | tools/fathomdesk/agents/research_manager.py | Synthesizes the 4 analyst-lens reports (fundamentals, technical, sentiment, macro) into a coherent 200–350-word investment plan narrative. Called by `DebateEngine` as pre-debate synthesis step. Entry: `run_synthesis(reports)` where reports is a dict of lens-name → report text. Uses Sonnet via `llm_factory.get_llm("research_manager")`; falls back to a truncated concatenation on LLM failure. | N/A (library; `from tools.fathomdesk.agents.research_manager import run_synthesis`) | Plain-prose investment plan string (200–350 words) |
| Trader | tools/fathomdesk/agents/trader.py | Translates the investment debate result into a concrete transaction proposal. Entry: `propose(investment_plan, ticker, analyst_reports)`. Uses Opus model (configured via `agent_llm_overrides.trader` in `args/fathomdesk_config.yaml`). Near-deterministic (temperature=0.1). Size modifier clamped to [0.25, 2.0]; direction validated to BUY/SELL/HOLD. Fallback: HOLD at 0.25× on any LLM/parse error. | N/A (library; `from tools.fathomdesk.agents.trader import propose`) | `{direction: BUY\|SELL\|HOLD, size_modifier: float 0.25–2.0, rationale: str ≤120 words}` |
| Sentiment Agent | tools/fathomdesk/agents/sentiment_agent.py | News-driven sentiment analyst lens for the multi-agent panel. `SentimentAgent.analyze()` reads recent headlines via `data_gateway.fetch_news(ticker, as_of_date)`. Fast path: uses pre-computed `intaas_sentiment` score directly when available (confidence 0.8). LLM path: sends up to 20 headlines to the configured LLM (technical_agent provider override) and parses JSON `{score, signals, reasoning, confidence}`. Extends `BaseAnalystAgent`. | N/A (library; `SentimentAgent(ticker, as_of_date).analyze()`) | `{score: float 0-1 (>0.5=bullish), signals: list[str], reasoning: str, confidence: float}` |
| Debate Engine | tools/fathomdesk/agents/debate_engine.py | Multi-agent bull/bear consensus debate orchestrator. Pipeline: (1) runs all 4 analyst agents concurrently via `ThreadPoolExecutor`; (2) synthesizes outputs into bull/bear case summaries via LLM; (3) spawns opposing bull-advocate and bear-advocate LLM calls concurrently (Opus model, configured in `args/fathomdesk_config.yaml`); (4) scores each position against analyst evidence; (5) returns `DebateResult` dataclass with `verdict` (BUY/SELL/HOLD), `confidence`, `bull_score`, `bear_score`, `reasoning`. | N/A (library; `DebateEngine(ticker, as_of_date).run(signals=[...])`) | `DebateResult` with verdict, confidence, bull_score, bear_score, reasoning |
| Risk Manager | tools/fathomdesk/agents/risk_manager.py | Portfolio-level arbitration + constraint enforcement. Consumes a `DebateResult` and applies three ordered gates: **Gate A** — signal confidence floor from `args/signal_thresholds.yaml`; **Gate B** — liquidity trap hard-block reading `ad_macro_regimes` + `ad_trap_events` (blocks short-premium strategies in active trap regime); **Gate C** — tax-lot wash-sale sizing penalty reading `ad_tax_wash_sale_flags`. Returns `{direction, confidence, size_modifier, reasoning}`. Optionally injects past decision context from `decision_memory.get_past_context()`. Uses `get_connection()` — SQLite + PostgreSQL portable. | N/A (library; `RiskManager(ticker, as_of_date).arbitrate(debate_result)`) | `{direction: BUY\|SELL\|HOLD, confidence: float, size_modifier: float, reasoning: str}` |
| Analyst Panel | tools/fathomdesk/analyst_panel.py | Top-level orchestrator for the multi-agent analyst panel. Reads `analyst_panel_enabled` flag from `args/fathomdesk_config.yaml` (overridable via `ANALYST_PANEL_ENABLED` env var). When enabled: calls `DebateEngine.run()` → `RiskManager.arbitrate()` → returns final recommendation. When disabled: falls back to deterministic heuristic signal scoring without touching LLM agents. Public entry: `run(ticker, as_of_date, signals=[...])`. | N/A (library; `from tools.fathomdesk.analyst_panel import run`) | `{direction: BUY\|SELL\|HOLD, confidence: float, size_modifier: float, reasoning: str, source: str}` |
| Decision Memory | tools/fathomdesk/decision_memory.py | Deferred reflection feedback loop for trading decisions. Writes an append-only markdown log at `data/fathomdesk_decisions.md` using atomic temp-file replace. On each same-ticker run: (1) fetches actual return for any pending decisions via `data_gateway`; (2) calls LLM for a 2–4 sentence reflection on each resolved outcome; (3) injects N same-ticker + M cross-ticker past lessons into risk_manager context. Public API: `store_decision(ticker, date, decision)` — persist immediately, no LLM; `get_pending(ticker)` — fetch unresolved entries; `update_with_return(ticker, date, raw, alpha)` — resolve + LLM reflection; `get_past_context(ticker, n_same, n_cross)` — returns prompt-ready context string. | N/A (library) | Context string / updated decision log |

