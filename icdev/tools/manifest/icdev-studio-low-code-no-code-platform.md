# ICDEV™ Studio — Low-Code/No-Code Platform (Phase 72 — D361-D366)

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## ICDEV™ Studio — Low-Code/No-Code Platform (Phase 72 — D361-D366)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Studio DB Init | tools/studio/init_db.py | Initialize 9 studio_* tables (PG + SQLite compatible, idempotent) | --json, --verbose | Table creation summary |
| Workflow Editor | tools/studio/workflow_editor.py | Workflow CRUD, tool catalog (5 categories, 22 tools), YAML validation | --json catalog, templates, list, get <id> | Tool catalog, templates, workflows |
| Studio API | tools/dashboard/api/studio.py | Flask Blueprint: workflow CRUD, tool catalog, marketplace storefront API | REST endpoints under /api/studio/ | JSON responses |
| Form Builder | tools/studio/form_builder.py | Form schema CRUD, JSON Schema output, 10 field types, 4 pre-built templates, submissions | --json field-types, templates, list, get <id> | Form schemas + submissions |
| Case Manager | tools/studio/case_manager.py | Case lifecycle engine: FSM state machine, Kanban board, SLA rules, 3 lifecycle templates | --json templates, types, cases, board <type_id> | Case data + board view |
| Dashboard Builder | tools/studio/dashboard_builder.py | Custom widget layouts: 15 widget types, 3 role defaults (PM/ISSO/Dev), save/share | --json widgets, roles, list, create-default <role> | Dashboard layouts |
| Automation Builder | tools/studio/automation_builder.py | Citizen automation: trigger/condition/action rules, 10 triggers, 8 actions, 5 templates, simulate | --json triggers, operators, actions, templates, list, runs, simulate <id> | Automation rules + run history |
| NL App Builder | tools/studio/nl_app_builder.py | NL-to-blueprint pipeline: extract capabilities from description, create session, refine, build child app | --json extract <desc>, create <desc> --name, refine <id> | Blueprint preview + build result |
| Studio CSS | tools/dashboard/static/css/studio.css | Premium design system: glass cards, gradients, animations, 8px grid | (asset) | CSS |
| Workflow Studio JS | tools/dashboard/static/js/workflow-studio.js | DAG canvas editor: drag-drop nodes, SVG edges, zoom, validate, YAML import/export | (asset) | JS |
| Marketplace JS | tools/dashboard/static/js/marketplace.js | Asset browser: search, filter, sort, detail modal, one-click install | (asset) | JS |
| WNE Blueprint | tools/studio/studio.py | Workflow Narrative Engine Flask Blueprint (`create_studio_wne_blueprint()`, mounted at root). Serves the `/studio/narrate` UI plus WNE chat API: `POST /api/wne/session` (create), `POST /api/wne/<session_id>/message` (chat turn), `GET /api/wne/<session_id>/artifact?type=` (retrieve exec_brief/coa_comparison/budget_table/roi_analysis/slide_outline/zip_bundle), `GET /api/wne/<session_id>/status`. Delegates to `WNEChatEngine`. | (Flask blueprint) | REST routes under /api/wne/ + /studio/narrate page |
| WNE Budget Estimator | tools/studio/wne/budget_estimator.py | Phase-by-phase budget estimator using node-type cost heuristics; T-shirt sizing; no LLM; air-gap safe | WorkflowContext | Phased cost table dict |
| WNE COA Builder | tools/studio/wne/coa_builder.py | Builds 3-COA comparison (A/B/C) from composite nodes or parametric fallback; reuses coa_generation.md structure | WorkflowContext | 3-COA markdown table |
| WNE Export Pack Generator | tools/studio/wne/export_pack_generator.py | Orchestrates all WNE modules into a downloadable zip (exec_brief, COA, budget, ROI, slides, summary) | WorkflowContext + audience | zip file |
| WNE ROI Calculator | tools/studio/wne/roi_calculator.py | Computes ROI, NPV, payback period, and sensitivity table from WorkflowContext parameters; no LLM; air-gap safe | WorkflowContext.parameters | ROI/NPV/payback dict |
| Workflow Runner | tools/studio/workflow_runner.py | Per-run SSE execution engine: DAG topological sort, subprocess step execution, per-run isolated queue (never mixes multi-user runs), DB run/step records. Key functions: `start_run(workflow_id, project_id, inputs=None)` (dwo-evt-04 — `inputs` dict is persisted to the run row's `inputs_json`; `None` records NULL, distinct from `{}`), `stream_run(run_id)` → SSE generator, `list_runs()`, `get_run()`, `get_run_steps()`, `generate_python_script(workflow_id)` | workflow_id, project_id, inputs | run_id + SSE event stream (run_started / step_started / step_done / run_complete / heartbeat / error) |
| Run Memory | tools/studio/run_memory.py | Run-scoped shared state for workflow steps (dwo-mem-01) — the single mechanism for inter-step data, replacing stdout-JSON scraping. Table `studio_run_memory`, PK (run_id, key), JSON parsed in Python (PG-portable). Functions: `get(run_id, key, default=None)`, `set(run_id, key, value)`, `all(run_id)`, `delete(run_id, key)`, `copy(src_run_id, dst_run_id)` (snapshot one run's memory into another; the shipped resume path is in-place — `workflow_runner.RESUME_MODE == "in_place"` — so no runtime caller forks a run id today, and this exists for one that would). The runner exports `ICDEV_RUN_ID` into every step env. Reserved keys: `_inputs` (run inputs), `canvas` (the template's `canvas:` slug, published before the first step), `artifacts` (`{step_name: [artifact, …]}`, published as each step completes — what `executors/_base.py` `get_iac_artifacts()` / `resolve_canvas()` read, dwo-mem-02). NOT `tools/memory/` (long-term session memory). | `--run-id ID --get KEY` / `--set KEY --value JSON` / `--all` / `--delete KEY` | `{"status": "ok", "run_id": ..., "value"/"memory": ...}` |
| Run Resume | tools/studio/workflow_runner.py + tools/dashboard/api/studio.py | Continues a run left mid-flight (dwo-dur-01/02/03). `resume_run(run_id)` re-attaches a worker to the ORIGINAL run row — `RESUME_MODE == "in_place"`, it does NOT fork a run linked by `resumed_from_run_id`, because a gate parked at `awaiting_approval` keeps its `step_run_id` and that step row is keyed to the original `run_id` (forking would strand every approval issued before the interruption). Steps already recorded success/approved/skipped are replayed from `_load_prior_steps()`, not re-executed — `terraform apply` is not idempotent. `RESUMABLE_RUN_STATUSES = (pending, running, awaiting_approval, failed)`; `failed` is included because a run that died part-way is the case resume exists for. Returns False for an unknown/terminal run or one a live worker already owns (single-owner guard on `_run_queues`). Per-step `retries` / `retry_backoff_seconds` read from the template YAML (both default 0, so existing templates are byte-for-byte unchanged); only `failed`/`timeout` are retried, backoff is linear and each sleep capped at 300s. `reconcile_runs_on_boot()` — call ONCE from app startup, never at import — resumes gates still inside their approval window, expires those past it, and fails steps orphaned at `running`. UI control in `static/js/workflow-studio-exec.js` (shown only for statuses the API accepts; a test asserts the JS list and the Python constant stay in step). Rationale + rejected fork alternative: docs/features/dwo-durable-workflow-orchestration.md. | `POST /api/studio/runs/<run_id>/resume` (alias `/api/studio/workflows/runs/<run_id>/resume`); `resume_run(run_id)` | 202 `{status: resuming, run_id, mode: in_place}`; 404 unknown run; 409 terminal status or already-owned |
| MCP Step Dispatch | tools/studio/workflow_runner.py (`MCP_EXECUTOR`, `_build_mcp_command`, `_step_tool_path`, `_mcp_params_json`) | Lets a workflow step run a registry tool instead of a script (dwo-mcp-03). A `node_type: mcp` step names a `tools/mcp/tool_registry.py` tool in `mcp_tool` (+ optional `mcp_params`) rather than a path in `tool`, and is compiled into `<MCP_EXECUTOR> --tool <name> --params <json>` plus `--step-id` / `--project-id` / `--run-id` / `--json`. `mcp_params` is JSON-dumped, except a string, which passes through untouched so a template may hand-author the JSON; a step's `args` are NOT forwarded — an MCP tool takes its arguments from `mcp_params` only. `_step_tool_path()` returns the fixed executor path for these nodes so the existence check, the run/step records and the standalone-script export report the executor rather than an empty `tool`. A step with no `mcp_tool` is skipped with that specific reason. `mcp` is in the linter's `VALID_NODE_TYPES` (tools/studio/template_linter.py) and the schema README. **Authorization is default-deny:** `mcp_workflow_tools` in `args/security_gates.yaml` (gate `MCP-WF-001`) lists 16 read-only `allowed` tools and 13 `requires_approval` tools that dispatch only after an approved `node_type: human` gate in the same run; anything unnamed is refused and audited. **NOTE:** the executor script itself, `tools/studio/executors/mcp_executor.py` (dwo-mcp-01), is NOT in the tree yet — until it merges an `mcp` step degrades to the existing "Tool not found" skip rather than failing a run. | step dict (`node_type: mcp`, `mcp_tool`, `mcp_params`) | executor argv list, or `[]` → step skipped |
| Agent Step Dispatch | tools/studio/executors/agent_executor.py + tools/studio/workflow_runner.py (`AGENT_EXECUTOR`, `_build_agent_command`, `_agent_tools_arg`, `_agent_degradation`) | Lets a workflow step run an AGENT LOOP instead of a script (hgx-agent-01). A `node_type: agent` step names its task in `prompt` and bounds its tools with `agent_tools` (bundle names from `args/agent_toolsets.yaml`) rather than a path in `tool`, and is compiled into `<AGENT_EXECUTOR> --prompt <text> --agent-tools <names>` plus the optional `--system-prompt` / `--llm-function` / `--work-dir` / `--max-iterations` / `--max-tokens` / `--effort` / `--approval-mode` and `--step-id` / `--project-id` / `--run-id` / `--json`. The executor invokes `icdev.tools.llm.agent_loop.run_agent_loop` with the worktree toolset (`tools/genesis/rubric_build_tools.py::build_worktree_toolset`, hgx-exec-01) INTERSECTED with `toolsets.resolve_bundles(agent_tools)`; the allowlist is default-deny, so a step declaring no bundle is refused rather than handed the worktree. That intersection is then narrowed again by `apply_tool_gate()` → `executors/agent_tool_gate.py` (gate `AGENT-WF-001`, hgx-agent-02): the bundles say what the STEP wants, the gate says what the CALLER may have, so a tool above the run's impact level is withheld before the model sees it (reported in the payload's `tools_refused`, never silently dropped) and a step whose every tool is withheld fails with `agent_step_all_tools_refused` rather than running with an empty toolbox. `--caller-il` / `--caller-roles` / `--caller-id` / `--tenant-id` / `--approval-wait` carry that caller context; they are NOT template-declarable. Every tool call then passes the ars-appr-01 reversibility gate (`tools/agent_runtime/approval_gate.py::build_approval_hook`, tiers in `args/agent_approval_policy.yaml`, decisions audited append-only) — passed explicitly so an agent node is gated whether or not `ICDEV_AGENT_APPROVAL_MODE` is set. Routing is by `llm_function`, NEVER a model id. `AgentLoopUnsupported` (CLI-bridge provider, `supports_tools: false`) DEGRADES the step: exit 0 with `degraded: true`, which `_exec_step` records as `skipped` with the reason, so the run continues and dependents can branch on `output.degraded`. Files the agent wrote or patched are declared as the step's `artifacts` and lifted into run memory. `agent` is in the linter's `VALID_NODE_TYPES` with a `bad_agent` lint for a missing `prompt`/`agent_tools`; `tools/orchestration/workflow_composer.py` builds a byte-identical command so a template behaves the same headless. | step dict (`node_type: agent`, `prompt`, `agent_tools`) | executor argv list, or `[]` → step skipped |
| Agent Tool Authorization Gate | tools/studio/executors/agent_tool_gate.py | Default-deny authorization for every tool an `node_type: agent` step calls (hgx-agent-02, gate `AGENT-WF-001`). The `mcp` counterpart of this — `mcp_workflow_tools` / `MCP-WF-001` — decides one authored tool per step; an agent step hands a MODEL a set and lets it choose, so the same policy is enforced TWICE: `authorize_toolset(names, caller=…)` filters the offered toolset before the model is told about it (an unauthorized tool cannot be asked for), and the `PreToolUseHook` from `build_gate_hook(...)` re-checks every call before the handler runs (a model can name a tool it was never given). Policy is the `agent_workflow_tools` section of `args/security_gates.yaml`: `default: deny`, 6 read-only `allowed`, 3 `requires_approval` (`write_file`/`patch_file`/`run_command`), `default_min_il: IL4`, and per-tool `tool_limits` `min_il`/`required_roles` — the declaration site the mcp surface does not need, because a worktree tool has no owning registry component. A tool reachable from BOTH surfaces takes the stricter IL of the two (`tool_limits` vs. the owning component's `min_il`), so the agent node is never the cheaper route. **This module is not a second mechanism:** the policy reader (`mcp_executor.load_gate_policy(key=…)`), the human gate (`mcp_executor.await_approval`, namespaced `approval:agent:<tool>` so approving a reviewed mcp step's `run_command` cannot authorize an agent loop's), the caller resolution (`resolve_caller` — run memory's `caller` key, then `$ICDEV_MCP_CALLER_IL`/`$ICDEV_IMPACT_LEVEL`/`$ICDEV_MCP_CALLER_ROLES`, then IL4), the IL ordering (`canvas_access._IL_ORDER`) and the append-only audit (`studio_mcp_dispatch_audit`) are all the mcp surface's. What differs is the section, the refusal vocabulary (`agent_tool_not_allowlisted` / `agent_tool_exceeds_caller_il` / `agent_tool_missing_required_role` / `agent_tool_awaiting_human_approval` / `agent_tool_approval_rejected` / `agent_tool_approval_gate_unavailable` / `agent_gate_policy_unavailable`) and the gate namespace. Fail-closed everywhere: no readable default-deny policy → no toolset; no run to park a gate on → refused; every decision (allowed / refused / pending_approval) is one audit row carrying a `params_sha256`, never the arguments. The hook CHAINS to the ars-appr-01 reversibility gate rather than displacing it — authorization first, so an unauthorized tool is refused without anyone reviewing its arguments — and a call must clear both. A template may NOT declare `caller_il` (deliberately absent from `workflow_runner._AGENT_STEP_FLAGS`): authored content cannot raise its own impact level. | `--tool NAME [--caller-il IL5] [--caller-roles r1,r2] [--run-id ID] [--json]`, or `--list` | exit 0 `{status: authorized, disposition, min_il, limits_source}`; exit 1 `{status: refused, error_type, error}` |
| Event Source Registry | tools/db/migrations/304_studio_event_tables.sql + tools/studio/init_db.py (`STUDIO_TABLES`) | Schema binding external events to workflow runs (dwo-evt-01-d1). ICDEV receives events through `tools/gateway/gateway_agent.py` and runs DAGs through `workflow_runner.py`; these three tables are the binding. `studio_event_sources` — one row per source, `kind` CHECK-constrained to `gateway_channel｜canvas_bus｜schedule｜manual`, plus `config_json`/`enabled`. `studio_workflow_triggers` — source → workflow, with `filter_json` and `input_mapping_json`; `filter_json` holds `automation_builder.CONDITION_OPERATORS` conditions (equals, not_equals, contains, greater_than, less_than, in_list, is_empty, is_not_empty) — deliberately no second condition DSL. `studio_trigger_events` — **APPEND-ONLY** (NIST AU; registered in `init_db.APPEND_ONLY_TABLES` and the `pre_tool_use.py` hook): one row per event evaluated, *including non-matches* (`matched=0`, `run_id` NULL) with a `reason`, because a trigger that silently never fires is otherwise undiagnosable — this table is the answer to "why did this run start". JSON columns are TEXT parsed in Python, never with SQLite JSON functions. Registered in `STUDIO_TABLES` so a fresh install gets them without the migration; schemas mirrored into `tests/conftest.py` and `icdev/tools/studio/init_db.py`. The CRUD + `match_event()` layer that reads these tables is `tools/studio/event_sources.py` (dwo-evt-01-d3, below). **NOTE:** the gateway ingress (dwo-evt-02) is not in the tree yet, so the producers today are the canvas bus subscriber and the `external_event` automation trigger (dwo-evt-03). | `python tools/studio/init_db.py --json`, or migration 304 | 3 tables + 6 indexes |
| Event Sources & Trigger Matching | tools/studio/event_sources.py | CRUD over the dwo-evt-01 tables plus the matcher that turns an event into a run (dwo-evt-01-d3). `match_event(event)` evaluates the event against every enabled trigger on its source using `automation_builder.evaluate_conditions` and the same `CONDITION_OPERATORS` automations use — there is deliberately no second rules DSL. Every result carries a `reason`, including non-matches, because a trigger that silently never fires is undiagnosable. `dispatch_event(source_id, event_type, payload)` is a **library call, not a route** — nothing here opens a socket; external events arrive only after the gateway's security chain has cleared them (dwo-evt-02). It writes one append-only `studio_trigger_events` row per candidate trigger, matched or not, and one row when a source has no triggers at all, so "my event went nowhere" is answerable. `normalize_event` lets filters name payload fields directly (`branch`) or fully (`event.payload.branch`). `simulate_workflow_trigger(trigger_id)` dry-runs one trigger, starting and logging nothing, and never back-fills filter fields to make its own conditions pass. **NOTE:** `start_run` does not yet accept `inputs=` (dwo-evt-04-d2), so mapped inputs are seeded into `run_memory` right after the run starts and a first step that reads one races the seeding. | `--list-sources` / `--list-triggers [--workflow-id ID]` / `--simulate TRIGGER_ID` / `--dispatch SOURCE_ID --event-type T --payload JSON`, all `--json` | `{status, matched, runs[], results[]}` — each result `{trigger_id, matched, reason, event_id, run_id?}` |
| Canvas Bus Subscriber | tools/studio/bus_subscriber.py | The wire that makes a `canvas_bus` event source live (dwo-evt-01-d5). Follows the existing `tools/*/bus_subscriber.py` pattern over migration `039_canvas_events`: `register()` — called once from `tools/dashboard/api/__init__.py` right after `studio_api` mounts — reads every enabled `canvas_bus` row and installs one `tools.canvas.event_bus.subscribe()` listener per `(canvas_id, event_type)` pair it declares. The source's `config_json` is the whole declaration: `{"canvas_id": "qdc", "event_types": ["qdc.gate.executed"], "project_id": "default"}`; `event_types` defaults to `["*"]` (the bus's wildcard key) and a source with no `canvas_id` is skipped with a warning rather than silently subscribing to nothing. The handler hands the payload straight to `event_sources.dispatch_event()`, so canvas events are matched by the same `match_event()` and audited in the same append-only `studio_trigger_events` as gateway events — no second matcher, no second audit. Payloads gain `_bus_event_id`/`_canvas_id` so a run traces back to the `canvas_events` row that caused it. Registration is idempotent (`_REGISTERED`), and `create_event_source()` calls `register_source()` on a new `canvas_bus` row so a source created from the Triggers panel is live without a restart. | `python tools/studio/bus_subscriber.py --list --json`; `register()` at blueprint init | `{sources, subscriptions, new}` + the installed `(source_id, canvas_id, event_type)` listeners |
| Gate Bridge | tools/studio/gate_bridge.py | Publishes a paused Studio approval gate (`node_type: human`) into the workflow_hitl reviewer inbox so there is exactly ONE approval surface (dwo-dur-04). Studio is a *producer* of `wf_external_steps`; workflow_hitl keeps ownership of notification routing, teams, templates and the webhook-token completion path. Linkage needs no new schema — `external_system='studio'` and `external_ref=<step_run_id>` give lookup in both directions. Functions: `open_gate(run_id, step_run_id, step_name, role, project_id)`, `find_external_step(step_run_id)`, `complete_external_step(step_run_id, decision, actor)` (Studio → HITL), `release_studio_gate(step, actor)` (HITL → Studio), `is_studio_step(step)`. A decision in either surface releases the other exactly once: a `_bridging` re-entrancy guard stops the callback looping, and terminal-status checks on both sides reject-and-audit a second decision. Shadow `wf_templates`/`wf_instances` rows (`wft-studio-gate`, `wfi-studio-<run_id>`) satisfy the existing FKs. | run_id, step_run_id, step_name, role, project_id | wf_external_steps id (or None when workflow_hitl is unavailable — Studio then falls back to its own Details-modal gate) |
| Workflow Chat | tools/studio/workflow_chat.py | Chat-to-YAML engine: converts natural language to workflow YAML via Ollama/Qwen3 through LLMRouter. Embeds compact tool catalog in system prompt, supports multi-turn conversation history, strips markdown fences from LLM output. Key function: `generate_workflow_yaml(user_message, conversation_history)` | user_message (str), optional conversation_history (list) | {"status": "ok", "yaml": str, "steps_count": int, "description": str, "category": str} or {"status": "error", "error": str} |
| Canvas Bridge | tools/studio/canvas_bridge.py | Canvas-to-workflow template mapper: maps active canvas context (NDC/SDC/PDC/BDC/DDC/ODC/IDC) to the closest pre-built YAML workflow template and seeds a new studio workflow pre-populated with canvas-specific steps. Called by `POST /api/studio/workflows/from-canvas`. | canvas_id (str), context_label (str) | {"redirect": "/studio/workflows/<id>"} |
| Workflow Templates | context/workflow_templates/ | 19 pre-built YAML workflow templates covering compliance, security, data, deploy, ops, ai, and govcon categories (infra IaC, network topology, security assessment, ATO boundary, observability gap, data quality, app migration, SBOM, AI model eval, incident response, ODCS contract, ZTA maturity, proposals, agentic AI review, A2A mesh test, FedRAMP evidence). Each file is a valid studio workflow YAML (name, description, category, tags, steps[]). Served by GET /api/studio/workflows/templates. | (static YAML files) | workflow YAML |
| Workflow Trigger Button | tools/dashboard/templates/includes/workflow_trigger_btn.html | Reusable Jinja2 partial: renders a gear-icon "Workflow" button that POSTs `{canvas_id, context_label}` to `/api/studio/workflows/from-canvas` and opens the created workflow in a new tab. Required context vars: `wf_canvas_id` (str), `wf_context_label` (str). Include via `{% include "includes/workflow_trigger_btn.html" %}`. | wf_canvas_id, wf_context_label | HTML button + inline JS (sendToWorkflowStudio) |

