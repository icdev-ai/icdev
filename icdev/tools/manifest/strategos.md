# Strategos

> Shard of `tools/manifest.md`. See index at `tools/manifest.md`.

## Strategos

| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Blueprint | apps/strategos/blueprint.py | Registered Flask blueprints for the full Strategos canvas (supply chain, cyber, darkweb, DAT, wargame turn engine, intel briefs, chat). Two factories: create_strategos_blueprint() → page bp mounted at /strategos; create_strategos_api_blueprint() → API bp mounted at /api/strategos. Registered in tools/dashboard/app.py. The legacy unregistered duplicate at tools/strategos/blueprint.py was removed (nav-strat-05); its unique wargame endpoints (orbat-seed, lanchester/monte-carlo, turn/advance, turns) were ported into the API blueprint under /api/strategos/wargame/. | Registered via Flask app factory | HTTP responses (HTML page or JSON) |
| Strategy Agent | tools/strategos/strategy_agent.py | Generates MDMP Courses of Action (COAs) from ScenarioContext. Before calling Ollama, enriches context via corrective_rag.parallel_retrieve() (D-KARL-3, provenance profile) — queries sg_corpus_documents (doctrine corpus) + sg_conflict_events (historical events) in parallel across 3 RAG strategies (vector, graphrag, source_scan). Falls back to deterministic stub COAs if Ollama is unavailable. rag_active=True when at least one document was retrieved. Entry point for hitl_advisor.py. | --scenario TEXT, --theater LABEL, --intent TEXT, --constraint TEXT (repeatable), --no-rag, --json | StrategyResult JSON: {courses_of_action[{name,description,scheme_of_maneuver,risk_level,feasibility,acceptability,suitability,distinguishing_factor,composite_score}], recommended_coa_index, recommended_coa, model_used, latency_ms, rag_active, rag_doc_count, theater, timestamp, error} |
| GitLab OSINT Collector | tools/strategos/gitlab_osint_collector.py | Standalone GitLab CI runner script. Reads internet-tier sources from args/strategos_config.yaml (or strategos_osint_sources.yaml), fetches RSS feeds, deduplicates via sha256, and writes two artifacts: osint_signals.json (sg_raw_signals array, max 500) and kg_delta.json (entity mentions: unit names, locations, equipment). Any source failure is logged and skipped. Always exits 0. | --output-dir PATH, --json | osint_signals.json {sg_raw_signals, count, run_at}; kg_delta.json {entities, run_at} |
| Tier Resolver | tools/strategos/tier_resolver.py | Resolves active OSINT and executor tiers at runtime. OSINT: INTERNET → GITLAB → FILE_INBOX → NONE. Executor: CLAUDE_CLI → GITLAB → OLLAMA_LOCAL. Caches 5 min; logs tier changes. Used by osint_harvester, kanban_scheduler, and external importers. | --json, --no-cache | TierStatus JSON (osint_tier, exec_tier, gitlab_reachable, file_inbox_count, ollama_reachable, resolved_at) |
| Cyber Feed Refresh Reflex | tools/genesis/reflexes/cyber_feed_refresh.py | Genesis reflex (24h cadence) that runs CISA KEV sync and records results. Returns {ok, nvd_upserted, kev_flagged, skipped_cooldown, ran_at}. COOLDOWN_HOURS=24 guard prevents double-firing; logs each run to genesis_reflex_log. CLI: python tools/genesis/reflexes/cyber_feed_refresh.py | --json (implicit via __main__) | {ok, nvd_upserted, kev_flagged, skipped_cooldown, ran_at} |
| OSINT Harvester | tools/genesis/reflexes/strategos/osint_harvester.py | Three-tier OSINT signal harvester (INTERNET→GITLAB→FILE_INBOX→NONE). Deduplicates via sha256. Writes sg_raw_signals + sg_raw_signals_audit. Handles RSS, Telegram, snscrape, GitLab CI artifacts, and file inbox JSON/TXT. | --json | {success, metric_value, details{tier, signals_harvested, duplicates_skipped, errors}} |
| GDELT Importer | tools/strategos/gdelt_importer.py | GDELT 2.0 narrative-events bulk importer. Downloads export CSV ZIPs, filters to Ukraine/Taiwan AOIs (country code + lat/lon bbox), and upserts into sg_conflict_events (event_type=narrative_event, source=gdelt). Deduplicates via (source, external_id) unique index. | --file, --date YYYYMMDD, --days N, --dry-run, --json | {dry_run, files_attempted, files_ok, total_aoi_rows, by_aoi, inserted, skipped_duplicates} |
| STIX Importer | tools/strategos/stix_importer.py | STIX 2.1 / CERT-UA cyber threat bundle importer. Parses campaign, intrusion-set, threat-actor, malware, tool, and indicator objects. Maps indicator patterns and relationship chains to MITRE ATT&CK technique IDs (T-codes). Writes to sg_conflict_events (event_type=cyber_op) and canvas_kg_nodes/edges (CyberOperation, ThreatActor, Malware, Tool, Indicator node types). Deduplicates via (source, external_id) unique index. | --file PATH, --source LABEL (default: stix; use cert-ua for CERT-UA feeds), --dry-run, --json | {dry_run, source, bundle_id, objects_found, by_type, inserted, skipped_duplicates, kg_nodes_written, kg_edges_written} |
| Behavior Pattern Lens | intelligence/oracle/lenses/behavior_pattern.py | Adversary behavior pattern lens for SIOEngine (lens_id=behavior_pattern). Reads sg_conflict_events (30d): escalation_velocity, pattern_repetition, deception_indicators. Score = weighted avg ×10. Writes to sg_sio_assessments. | python intelligence/oracle/lenses/behavior_pattern.py | {score, confidence, nato_reliability, narrative, sub_scores} |
| Threat Posture Lens | intelligence/oracle/lenses/threat_posture.py | Threat posture lens for SIOEngine (lens_id=threat_posture). Reads sg_conflict_events + sg_raw_signals: adversary_mobility, force_concentration, offensive_indicators. Score = weighted avg ×10. Writes to sg_sio_assessments. | python intelligence/oracle/lenses/threat_posture.py | {score, confidence, nato_reliability, narrative, sub_scores} |
| Intent Assessment Lens | intelligence/oracle/lenses/intent_assessment.py | PMESII-PT historical precedent matcher (lens_id=intent_assessment). Compares 7-dim PMESII-PT vector vs 15 seeded historical cases via cosine similarity. Returns top-3 precedents, score 0–10, NATO reliability. Writes to sg_sio_assessments + historical_cases. | python intelligence/oracle/lenses/intent_assessment.py [P M E S I I2 PT] | {current_vector, top_matches, score, confidence, nato_reliability, narrative} |
| Convergence Lens | intelligence/oracle/lenses/convergence.py | Cross-domain convergence lens for SIOEngine (lens_id=convergence). Reads latest scores from sg_sio_assessments for peer lenses, computes std_dev, convergence_score = 10-(std_dev×2). I&W triggered when convergence>7.5 AND avg>6.0. | python intelligence/oracle/lenses/convergence.py | {score, confidence, nato_reliability, narrative, iw_triggered, lens_scores} |
| SIO Engine | intelligence/oracle/sio_engine.py | Strategic Intelligence Oracle orchestration engine. Runs all 4 lenses in sequence, computes composite_score (weighted avg), returns OracleAssessment dataclass. CLI --run-all --json. Also exposes get_latest_assessment() for dashboard API. | python -m intelligence.oracle.sio_engine --run-all --json | OracleAssessment JSON: {lenses, composite_score, iw_triggered, top_narrative, timestamp} |
| Predictive Intelligence Engine | tools/strategos/predictive_intel_engine.py | Aggregates the OSINT data mesh (sg_raw_signals, sg_conflict_events, sg_prioritized_signals) and runs the SIO Oracle to produce forward-looking predictive assessments and auto-generated leadership briefings. Computes signal velocity, Bayesian P(war) posterior, and 3-horizon forecasts (24h/72h/7d). Generates Markdown narrative via LLM. Persists to sg_leadership_briefs. CLI --theater --json. | python tools/strategos/predictive_intel_engine.py --theater global --json | {brief_id, theater, sio_composite_score, iw_triggered, threat_tier, signal_count_24h, signal_velocity, p_war, forecasts{24h,72h,7d}, narrative_md, generated_at, latency_ms} |
| Targeting Package Optimizer | tools/strategos/targeting_package_optimizer.py | Greedy hill-climb + 1-opt local search for optimal interdiction package. Objectives: maximize_blast_radius, minimize_time_to_effect, maximize_endurance_delta. Synergy: delta_blast(A+B)−delta_blast(A)−delta_blast(B). Returns OptimalPackage: nodes, combined_blast_radius, blast_pct, time_to_effect_p50_days, endurance_delta_months, marginal_value_per_node, synergy_pairs. | --budget N --objective X --theater Y --json | OptimalPackage JSON + computed_at + params |
| OSM Logistics Importer | tools/strategos/osm_importer.py | Imports Ukraine rail/road/bridge/port infrastructure from an OSM GeoJSON export into sg_entities (entity_type=logistics_node). Classifies features by OSM tags (bridge→bridge, railway→rail, port amenity/landuse/man_made/waterway→port, highway→road). Computes geometry centroids. Seeds vulnerability_score=0.5; domain scorer updates scores later. Deduplicates via UNIQUE(source, external_id). Requires migration 061_sg_entities. | --file PATH [--node-types rail,road,bridge,port] [--aoi ukraine] [--dry-run] [--json] | {dry_run, file, aoi, total_features, by_node_type, inserted, skipped_duplicates} |
| Exercise Classifier | tools/strategos/exercise_classifier.py | PLA exercise signature classifier. Detects and classifies People's Liberation Army exercise types from raw OSINT text: DONGFENG (DF-series ballistic missile/PLARF), JOINT_SWORD (Taiwan encirclement), ISLAND_ATTACK (amphibious assault), COMBAT_PATROL (PLAAF ADIZ incursion), MARITIME_INTERDICTION (PLAN blockade). Uses weighted heuristic regex scoring. Pacific theater detection via args/pacific_dtw_patterns.yaml. | --text "..." or --days N [--json] | {exercise_type, confidence, signatures_matched, is_pla, theater, all_scores} |
| WAR Retriever | tools/strategos/war_retriever.py | Domain-specific RAG retrieval profiles for Strategos war-analysis queries. Three profiles: ghost_track (recency 0.5 + geo proximity 0.3 + semantic 0.2), iw_engine (semantic 0.5 + PMESII-PT dimension overlap 0.5 using dominant WRI indicators from sg_wri_assessments), strategy_advisor (doctrine relevance 0.8 + semantic 0.2 with doctrine keyword density scoring). Each profile retrieves 3× top_k candidates from base RAGRetriever (rerank=False), applies profile-specific scoring to final_score, stores component scores in metadata["_scores"]. Context keys: ghost_track {ref_lat, ref_lon, aoi}; iw_engine {pmesii_dominant: list[str]}. | --query TEXT --profile ghost_track|iw_engine|strategy_advisor [--top-k N] [--ref-lat F --ref-lon F --aoi STR] [--pmesii-dominant DIMS] [--json] | List[SearchResult] re-scored by profile; JSON {classification, query, profile, results_count, results[]} |
| Temporal Correlator | tools/strategos/temporal_correlator.py | Matches CyberOperation events (event_type=cyber_op) to ConflictEvents (event_type=narrative_event) in sg_conflict_events within a configurable time window (default 72h). Upserts CyberOperation and ConflictEvent nodes into canvas_kg_nodes, then writes CORRELATES edges to canvas_kg_edges using deterministic IDs (corr_<cyber_op_id>_<conflict_event_id>) — safe to re-run. Canvas=sg, design_id=temporal-correlator-v1. | --run \| --dry-run [--window-hours N] [--json] [-v] | {dry_run, window_hours, cyber_ops, conflict_events, pairs_found, edges_written, edges_skipped} |
| Analyst Annotation Layer | tools/intelligence/annotations.py | Allows analysts to attach typed annotations to any KG node, conflict event, ORBAT entry, or supply chain node in the Strategos knowledge model (table: sg_analyst_annotations, append-only per NIST AU-9). Annotation types: assessed, unconfirmed, disputed, source_tag. Entity types: kg_node, conflict_event, orbat_unit, supply_node, signal. Content sanitised and capped at 2 000 chars. Exposes add_annotation(), get_annotations(), get_annotation_counts() (batch, avoids N+1), get_annotation_summary(). | Python API (no CLI) | add_annotation → {id, entity_type, entity_id, annotation_type, content, analyst_id, created_at}; get_annotations → list[dict] newest-first; get_annotation_counts → {entity_id: count}; get_annotation_summary → {entity_type, entity_id, total, by_type, latest} |
| Adversarial Data Validator | icdev/tools/strategos/adversarial_validator.py | Deterministic pipeline that screens unstructured OSINT sources (social_media, satellite_imagery, drone_feeds) for bias, deepfake indicators, and adversarial text manipulation before they enter the predictive analysis engine. Three sub-detectors: BiasDetector (weighted regex heuristics for sentiment skew, demographic bias, political bias), DeepfakeDetector (metadata inconsistency + compression anomaly heuristics), ManipulationDetector (homoglyphs, invisible characters, unicode confusables). Pipeline outputs ValidationResult with flag/block status and append-only audit event (sg_adversarial_validation_audit, NIST AU-9). CLI: --signal, --signals-file, --json, --health, --gate. Classification: CUI // SP-CTI. | --signal JSON, --signals-file PATH, --json, --health, --gate | ValidationResult / batch envelope JSON: {total, flagged, clean, results[{signal_id, flagged, status, issues, bias_score, deepfake_score, manipulation_score}]}; audit_event → {id, timestamp, signal_id, status, issues, scores, details} |
| OODA Tempo Analyzer | tools/strategos/ooda.py | Measures OODA decision-cycle speed for Blue and Red forces across 6 domains (land, air, sea, cyber, ew, space). Computes per-phase latencies (observe/orient/decide/act), cycle_time_s, tempo_score (0–1, higher=faster), and command_interval_s from sg_ooda_events; falls back to open-source doctrine baselines when no DB data exists. Also provides: Lanchester square-law and linear-law attrition models (time-series + winner/margin), Colonel Blotto force-allocation game (per-battlefield outcomes + aggregate payoff), pure and mixed Nash equilibrium finder for n×m bimatrix games, and Course-of-Action (COA) scoring/ranking across 6 criteria (speed, surprise, mass, economy_of_force, maneuver, sustainability). | --wargame-id ID, --lanchester, --b0/--r0/--beta/--rho (float), --json; Python API: compute_tempo(wargame_id), lanchester_square(), lanchester_linear(), blotto_expected_payoff(blue_forces, red_forces), find_nash_2x2(payoff_blue, payoff_red), score_coa(coas, weights) | compute_tempo → {domains, blue, red, advantage{domain: blue|red|parity}, radar_data{labels,blue[],red[]}, generated_at}; lanchester → {model, t_series, b_series, r_series, winner, margin_pct, final_b, final_r}; blotto → {battlefields[], blue_wins, red_wins, ties, blue_score, red_score, overall_winner}; find_nash_2x2 → {pure_equilibria[], mixed_equilibrium}; score_coa → sorted list with composite_score and rank |
| War Council Engine | tools/strategos/war_council.py | Generates doctrine-grounded War Council strategic briefs (sg-rag-05). Retrieves Clausewitz CoG passage, FM/JP doctrine citations scored by theater+scenario, and historical precedent quotes from a built-in offline corpus (no DB required). Enriches COA narratives via StrategyAgent (live RAG when DB is seeded; falls back to deterministic stub COAs if Ollama is unavailable). Renders output via war_council.md.j2 Jinja2 template. Optionally persists to sg_intelligence_briefs_war_council (or sg_intelligence_briefs fallback) with --save. Entry points: WarCouncilEngine.generate(WarCouncilRequest), convenience wrapper generate_war_council_brief(). | --scenario TEXT (required), --theater LABEL (default: unspecified), --intent TEXT, --pmesii-summary TEXT, --adversary-cog TEXT, --friendly-cog TEXT, --save, --json, --markdown | WarCouncilBrief: {brief_id, content_md (rendered Markdown), theater, doctrine_citations, historical_precedents, strategy_result, generated_at, error}; --json: {brief_id, theater, doctrine_citations_count, historical_precedents_count, rag_active, rag_doc_count, coa_count, recommended_coa, model_used, latency_ms, generated_at, error, content_md} |
| War Council Brief Generator | tools/strategos/war_council_generator.py | Assembles full strategic assessment briefs by combining Clausewitz CoG doctrine passages, scenario-matched doctrine citations, historical precedent quotes, and LLM COA narratives grounded in retrieved doctrine. Renders output via war_council.md.j2 Jinja2 template; optionally persists to sg_intelligence_briefs. Entry point: generate_war_council_brief(theater, scenario, commander_intent, pmesii_summary, save). Classification: CUI // SP-CTI. | --theater TEXT (required), --scenario TEXT (required), --intent TEXT, --pmesii TEXT, --no-save, --json; Python API: generate_war_council_brief(theater, scenario, commander_intent, pmesii_summary, save) | {brief_id, theater, scenario, content_md, doctrine_citations, historical_precedents, generated_at, error}; --json: summary dict without content_md |
| CCIR Trigger Engine | tools/strategos/ccir_trigger.py | Auto-evaluates active CCIRs against recent SIGINT/EO/SOCMINT signals. Scans last N hours of signals for keyword matches against active CCIR topics; records trigger events in sg_ccir_trigger_events. Entry points: evaluate_ccirs(hours, dry_run), CLI --hours / --dry-run / --json. | --hours N (default: 24), --dry-run, --json | {status, ccirs_evaluated, triggered[], inserted, dry_run} |
| INTSUM Auto-Generator | tools/strategos/intsum.py | Builds a 6-paragraph Daily Intelligence Summary (INTSUM) by aggregating recent SIGINT, vessel/air tracks, ORBAT changes, and threat assessments; uses LLM to synthesize each paragraph. Paragraphs: Situation Overview, Ground, Maritime, Air/Space, Cyber/EW, Outlook. Persists to sg_intelligence_briefs with brief_type='intsum'. | --wargame-id ID, --hours N (default: 24), --no-llm, --save, --json | {brief_id, paragraphs{p1..p6}, generated_at, wargame_id, error} |
| Copernicus EO Importer | tools/strategos/eo_importer.py | Copernicus Data Space Sentinel-2 L2A EO scene importer for GeoSIGINT theater AOIs. Queries the public Copernicus OData API (no auth required) for scenes intersecting each active theater's area_wkt; filters cloud_pct < 20 and sensing_date within the last N days. Writes to sg_eo_signals via get_connection(); promotes scenes with relevance_score >= 0.5 to sg_osint_results as geo_intelligence findings (finding_type=geo_intelligence, source=copernicus). Deduplicates via Copernicus UUID primary key (INSERT OR IGNORE). Falls back with status='api_unavailable' if network is unreachable. | --theater CODE (e.g. INDOPACOM), --days N (default: 30), --dry-run, --list, --json, -v | {status (ok|api_unavailable|dry_run|no_theaters), theaters, scenes_fetched, inserted, skipped, findings_inserted, scan_id}; --dry-run also includes signals[]; --list prints sg_eo_signals table to stdout |
| F3EAD Targeting Engine | tools/strategos/f3ead.py | F3EAD (Find, Fix, Finish, Exploit, Analyze, Disseminate) targeting workflow. Manages the full kinetic/non-kinetic targeting lifecycle: create_target(), advance_phase(), record_exploitation(), finalize_dissemination(). Persists targeting packages to sg_f3ead_targets. Entry points: Python API (no CLI). | Python API: create_target(name, type, geo_hint, theater), advance_phase(target_id, phase), record_exploitation(target_id, findings), finalize_dissemination(target_id) | {target_id, phase, status, created_at} |
| IPB Engine | tools/strategos/ipb.py | Intelligence Preparation of the Battlespace (FM 2-01.3) four-step doctrinal workflow: Step 1 Define Battlespace Environment, Step 2 Describe Battlespace Effects, Step 3 Evaluate Threat, Step 4 Determine Threat COA. Aggregates theater data (SIGINT, ORBAT, terrain) and uses LLM to synthesize each step. Persists to sg_ipb_worksheets. | --theater TEXT, --dry-run, --json | {worksheet_id, steps[{num, title, content}], theater, generated_at, error} |
| ISR Collection Planner | tools/strategos/isr_planner.py | ISR (Intelligence, Surveillance, Reconnaissance) collection planner mapping Priority Intelligence Requirements (PIR/CCIR) to available collection assets with gap analysis. create_requirement() registers a new collection tasking; assign_asset() matches assets to requirements; compute_gaps() identifies uncovered PIRs. Persists to sg_isr_requirements, sg_isr_assets, sg_isr_gaps. | Python API: create_requirement(topic, priority, theater), assign_asset(req_id, asset), compute_gaps() | {requirement_id, gaps[], coverage_pct} |
| I&W Indicators Board | tools/strategos/iw_indicators.py | Indicators and Warnings (I&W) system. Manages a board of threat indicators with observation recording, threshold alerting, and indicator lifecycle (draft→active→triggered→closed). create_indicator() registers a new I&W indicator; record_observation() logs an observation and auto-triggers when count crosses threshold; list_triggered() returns currently active triggers. Persists to sg_iw_indicators and sg_iw_observations. | Python API: create_indicator(text, category, threshold, theater), record_observation(indicator_id, source, evidence), list_triggered() | {indicator_id, triggered: bool, observations_count} |
| METT-TC Worksheet | tools/strategos/mett_tc.py | METT-TC Mission Analysis Framework (FM 6-0 / MDMP Step 2). auto_populate(theater) pulls live DB data (ORBAT counts, active PIRs, signal counts, CCIR triggers, terrain) to pre-fill all six factors: Mission, Enemy, Terrain & Weather, Troops & Support Available, Time, Civil Considerations. Returns a structured worksheet dict suitable for OPORD generation or commander review. | --theater TEXT (default: unspecified), --json | {mission, enemy, terrain_weather, troops, time, civil, theater, populated_at} |
| OPORD Generator | tools/strategos/opord.py | Five-Paragraph Operations Order (OPORD) generator (FM 5-0 / JP 5-0). Synthesizes a full OPORD from METT-TC data, War Council COA, and INTSUM into the standard format: Para 1 Situation, Para 2 Mission, Para 3 Execution (with COA, tasks, coordinating instructions), Para 4 Sustainment, Para 5 Command & Signal. Uses LLM to draft each paragraph; persists to sg_opords. | --theater TEXT, --scenario TEXT, --intent TEXT, --dry-run, --json | {opord_id, paragraphs[{num, title, content}], theater, generated_at, error} |
| Synchronization Matrix | tools/strategos/sync_matrix.py | Time-phased operational effects synchronization matrix (MDMP). create_matrix() initializes a matrix for a given operation; add_effect() records a planned effect at a specific phase/time against a target; render_matrix() returns the full matrix as a list of rows sorted by phase and time. Persists to sg_sync_matrices and sg_sync_effects; supports export to Markdown table. | Python API: create_matrix(operation_name, theater), add_effect(matrix_id, phase, time, target, effect, asset), render_matrix(matrix_id) | {matrix_id, rows[{phase, time, target, effect, asset}], operation_name} |

| OSINT Satellite Analyzer | tools/strategos/osint_analyzer.py | Bias and deepfake detection for satellite imagery. Implements heuristic checks in two layers: (1) metadata integrity — timestamp consistency (future/pre-satellite-era/processing-before-sensing), sensor calibration (cloud_pct, relevance_score, resolution_m, incidence_angle range validation), and field completeness; (2) pixel-level anomaly detection — block uniformity (unnaturally flat 8×8 blocks indicating synthetic fill), noise consistency (inter-quadrant noise ratio > 3× indicating splicing), and histogram over-quantization (< 16 unique values indicating AI generation). Returns AnalysisResult with manipulation_likely bool, confidence score, and ordered SatelliteFlag list. | analyze_metadata(metadata_dict), analyze_pixels(2D_array), analyze(metadata, pixels) | AnalysisResult {manipulation_likely, confidence, flags[{rule, severity, detail}]}; severity levels: LOW/MEDIUM/HIGH/CRITICAL |
| Adversarial Validation Pipeline | tools/strategos/adversarial_validation.py | TDD-built adversarial data validation pipeline for OSINT sources. Detects source-concentration bias (statistical), sentiment skew, deepfake metadata anomalies (missing EXIF, hash mismatch, small file size), and adversarial text manipulation (character substitution, prompt injection, data poisoning patterns). Orchestrated by AdversarialValidationPipeline which produces a ValidationReport with block_reasons. Thresholds: bias 0.75, deepfake 0.70, manipulation 0.60. Classification: CUI // SP-CTI. NIST: SA-11, CM-3, SI-3, SI-10. | Python API: BiasDetector().detect(signals), DeepfakeDetector().detect(media), AdversarialManipulationDetector().detect(text), AdversarialValidationPipeline().validate(data) | PipelineResult {is_valid, block_reasons[], report{bias_result, deepfake_result, manipulation_result}}; JSON serializable via to_dict(). |

## STRATEGOS Foundation Layer (sg-foundation epic)
| Tool | File | Description | Input | Output |
|------|------|-------------|-------|--------|
| Theater Abstraction Layer | tools/strategos/theater.py | Conflict-agnostic theater base class for all 14 STRATEGOS epics. Loads theater YAML from args/theaters/<id>.yaml. Exposes TheaterBounds, TheaterActor, OrbatUnit, EscalationLevel dataclasses. Registry auto-loads all YAMLs in args/theaters/. DOMAIN_REGISTRY maps 8 domains (land/sea/air/cyber/space/ew/info/supply) to importers + agents. | `Theater.load(theater_id)`, `get_theater(id)`, `list_theaters()` | Theater dataclass / summary dict |
| War Knowledge Graph Schema | tools/strategos/war_kg.py | Defines 9 war-domain KG node types (MilitaryUnit, Equipment, ConflictEvent, CyberOperation, Vessel, Infrastructure, LogisticsNode, BehaviorProfile, WarReadinessEvent) and 7 edge types (DEPLOYED_AT, ATTACKED, PRECEDES, CORRELATES, LAUNCHED_FROM, SUPPLIES, DAMAGED). Uses existing icdev.db kg_nodes/kg_edges tables (migration 031). Graph ID: strategos-war-kg. | `ensure_war_graph()`, `upsert_node(entity_type, label, props)`, `upsert_edge(src, tgt, rel)`, `get_schema_summary()` | node_id str / edge_id str / schema dict |
| Vendor Bundle Fetcher | apps/strategos/static/fetch_vendor.py | Downloads all 8 air-gap JS/CSS vendor assets (leaflet, pmtiles, d3, plotly, cytoscape, vis-network, vis-timeline, chart.js) into apps/strategos/static/js+css/. Run once on network-connected machine; templated paths then work air-gapped. | `python apps/strategos/static/fetch_vendor.py [--dry-run]` | Downloaded files in static/js/ and static/css/ |

## STRATEGOS Core DB Tables (migration 118)
| Table | Description |
|-------|-------------|
| sg_theaters | Theater registry (pacific, ukraine, ...) — id, bounds_json, actors_json, active_domains |
| sg_entities | ORBAT units, vessels, aircraft, installations — theater_id, entity_type, actor, domain, lat/lon, status |
| sg_conflict_events | ACLED/Oryx/OSINT events — theater_id, event_type, date, lat/lon, fatalities, source |
| sg_tracks | Live AIS/ADS-B/TLE position tracks — entity_id, mmsi, icao24, lat/lon, speed, heading, timestamp |
| sg_economic_signals | Economic indicator time-series — indicator, country, date, value, cusum_alert |
| sg_war_readiness_events | Append-only I&W composite event log (NIST AU) — event_class, domain, composite_score, escalation_lvl |
| sg_ghost_tracks | Behavioral anomaly / ghost track registry — mmsi, anomaly_type, confidence, kalman_state, hitl_status |
| sg_coa_decisions | COA decision capture from HITL agent — operator, coa_selected, rationale, escalation_lvl |

## Theater YAML Files (args/theaters/)
| File | Theater | Status |
|------|---------|--------|
| args/theaters/pacific.yaml | Indo-Pacific (Taiwan Strait) | Full — 6 actors, weapon systems, 10-level escalation |
| args/theaters/ukraine.yaml | Russo-Ukrainian Conflict | Stub — 4 actors, 8 domains, 10-level escalation, placeholder ORBAT |

## STRATEGOS I&W Analysis Layer (sg-iw + sg-import epics)
| Tool | File | Description | Key Functions |
|------|------|-------------|---------------|
| ACLED Importer | tools/strategos/acled_importer.py | Parses ACLED CSV/JSON → sg_conflict_events (source='acled'). Deduplication on external_id. | `parse_csv()`, `parse_json_file()`, `write_events()` |
| Oryx Importer | tools/strategos/oryx_importer.py | Parses Oryx equipment loss JSON → sg_entities (Equipment) + sg_conflict_events. | `parse_oryx_json()`, `write_all()` |
| Frontline Importer | tools/strategos/frontline_importer.py | Parses GeoJSON FeatureCollections → sg_frontline_segments. Supports file or directory. | `load_file()`, `load_directory()`, `write_segments()` |
| Economic Importer | tools/strategos/economic_importer.py | WorldBank/FAO/JSON → sg_economic_signals with CUSUM anomaly detection. | `parse_worldbank_csv()`, `parse_fao_csv()`, `parse_generic_json()`, `write_signals()` |
| Historical Baselines | tools/strategos/historical_baselines.py | 23 historical pre-war case signatures → sg_war_readiness_events. Used for DTW + Bayesian training. | `seed_baselines()`, `get_pattern_matrix()`, `list_cases()` |
| IW Domain Scorers | tools/strategos/iw_scorers.py | Five PMESII-PT domain scorers: EconomicSignalScorer, MilitarySignalScorer, DiplomaticSignalScorer, InfrastructureScorer, InformationScorer. Each reads from live DB. | `score_all_domains(theater_id)` |
| Bayesian IW Updater | tools/strategos/iw_bayesian.py | Sequential Bayesian P(war) updater + pure-Python GBM Exercise vs Pre-War classifier (trained on 23 cases). | `BayesianIWUpdater.update()`, `ExerciseVsPreWarClassifier.predict()` |
| DTW Pattern Matcher | tools/strategos/iw_pattern_matcher.py | DTW similarity search against 23 historical cases + Weibull TTE estimator for onset probability. | `DTWPatternMatcher.find_similar()`, `WeibullTTEEstimator.estimate_days_to_onset()` |
| A2/AD Mapper | tools/strategos/a2ad_mapper.py | Computes PRC A2/AD threat rings (DF-21D/DF-26/DF-17/YJ-18/S-400) as GeoJSON + chokepoint analysis. | `A2ADMapper.compute_threat_rings()`, `analyze_chokepoints()`, `full_overlay()` |
| DAT Engine | tools/strategos/dat.py | Diplomatic Activity Tracker — ingests State Dept cable traffic, UNSC meeting schedules, and back-channel metadata; derives Diplomatic Tension Index (DTI) [0–100] on 6h cadence. Tables: sg_dat_cables, sg_dat_unsc_events, sg_dat_backchannel, sg_dat_dti_snapshots. | `ingest_cable()`, `ingest_unsc_event()`, `ingest_backchannel()`, `compute_dti()`, `refresh_dti()`, `get_latest_dti()`, `get_dti_history()` |
