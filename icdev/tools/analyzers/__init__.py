# CUI // SP-CTI
"""ICDEV™ analyzer/responder contract (anz-con-01).

The contract is declared as data in ``args/analyzer_contract.yaml``; this
package is only its loader and validator. See :mod:`tools.analyzers.contract`.
"""
