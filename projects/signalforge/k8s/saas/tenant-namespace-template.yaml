# [TEMPLATE: CUI // SP-CTI]
# SIGNALFORGE SaaS - Tenant Namespace Template
# Replace {{TENANT_SLUG}}, {{IMPACT_LEVEL}}, {{TIER}} before applying
#
# Isolation model:
#   IL2-IL4: Dedicated K8s namespace (signalforge-tenant-{slug})
#   IL5:     Dedicated namespace + dedicated node pool + dedicated RDS
#   IL6:     Dedicated AWS sub-account (SIPR air-gapped)
#
# Usage:
#   sed -e 's/{{TENANT_SLUG}}/acme-defense/g' \
#       -e 's/{{IMPACT_LEVEL}}/IL4/g' \
#       -e 's/{{TIER}}/professional/g' \
#       k8s/saas/tenant-namespace-template.yaml | kubectl apply -f -
#
# Or use the provisioner:
#   python tools/saas/infra/namespace_provisioner.py \
#       --create --slug acme-defense --il IL4 --tier professional
---
apiVersion: v1
kind: Namespace
metadata:
  name: signalforge-tenant-{{TENANT_SLUG}}
  labels:
    app.kubernetes.io/part-of: signalforge
    app.kubernetes.io/managed-by: signalforge-namespace-provisioner
    signalforge/tenant: "{{TENANT_SLUG}}"
    signalforge/impact-level: "{{IMPACT_LEVEL}}"
    signalforge/tier: "{{TIER}}"
    classification: CUI
---
# Default deny all traffic — zero-trust baseline
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: signalforge-tenant-{{TENANT_SLUG}}
  labels:
    app.kubernetes.io/part-of: signalforge
    classification: CUI
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress: []
---
# Allow ingress from API gateway (signalforge-platform namespace) only
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-gateway
  namespace: signalforge-tenant-{{TENANT_SLUG}}
  labels:
    app.kubernetes.io/part-of: signalforge
    classification: CUI
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: signalforge-platform
      ports:
        - protocol: TCP
          port: 9443
        - protocol: TCP
          port: 9444
        - protocol: TCP
          port: 9445
---
# Allow controlled egress (DNS, HTTPS to AWS, platform namespace)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-controlled-egress
  namespace: signalforge-tenant-{{TENANT_SLUG}}
  labels:
    app.kubernetes.io/part-of: signalforge
    classification: CUI
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    # DNS resolution
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # HTTPS to AWS endpoints (Bedrock, S3, Secrets Manager)
    - ports:
        - protocol: TCP
          port: 443
    # PostgreSQL to RDS
    - ports:
        - protocol: TCP
          port: 5432
    # Platform namespace communication
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: signalforge-platform
---
# Resource quota — values are for {{TIER}} tier
# Tier defaults: starter=2CPU/4Gi, professional=8CPU/16Gi, enterprise=32CPU/64Gi
apiVersion: v1
kind: ResourceQuota
metadata:
  name: tenant-quota
  namespace: signalforge-tenant-{{TENANT_SLUG}}
  labels:
    app.kubernetes.io/part-of: signalforge
    signalforge/tier: "{{TIER}}"
    classification: CUI
spec:
  hard:
    requests.cpu: "8"
    requests.memory: "16Gi"
    limits.cpu: "16"
    limits.memory: "32Gi"
    pods: "20"
    persistentvolumeclaims: "10"
    services: "10"
---
# Limit range — enforce per-pod defaults and maximums
apiVersion: v1
kind: LimitRange
metadata:
  name: tenant-limits
  namespace: signalforge-tenant-{{TENANT_SLUG}}
  labels:
    app.kubernetes.io/part-of: signalforge
    classification: CUI
spec:
  limits:
    - type: Container
      default:
        cpu: "500m"
        memory: "512Mi"
      defaultRequest:
        cpu: "100m"
        memory: "128Mi"
      max:
        cpu: "4"
        memory: "8Gi"
    - type: Pod
      max:
        cpu: "8"
        memory: "16Gi"
---
# Service account for tenant agents
apiVersion: v1
kind: ServiceAccount
metadata:
  name: signalforge-agent
  namespace: signalforge-tenant-{{TENANT_SLUG}}
  labels:
    app.kubernetes.io/part-of: signalforge
    signalforge/tenant: "{{TENANT_SLUG}}"
    classification: CUI
automountServiceAccountToken: false
