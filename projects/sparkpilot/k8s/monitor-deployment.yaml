# [TEMPLATE: CUI // SP-CTI]
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sparkpilot-monitor
  namespace: sparkpilot
  labels:
    app.kubernetes.io/name: monitor
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: sparkpilot
    classification: CUI
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: monitor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: monitor
        app.kubernetes.io/component: agent
        app.kubernetes.io/part-of: sparkpilot
        classification: CUI
    spec:
      serviceAccountName: sparkpilot-agent
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: monitor
          image: ${ECR_REGISTRY}/sparkpilot-agent:latest
          args: ["tools/a2a/agent_server.py", "--agent-id", "monitor-agent", "--port", "9450"]
          ports:
            - containerPort: 9450
              protocol: TCP
          envFrom:
            - configMapRef:
                name: sparkpilot-config
            - secretRef:
                name: sparkpilot-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: 9450
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 9450
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: monitor
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: monitor
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: sparkpilot-data
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: sparkpilot-monitor
  namespace: sparkpilot
  labels:
    classification: CUI
spec:
  selector:
    app.kubernetes.io/name: monitor
  ports:
    - port: 9450
      targetPort: 9450
      protocol: TCP
  type: ClusterIP
