# [TEMPLATE: CUI // SP-CTI]
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sparkpilot-modernization-agent
  namespace: sparkpilot
  labels:
    app: sparkpilot-modernization-agent
    tier: domain
    app.kubernetes.io/name: modernization-agent
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: sparkpilot
    classification: CUI
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sparkpilot-modernization-agent
  template:
    metadata:
      labels:
        app: sparkpilot-modernization-agent
        tier: domain
        app.kubernetes.io/name: modernization-agent
        app.kubernetes.io/component: agent
        app.kubernetes.io/part-of: sparkpilot
        classification: CUI
    spec:
      serviceAccountName: sparkpilot-agent
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: modernization-agent
          image: ${ECR_REGISTRY}/sparkpilot-modernization-agent:latest
          ports:
            - containerPort: 8452
              protocol: TCP
          envFrom:
            - configMapRef:
                name: sparkpilot-config
            - secretRef:
                name: sparkpilot-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 1000
          livenessProbe:
            httpGet:
              path: /health
              port: 8452
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8452
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: data
              mountPath: /app/data
            - name: tmp
              mountPath: /app/.tmp
            - name: certs
              mountPath: /etc/sparkpilot/certs
              readOnly: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: modernization-agent
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: modernization-agent
      volumes:
        - name: data
          emptyDir: {}
        - name: tmp
          emptyDir: {}
        - name: certs
          secret:
            secretName: modernization-agent-tls
---
apiVersion: v1
kind: Service
metadata:
  name: sparkpilot-modernization-agent
  namespace: sparkpilot
  labels:
    app: sparkpilot-modernization-agent
    tier: domain
    classification: CUI
spec:
  selector:
    app: sparkpilot-modernization-agent
  ports:
    - port: 8452
      targetPort: 8452
      protocol: TCP
  type: ClusterIP
# [TEMPLATE: CUI // SP-CTI]
