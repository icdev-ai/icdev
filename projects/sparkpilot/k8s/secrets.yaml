# [TEMPLATE: CUI // SP-CTI]
# NOTE: In production, use AWS Secrets Manager via External Secrets Operator
# This file is a template — never commit actual secrets
apiVersion: v1
kind: Secret
metadata:
  name: sparkpilot-secrets
  namespace: sparkpilot
  labels:
    app.kubernetes.io/part-of: sparkpilot
    classification: CUI
type: Opaque
stringData:
  OPENAI_API_KEY: "${OPENAI_API_KEY}"
  AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID}"
  AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY}"
  SPLUNK_HEC_TOKEN: "${SPLUNK_HEC_TOKEN}"
  SLACK_WEBHOOK_URL: "${SLACK_WEBHOOK_URL}"
