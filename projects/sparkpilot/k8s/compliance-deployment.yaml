# [TEMPLATE: CUI // SP-CTI]
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sparkpilot-compliance
  namespace: sparkpilot
  labels:
    app.kubernetes.io/name: compliance
    app.kubernetes.io/component: agent
    app.kubernetes.io/part-of: sparkpilot
    classification: CUI
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: compliance
  template:
    metadata:
      labels:
        app.kubernetes.io/name: compliance
        app.kubernetes.io/component: agent
        app.kubernetes.io/part-of: sparkpilot
        classification: CUI
    spec:
      serviceAccountName: sparkpilot-agent
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: compliance
          image: ${ECR_REGISTRY}/sparkpilot-agent:latest
          args: ["tools/a2a/agent_server.py", "--agent-id", "compliance-agent", "--port", "9446"]
          ports:
            - containerPort: 9446
              protocol: TCP
          envFrom:
            - configMapRef:
                name: sparkpilot-config
            - secretRef:
                name: sparkpilot-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: 9446
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 9446
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: compliance
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: compliance
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: sparkpilot-data
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: sparkpilot-compliance
  namespace: sparkpilot
  labels:
    classification: CUI
spec:
  selector:
    app.kubernetes.io/name: compliance
  ports:
    - port: 9446
      targetPort: 9446
      protocol: TCP
  type: ClusterIP
