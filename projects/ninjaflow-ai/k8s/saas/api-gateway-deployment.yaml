# [TEMPLATE: CUI // SP-CTI]
# NINJAFLOW_AI SaaS - API Gateway Deployment
# Routes tenant requests to isolated namespaces, enforces auth + rate limiting
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ninjaflow_ai-api-gateway
  namespace: ninjaflow_ai-platform
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: ninjaflow_ai
    classification: CUI
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: api-gateway
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: api-gateway
        app.kubernetes.io/component: gateway
        app.kubernetes.io/part-of: ninjaflow_ai
        classification: CUI
    spec:
      serviceAccountName: ninjaflow_ai-api-gateway
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: api-gateway
          image: ${ECR_REGISTRY}/ninjaflow_ai-api-gateway:latest
          ports:
            - containerPort: 9443
              protocol: TCP
          envFrom:
            - configMapRef:
                name: ninjaflow_ai-gateway-config
            - secretRef:
                name: ninjaflow_ai-gateway-secrets
          env:
            - name: PLATFORM_DB_URL
              valueFrom:
                secretKeyRef:
                  name: ninjaflow_ai-platform-db
                  key: url
            - name: LOG_LEVEL
              value: "INFO"
            - name: WORKERS
              value: "4"
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /health
              port: 9443
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 9443
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: tls-certs
              mountPath: /etc/tls
              readOnly: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: api-gateway
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: api-gateway
      volumes:
        - name: tmp
          emptyDir: {}
        - name: tls-certs
          secret:
            secretName: ninjaflow_ai-gateway-tls
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: api-gateway
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: ninjaflow_ai-api-gateway
  namespace: ninjaflow_ai-platform
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: ninjaflow_ai
    classification: CUI
spec:
  selector:
    app.kubernetes.io/name: api-gateway
  ports:
    - name: https
      port: 9443
      targetPort: 9443
      protocol: TCP
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ninjaflow_ai-api-gateway
  namespace: ninjaflow_ai-platform
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: ninjaflow_ai
    classification: CUI
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/rate-limit-connections: "50"
    nginx.ingress.kubernetes.io/rate-limit-rps: "100"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.ninjaflow_ai.gov
      secretName: ninjaflow_ai-gateway-tls
  rules:
    - host: api.ninjaflow_ai.gov
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ninjaflow_ai-api-gateway
                port:
                  number: 9443
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ninjaflow_ai-api-gateway
  namespace: ninjaflow_ai-platform
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: ninjaflow_ai
    classification: CUI
automountServiceAccountToken: false
