# {{ display_name }} defaults
# Generated from data/templates/canvases/info_ops

project:
  key: {{ key }}
  display_name: "{{ display_name }}"
  url_prefix: "{{ url_prefix }}"
  env_flag: "{{ env_flag }}"

security:
  classification_default: CUI
  cui_banner: true

logging:
  level: INFO
