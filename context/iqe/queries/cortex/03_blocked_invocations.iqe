# IQE-CORTEX-03: Governance-blocked facade invocations
# Facade calls the TRUST governance pipeline blocked, with reason.

foreach a in cortex.audit
where a.blocked == true
select a.facade, a.outcome, a.detail, a.session_id, a.created_at
