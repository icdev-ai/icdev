# IQE-QDC-004: Recent quality-twin snapshots
# Lists the most recent QDC digital-twin snapshots (gate topology freezes).

foreach s in qdc.twin_snapshots
select s.id, s.design_id, s.label, s.node_count, s.edge_count, s.created_at
