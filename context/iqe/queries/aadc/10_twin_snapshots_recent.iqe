# IQE-AADC-010: Recent agent-topology twin snapshots
# Lists the most recent AADC digital-twin snapshots (agent topology freezes).

foreach s in aadc.twin_snapshots
select s.id, s.design_id, s.label, s.node_count, s.edge_count, s.created_at
